// Copyright (c) ZeroC, Inc.

// slice2js version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Key.ice'.</auto-generated>

/* eslint-disable */
/* jshint ignore: start */

import { Ice } from "@zeroc/ice";

export const abstract = {};

abstract.assert = Ice.defineEnum([
    ['boolean', 0]]);

abstract.break = class
{
    constructor(case = 0)
    {
        this.case = case;
    }

    _write(ostr)
    {
        ostr.writeInt(this.case);
    }

    _read(istr)
    {
        this.case = istr.readInt();
    }

    static get minWireSize()
    {
        return  4;
    }
};

Ice.defineStruct(abstract.break, true, false);

const iceC_abstract_catch_ids = [
    "::Ice::Object",
    "::abstract::catch"
];

abstract.catch = class extends Ice.Object
{
};

abstract.catchPrx = class extends Ice.ObjectPrx
{
};

Ice.TypeRegistry.declareProxyType("abstract.catchPrx", abstract.catchPrx);

Ice.defineOperations(
    abstract.catch,
    abstract.catchPrx,
    iceC_abstract_catch_ids,
    "::abstract::catch",
    {
        "checkedCast": [, 0, , , [[3]], [[3]], , , ]
    });

const iceC_abstract_default_ids = [
    "::Ice::Object",
    "::abstract::default"
];

abstract.default = class extends Ice.Object
{
};

abstract.defaultPrx = class extends Ice.ObjectPrx
{
};

Ice.TypeRegistry.declareProxyType("abstract.defaultPrx", abstract.defaultPrx);

Ice.defineOperations(
    abstract.default,
    abstract.defaultPrx,
    iceC_abstract_default_ids,
    "::abstract::default",
    {
        "do": [, 0, , , , , , , ]
    });

abstract.notify = class extends Ice.Value
{
    constructor(if = 0, equals = null)
    {
        super();
        this.if = if;
        this.equals = equals;
    }

    _iceWriteMemberImpl(ostr)
    {
        ostr.writeInt(this.if);
        ostr.writeProxy(this.equals);
    }

    _iceReadMemberImpl(istr)
    {
        this.if = istr.readInt();
        this.equals = istr.readProxy();
    }
};

Ice.defineClass(abstract.notify, "::abstract::notify");

Ice.TypeRegistry.declareValueType("abstract.notify", abstract.notify);

abstract.hashCode = class extends Ice.UserException
{
    constructor(if = 0, _cause = "")
    {
        super(_cause);
        this.if = if;
    }

    static get _parent()
    {
        return Ice.UserException;
    }

    static get _ice_id()
    {
        return "::abstract::hashCode";
    }

    _mostDerivedType()
    {
        return abstract.hashCode;
    }

    _writeMemberImpl(ostr)
    {
        ostr.writeInt(this.if);
    }

    _readMemberImpl(istr)
    {
        this.if = istr.readInt();
    }
};

Ice.defineClass(abstract.hashCode, "::abstract::hashCode");

abstract.clone = class extends abstract.hashCode
{
    constructor(if, native = "", _cause = "")
    {
        super(if, _cause);
        this.native = native;
    }

    static get _parent()
    {
        return abstract.hashCode;
    }

    static get _ice_id()
    {
        return "::abstract::clone";
    }

    _mostDerivedType()
    {
        return abstract.clone;
    }

    _writeMemberImpl(ostr)
    {
        ostr.writeString(this.native);
    }

    _readMemberImpl(istr)
    {
        this.native = istr.readString();
    }
};

Ice.defineClass(abstract.clone, "::abstract::clone");

const iceC_abstract_finalize_ids = [
    "::Ice::Object",
    "::abstract::catch",
    "::abstract::default",
    "::abstract::finalize"
];

abstract.finalize = class extends Ice.Object
{
    static get _iceImplements()
    {
        return [
            abstract.default,
            abstract.catch
        ];
    }
};

abstract.finalizePrx = class extends Ice.ObjectPrx
{
    static get _implements()
    {
        return [
            abstract.defaultPrx,
            abstract.catchPrx];
    }
};

Ice.TypeRegistry.declareProxyType("abstract.finalizePrx", abstract.finalizePrx);

Ice.defineOperations(
    abstract.finalize,
    abstract.finalizePrx,
    iceC_abstract_finalize_ids,
    "::abstract::finalize",
    {
        "notify": [, 0, , [abstract.assert._helper], [[abstract.break], ["abstract.notify", true], ["abstract.finalizePrx"], ["abstract.defaultPrx"], [3]], ,
        [
            abstract.clone,
            abstract.hashCode
        ], true, ]
    });

abstract.forHelper = Ice.StreamHelpers.generateSeqHelper(abstract.assert._helper, false);

[abstract.goto, abstract.gotoHelper] = Ice.defineDictionary(Ice.StringHelper, abstract.assert._helper, false, undefined);

Object.defineProperty(abstract, 'synchronized', {
    enumerable: true,
    value: 0
});
