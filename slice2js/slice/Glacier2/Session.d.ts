// Copyright (c) ZeroC, Inc.

// slice2js version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Session.ice'.</auto-generated>


declare module "@zeroc/ice"
{
    namespace Glacier2
    {
        /**
         * The exception that is thrown when an attempt to create a new session failed.
         **/
        export class CannotCreateSessionException extends Ice.UserException
        {
            /**
             * One-shot constructor to initialize all data members.
             * @param reason The reason why the session creation failed.
             */
            constructor(reason?: string);
            reason: string;
        }

        /**
         * Represents a session between a client application and the Glacier2 router. With Glacier2, the lifetime of a
         * session is tied to the Ice connection between the client and the router: the session is destroyed when the
         * connection is closed.
         * @see Router
         * @see SessionManager
         **/
        export class SessionPrx extends Ice.ObjectPrx
        {
            /**
             * Constructs a new SessionPrx proxy.
             * @param communicator - The communicator for the new proxy.
             * @param proxyString - The string representation of the proxy.
             * @returns The new SessionPrx proxy.
             * @throws ParseException - Thrown if the proxyString is not a valid proxy string.
             */
            constructor(communicator: Ice.Communicator, proxyString: string);

            /**
             * Constructs a new SessionPrx proxy from an ObjectPrx. The new proxy is a clone of the
             * provided proxy.
             * @param prx - The proxy to clone.
             * @returns The new SessionPrx proxy.
             */
            constructor(prx: Ice.ObjectPrx);

            /**
             * Destroys this session.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             */
            destroy(context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @returns A proxy with the requested type.
             */
            static uncheckedCast(prx: Ice.ObjectPrx, facet?: string): SessionPrx;

            /**
             * Downcasts a proxy after confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet A facet name.
             * @param context The request context.
             * @returns A proxy with the requested type and facet, or nil if the target proxy is nil or the target
             * object does not support the requested type.
             */
            static checkedCast(prx: Ice.ObjectPrx, facet?: string, context?: Map<string, string>): Ice.AsyncResult<SessionPrx | null>;
        }

        /**
         * Represents a session between a client application and the Glacier2 router. With Glacier2, the lifetime of a
         * session is tied to the Ice connection between the client and the router: the session is destroyed when the
         * connection is closed.
         * @see Router
         * @see SessionManager
         **/
        export abstract class Session extends Ice.Object
        {
            /**
             * Destroys this session.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             */
            abstract destroy(current: Ice.Current): PromiseLike<void>|void;

            /**
             * Obtains the Slice type ID of this type.
             * @returns The return value is always "::Glacier2::Session".
             */
            static ice_staticId(): string;
        }

        /**
         * Manages a set of constraints on a {@link Glacier2.SessionPrx}.
         * @see SessionControl
         **/
        export class StringSetPrx extends Ice.ObjectPrx
        {
            /**
             * Constructs a new StringSetPrx proxy.
             * @param communicator - The communicator for the new proxy.
             * @param proxyString - The string representation of the proxy.
             * @returns The new StringSetPrx proxy.
             * @throws ParseException - Thrown if the proxyString is not a valid proxy string.
             */
            constructor(communicator: Ice.Communicator, proxyString: string);

            /**
             * Constructs a new StringSetPrx proxy from an ObjectPrx. The new proxy is a clone of the
             * provided proxy.
             * @param prx - The proxy to clone.
             * @returns The new StringSetPrx proxy.
             */
            constructor(prx: Ice.ObjectPrx);

            /**
             * Adds a sequence of strings to this set of constraints. Order is not preserved and duplicates are implicitly
             * removed.
             * @param additions The sequence of strings to add.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             */
            add(additions: string[], context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Removes a sequence of strings from this set of constraints. No errors are returned if an entry is not found.
             * @param deletions The sequence of strings to remove.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             */
            remove(deletions: string[], context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Gets a sequence of strings describing the constraints in this set.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - string[] : The sequence of strings for this set.
             */
            get(context?: Map<string, string>): Ice.AsyncResult<string[]>;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @returns A proxy with the requested type.
             */
            static uncheckedCast(prx: Ice.ObjectPrx, facet?: string): StringSetPrx;

            /**
             * Downcasts a proxy after confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet A facet name.
             * @param context The request context.
             * @returns A proxy with the requested type and facet, or nil if the target proxy is nil or the target
             * object does not support the requested type.
             */
            static checkedCast(prx: Ice.ObjectPrx, facet?: string, context?: Map<string, string>): Ice.AsyncResult<StringSetPrx | null>;
        }

        /**
         * Manages a set of constraints on a {@link Glacier2.SessionPrx}.
         * @see SessionControl
         **/
        export abstract class StringSet extends Ice.Object
        {
            /**
             * Adds a sequence of strings to this set of constraints. Order is not preserved and duplicates are implicitly
             * removed.
             * @param additions The sequence of strings to add.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             */
            abstract add(additions: string[], current: Ice.Current): PromiseLike<void>|void;

            /**
             * Removes a sequence of strings from this set of constraints. No errors are returned if an entry is not found.
             * @param deletions The sequence of strings to remove.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             */
            abstract remove(deletions: string[], current: Ice.Current): PromiseLike<void>|void;

            /**
             * Gets a sequence of strings describing the constraints in this set.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - string[] : The sequence of strings for this set.
             */
            abstract get(current: Ice.Current): PromiseLike<string[]>|string[];

            /**
             * Obtains the Slice type ID of this type.
             * @returns The return value is always "::Glacier2::StringSet".
             */
            static ice_staticId(): string;
        }

        /**
         * Manages a set of object identity constraints on a {@link Glacier2.SessionPrx}.
         * @see SessionControl
         **/
        export class IdentitySetPrx extends Ice.ObjectPrx
        {
            /**
             * Constructs a new IdentitySetPrx proxy.
             * @param communicator - The communicator for the new proxy.
             * @param proxyString - The string representation of the proxy.
             * @returns The new IdentitySetPrx proxy.
             * @throws ParseException - Thrown if the proxyString is not a valid proxy string.
             */
            constructor(communicator: Ice.Communicator, proxyString: string);

            /**
             * Constructs a new IdentitySetPrx proxy from an ObjectPrx. The new proxy is a clone of the
             * provided proxy.
             * @param prx - The proxy to clone.
             * @returns The new IdentitySetPrx proxy.
             */
            constructor(prx: Ice.ObjectPrx);

            /**
             * Adds a sequence of Ice identities to this set of constraints. Order is not preserved and duplicates are
             * implicitly removed.
             * @param additions The sequence of Ice identities to add.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             */
            add(additions: Ice.Identity[], context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Removes a sequence of identities from this set of constraints. No errors are returned if an entry is not
             * found.
             * @param deletions The sequence of Ice identities to remove.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             */
            remove(deletions: Ice.Identity[], context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Gets a sequence of identities describing the constraints in this set.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - Ice.Identity[] : The sequence of Ice identities for this set.
             */
            get(context?: Map<string, string>): Ice.AsyncResult<Ice.Identity[]>;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @returns A proxy with the requested type.
             */
            static uncheckedCast(prx: Ice.ObjectPrx, facet?: string): IdentitySetPrx;

            /**
             * Downcasts a proxy after confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet A facet name.
             * @param context The request context.
             * @returns A proxy with the requested type and facet, or nil if the target proxy is nil or the target
             * object does not support the requested type.
             */
            static checkedCast(prx: Ice.ObjectPrx, facet?: string, context?: Map<string, string>): Ice.AsyncResult<IdentitySetPrx | null>;
        }

        /**
         * Manages a set of object identity constraints on a {@link Glacier2.SessionPrx}.
         * @see SessionControl
         **/
        export abstract class IdentitySet extends Ice.Object
        {
            /**
             * Adds a sequence of Ice identities to this set of constraints. Order is not preserved and duplicates are
             * implicitly removed.
             * @param additions The sequence of Ice identities to add.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             */
            abstract add(additions: Ice.Identity[], current: Ice.Current): PromiseLike<void>|void;

            /**
             * Removes a sequence of identities from this set of constraints. No errors are returned if an entry is not
             * found.
             * @param deletions The sequence of Ice identities to remove.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             */
            abstract remove(deletions: Ice.Identity[], current: Ice.Current): PromiseLike<void>|void;

            /**
             * Gets a sequence of identities describing the constraints in this set.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - Ice.Identity[] : The sequence of Ice identities for this set.
             */
            abstract get(current: Ice.Current): PromiseLike<Ice.Identity[]>|Ice.Identity[];

            /**
             * Obtains the Slice type ID of this type.
             * @returns The return value is always "::Glacier2::IdentitySet".
             */
            static ice_staticId(): string;
        }

        /**
         * Represents a router-provided object that allows an application-provided session manager to configure the
         * routing constraints for a session.
         **/
        export class SessionControlPrx extends Ice.ObjectPrx
        {
            /**
             * Constructs a new SessionControlPrx proxy.
             * @param communicator - The communicator for the new proxy.
             * @param proxyString - The string representation of the proxy.
             * @returns The new SessionControlPrx proxy.
             * @throws ParseException - Thrown if the proxyString is not a valid proxy string.
             */
            constructor(communicator: Ice.Communicator, proxyString: string);

            /**
             * Constructs a new SessionControlPrx proxy from an ObjectPrx. The new proxy is a clone of the
             * provided proxy.
             * @param prx - The proxy to clone.
             * @returns The new SessionControlPrx proxy.
             */
            constructor(prx: Ice.ObjectPrx);

            /**
             * Gets a proxy to the object that manages the allowable categories for object identities for this session.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - __global_Glacier2.StringSetPrx | null : A proxy to a StringSet object. This proxy is never null.
             */
            categories(context?: Map<string, string>): Ice.AsyncResult<__global_Glacier2.StringSetPrx | null>;

            /**
             * Gets a proxy to the object that manages the allowable adapter identities for objects for this session.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - __global_Glacier2.StringSetPrx | null : A proxy to StringSet object. This proxy is never null.
             */
            adapterIds(context?: Map<string, string>): Ice.AsyncResult<__global_Glacier2.StringSetPrx | null>;

            /**
             * Gets a proxy to the object that manages the allowable object identities for this session.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - __global_Glacier2.IdentitySetPrx | null : A proxy to an IdentitySet object. This proxy is never null.
             */
            identities(context?: Map<string, string>): Ice.AsyncResult<__global_Glacier2.IdentitySetPrx | null>;

            /**
             * Gets the session timeout.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - number : The timeout.
             */
            getSessionTimeout(context?: Map<string, string>): Ice.AsyncResult<number>;

            /**
             * Destroys the associated session.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             */
            destroy(context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @returns A proxy with the requested type.
             */
            static uncheckedCast(prx: Ice.ObjectPrx, facet?: string): SessionControlPrx;

            /**
             * Downcasts a proxy after confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet A facet name.
             * @param context The request context.
             * @returns A proxy with the requested type and facet, or nil if the target proxy is nil or the target
             * object does not support the requested type.
             */
            static checkedCast(prx: Ice.ObjectPrx, facet?: string, context?: Map<string, string>): Ice.AsyncResult<SessionControlPrx | null>;
        }

        /**
         * Represents a router-provided object that allows an application-provided session manager to configure the
         * routing constraints for a session.
         **/
        export abstract class SessionControl extends Ice.Object
        {
            /**
             * Gets a proxy to the object that manages the allowable categories for object identities for this session.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - __global_Glacier2.StringSetPrx | null : A proxy to a StringSet object. This proxy is never null.
             */
            abstract categories(current: Ice.Current): PromiseLike<__global_Glacier2.StringSetPrx | null>|__global_Glacier2.StringSetPrx | null;

            /**
             * Gets a proxy to the object that manages the allowable adapter identities for objects for this session.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - __global_Glacier2.StringSetPrx | null : A proxy to StringSet object. This proxy is never null.
             */
            abstract adapterIds(current: Ice.Current): PromiseLike<__global_Glacier2.StringSetPrx | null>|__global_Glacier2.StringSetPrx | null;

            /**
             * Gets a proxy to the object that manages the allowable object identities for this session.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - __global_Glacier2.IdentitySetPrx | null : A proxy to an IdentitySet object. This proxy is never null.
             */
            abstract identities(current: Ice.Current): PromiseLike<__global_Glacier2.IdentitySetPrx | null>|__global_Glacier2.IdentitySetPrx | null;

            /**
             * Gets the session timeout.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - number : The timeout.
             */
            abstract getSessionTimeout(current: Ice.Current): PromiseLike<number>|number;

            /**
             * Destroys the associated session.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             */
            abstract destroy(current: Ice.Current): PromiseLike<void>|void;

            /**
             * Obtains the Slice type ID of this type.
             * @returns The return value is always "::Glacier2::SessionControl".
             */
            static ice_staticId(): string;
        }

        /**
         * Represents an application-provided factory for session objects. You can configure a Glacier2 router with your
         * own SessionManager implementation; this router will then return the sessions created by this session manager to
         * its clients.
         **/
        export class SessionManagerPrx extends Ice.ObjectPrx
        {
            /**
             * Constructs a new SessionManagerPrx proxy.
             * @param communicator - The communicator for the new proxy.
             * @param proxyString - The string representation of the proxy.
             * @returns The new SessionManagerPrx proxy.
             * @throws ParseException - Thrown if the proxyString is not a valid proxy string.
             */
            constructor(communicator: Ice.Communicator, proxyString: string);

            /**
             * Constructs a new SessionManagerPrx proxy from an ObjectPrx. The new proxy is a clone of the
             * provided proxy.
             * @param prx - The proxy to clone.
             * @returns The new SessionManagerPrx proxy.
             */
            constructor(prx: Ice.ObjectPrx);

            /**
             * Creates a new session object.
             * @param userId The user ID for the session.
             * @param control A proxy to the session control object. This proxy is null when `Glacier2.Server.Endpoints`
             * is not configured.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - __global_Glacier2.SessionPrx | null : A proxy to the newly created session. This proxy is never null.
             * @throws {@link Glacier2.CannotCreateSessionException} Thrown when the session cannot be created.
             */
            create(userId: string, control: __global_Glacier2.SessionControlPrx | null, context?: Map<string, string>): Ice.AsyncResult<__global_Glacier2.SessionPrx | null>;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @returns A proxy with the requested type.
             */
            static uncheckedCast(prx: Ice.ObjectPrx, facet?: string): SessionManagerPrx;

            /**
             * Downcasts a proxy after confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet A facet name.
             * @param context The request context.
             * @returns A proxy with the requested type and facet, or nil if the target proxy is nil or the target
             * object does not support the requested type.
             */
            static checkedCast(prx: Ice.ObjectPrx, facet?: string, context?: Map<string, string>): Ice.AsyncResult<SessionManagerPrx | null>;
        }

        /**
         * Represents an application-provided factory for session objects. You can configure a Glacier2 router with your
         * own SessionManager implementation; this router will then return the sessions created by this session manager to
         * its clients.
         **/
        export abstract class SessionManager extends Ice.Object
        {
            /**
             * Creates a new session object.
             * @param userId The user ID for the session.
             * @param control A proxy to the session control object. This proxy is null when `Glacier2.Server.Endpoints`
             * is not configured.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - __global_Glacier2.SessionPrx | null : A proxy to the newly created session. This proxy is never null.
             * @throws {@link Glacier2.CannotCreateSessionException} Thrown when the session cannot be created.
             */
            abstract create(userId: string, control: __global_Glacier2.SessionControlPrx | null, current: Ice.Current): PromiseLike<__global_Glacier2.SessionPrx | null>|__global_Glacier2.SessionPrx | null;

            /**
             * Obtains the Slice type ID of this type.
             * @returns The return value is always "::Glacier2::SessionManager".
             */
            static ice_staticId(): string;
        }

        /**
         * Represents an application-provided factory for session objects. You can configure a Glacier2 router with your
         * own SSLSessionManager implementation; this router will then return the sessions created by this session manager
         * to its clients.
         **/
        export class SSLSessionManagerPrx extends Ice.ObjectPrx
        {
            /**
             * Constructs a new SSLSessionManagerPrx proxy.
             * @param communicator - The communicator for the new proxy.
             * @param proxyString - The string representation of the proxy.
             * @returns The new SSLSessionManagerPrx proxy.
             * @throws ParseException - Thrown if the proxyString is not a valid proxy string.
             */
            constructor(communicator: Ice.Communicator, proxyString: string);

            /**
             * Constructs a new SSLSessionManagerPrx proxy from an ObjectPrx. The new proxy is a clone of the
             * provided proxy.
             * @param prx - The proxy to clone.
             * @returns The new SSLSessionManagerPrx proxy.
             */
            constructor(prx: Ice.ObjectPrx);

            /**
             * Creates a new session object.
             * @param info The SSL info.
             * @param control A proxy to the session control object. This proxy is null when `Glacier2.Server.Endpoints`
             * is not configured.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - __global_Glacier2.SessionPrx | null : A proxy to the newly created session. This proxy is never null.
             * @throws {@link Glacier2.CannotCreateSessionException} Thrown when the session cannot be created.
             */
            create(info: __global_Glacier2.SSLInfo, control: __global_Glacier2.SessionControlPrx | null, context?: Map<string, string>): Ice.AsyncResult<__global_Glacier2.SessionPrx | null>;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @returns A proxy with the requested type.
             */
            static uncheckedCast(prx: Ice.ObjectPrx, facet?: string): SSLSessionManagerPrx;

            /**
             * Downcasts a proxy after confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet A facet name.
             * @param context The request context.
             * @returns A proxy with the requested type and facet, or nil if the target proxy is nil or the target
             * object does not support the requested type.
             */
            static checkedCast(prx: Ice.ObjectPrx, facet?: string, context?: Map<string, string>): Ice.AsyncResult<SSLSessionManagerPrx | null>;
        }

        /**
         * Represents an application-provided factory for session objects. You can configure a Glacier2 router with your
         * own SSLSessionManager implementation; this router will then return the sessions created by this session manager
         * to its clients.
         **/
        export abstract class SSLSessionManager extends Ice.Object
        {
            /**
             * Creates a new session object.
             * @param info The SSL info.
             * @param control A proxy to the session control object. This proxy is null when `Glacier2.Server.Endpoints`
             * is not configured.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - __global_Glacier2.SessionPrx | null : A proxy to the newly created session. This proxy is never null.
             * @throws {@link Glacier2.CannotCreateSessionException} Thrown when the session cannot be created.
             */
            abstract create(info: __global_Glacier2.SSLInfo, control: __global_Glacier2.SessionControlPrx | null, current: Ice.Current): PromiseLike<__global_Glacier2.SessionPrx | null>|__global_Glacier2.SessionPrx | null;

            /**
             * Obtains the Slice type ID of this type.
             * @returns The return value is always "::Glacier2::SSLSessionManager".
             */
            static ice_staticId(): string;
        }
    }
    import __global_Glacier2 = Glacier2;
    import __global_Ice = Ice;
}
