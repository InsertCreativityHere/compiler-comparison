// Copyright (c) ZeroC, Inc.

// slice2js version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Router.ice'.</auto-generated>


declare module "@zeroc/ice" {
    namespace Glacier2 {
        /**
         * The exception that is thrown when a client tries to destroy a session with a router, but no session exists for
         * this client.
         * @see Router#destroySession
         */
        export class SessionNotExistException extends Ice.UserException {}

        /**
         * The Glacier2 specialization of the {@link Ice.RouterPrx} interface.
         */
        export class RouterPrx extends Ice.ObjectPrx implements __global_Ice.RouterPrx {
            /**
             * Constructs a new RouterPrx proxy.
             * @param communicator - The communicator for the new proxy.
             * @param proxyString - The string representation of the proxy.
             * @returns The new RouterPrx proxy.
             * @throws ParseException - Thrown if the proxyString is not a valid proxy string.
             */
            constructor(communicator: Ice.Communicator, proxyString: string);

            /**
             * Gets the router's client proxy, i.e., the proxy to use for forwarding requests from the client to the
             * router. If a null proxy is returned, the client will forward requests to the router's endpoints.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to an array with the following entries:
             * - Ice.ObjectPrx | null : The router's client proxy.
             * - boolean | undefined : Indicates whether or not the router supports a routing table. If `true`, the Ice
             *   runtime will call {@link Ice.RouterPrx.addProxies} to populate the routing table. The Ice runtime assumes the router has
             *   a routing table when @p hasRoutingTable is not set. Introduced in Ice 3.7.
             */
            getClientProxy(context?: Map<string, string>): Ice.AsyncResult<[Ice.ObjectPrx | null, boolean | undefined]>;

            /**
             * Gets the router's server proxy, i.e., the proxy to use for forwarding requests from the server to the
             * router. The Ice runtime uses the endpoints of this proxy as the published endpoints of bi-dir object
             * adapters.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - Ice.ObjectPrx | null : The router's server proxy.
             */
            getServerProxy(context?: Map<string, string>): Ice.AsyncResult<Ice.ObjectPrx | null>;

            /**
             * Adds new proxy information to the router's routing table.
             * @param proxies The proxies to add. Adding a null proxy is an error.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - (Ice.ObjectPrx | null)[] : Proxies discarded by the router. These proxies are all non-null.
             */
            addProxies(proxies: (Ice.ObjectPrx | null)[], context?: Map<string, string>): Ice.AsyncResult<(Ice.ObjectPrx | null)[]>;

            /**
             * Gets a unique category that identifies the client (caller) in the router. This category must be used in the
             * identities of all the client's callback objects.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - string : The category. It's an empty string when `Glacier2.Server.Endpoints` is not configured.
             */
            getCategoryForClient(context?: Map<string, string>): Ice.AsyncResult<string>;

            /**
             * Creates a session for the client (caller) with the router. If a {@link Glacier2.SessionManagerPrx} is configured,
             * a proxy to a {@link Glacier2.SessionPrx} object is returned to the client. Otherwise, null is returned and only an
             * internal session (i.e., not visible to the client) is created.
             * If a non-null session proxy is returned, it must be configured to route through the router that created it.
             * This occurs automatically when the router is configured as the client's default router at the time the
             * session proxy is created in the client application; otherwise, the client must configure the session proxy
             * explicitly.
             * @param userId The user ID.
             * @param password The password.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - __global_Glacier2.SessionPrx | null : A proxy for the newly created session, or null if no {@link Glacier2.SessionManagerPrx} is configured.
             * @throws {@link Glacier2.CannotCreateSessionException} Thrown when the session cannot be created.
             * @throws {@link Glacier2.PermissionDeniedException} Thrown when an authentication or authorization failure occurs.
             * @see Session
             * @see SessionManager
             * @see PermissionsVerifier
             */
            createSession(userId: string, password: string, context?: Map<string, string>): Ice.AsyncResult<__global_Glacier2.SessionPrx | null>;

            /**
             * Creates a per-client session with the router. The user is authenticated through the SSL certificate(s)
             * associated with the connection. If an {@link Glacier2.SSLSessionManagerPrx} is configured, a proxy to a {@link Glacier2.SessionPrx}
             * object is returned to the client. Otherwise, null is returned and only an internal session (i.e., not
             * visible to the client) is created.
             * If a non-null session proxy is returned, it must be configured to route through the router that created it.
             * This occurs automatically when the router is configured as the client's default router at the time the
             * session proxy is created in the client application; otherwise, the client must configure the session proxy
             * explicitly.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - __global_Glacier2.SessionPrx | null : A proxy for the newly created session, or null if no {@link Glacier2.SSLSessionManagerPrx} is configured.
             * @throws {@link Glacier2.CannotCreateSessionException} Thrown when the session cannot be created.
             * @throws {@link Glacier2.PermissionDeniedException} Thrown when an authentication or authorization failure occurs.
             * @see Session
             * @see SessionManager
             * @see PermissionsVerifier
             */
            createSessionFromSecureConnection(context?: Map<string, string>): Ice.AsyncResult<__global_Glacier2.SessionPrx | null>;

            /**
             * Keeps the session with this router alive.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             * @throws {@link Glacier2.SessionNotExistException} Thrown when no session exists for the caller (client).
             * @deprecated As of Ice 3.8, this operation does nothing.
             */
            refreshSession(context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Destroys the session of the caller with this router.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             * @throws {@link Glacier2.SessionNotExistException} Thrown when no session exists for the caller (client).
             */
            destroySession(context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Gets the idle timeout used by the server-side of the connection.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - bigint : The idle timeout (in seconds).
             */
            getSessionTimeout(context?: Map<string, string>): Ice.AsyncResult<bigint>;

            /**
             * Gets the idle timeout used by the server-side of the connection.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation, which resolves to:
             * - number : The idle timeout (in seconds).
             */
            getACMTimeout(context?: Map<string, string>): Ice.AsyncResult<number>;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet An optional facet name.
             * @returns A proxy with the requested type and facet, or null if the target proxy is null.
             */
            static uncheckedCast(prx: Ice.ObjectPrx, facet?: string): RouterPrx;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet An optional facet name.
             * @returns A proxy with the requested type and facet, or null if the target proxy is null.
             */
            static uncheckedCast(prx: Ice.ObjectPrx | null, facet?: string): RouterPrx | null;

            /**
             * Downcasts a proxy after confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet An optional facet name.
             * @param context The request context.
             * @returns A proxy with the requested type and facet, or null if the target proxy is null or the target
             * object does not support the requested type.
             */
            static checkedCast(prx: Ice.ObjectPrx | null, facet?: string, context?: Map<string, string>): Ice.AsyncResult<RouterPrx | null>;
        }

        /**
         * The Glacier2 specialization of the {@link Ice.RouterPrx} interface.
         */
        export abstract class Router extends Ice.Object implements __global_Ice.Router {
            /**
             * Gets the router's client proxy, i.e., the proxy to use for forwarding requests from the client to the
             * router. If a null proxy is returned, the client will forward requests to the router's endpoints.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to an array with the following entries:
             * - Ice.ObjectPrx | null : The router's client proxy.
             * - boolean | undefined : Indicates whether or not the router supports a routing table. If `true`, the Ice
             *   runtime will call {@link Ice.RouterPrx.addProxies} to populate the routing table. The Ice runtime assumes the router has
             *   a routing table when @p hasRoutingTable is not set. Introduced in Ice 3.7.
             */
            abstract getClientProxy(current: Ice.Current): PromiseLike<[Ice.ObjectPrx | null, boolean | undefined]> | [Ice.ObjectPrx | null, boolean | undefined];

            /**
             * Gets the router's server proxy, i.e., the proxy to use for forwarding requests from the server to the
             * router. The Ice runtime uses the endpoints of this proxy as the published endpoints of bi-dir object
             * adapters.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - Ice.ObjectPrx | null : The router's server proxy.
             */
            abstract getServerProxy(current: Ice.Current): PromiseLike<Ice.ObjectPrx | null> | Ice.ObjectPrx | null;

            /**
             * Adds new proxy information to the router's routing table.
             * @param proxies The proxies to add. Adding a null proxy is an error.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - (Ice.ObjectPrx | null)[] : Proxies discarded by the router. These proxies are all non-null.
             */
            abstract addProxies(proxies: (Ice.ObjectPrx | null)[], current: Ice.Current): PromiseLike<(Ice.ObjectPrx | null)[]> | (Ice.ObjectPrx | null)[];

            /**
             * Gets a unique category that identifies the client (caller) in the router. This category must be used in the
             * identities of all the client's callback objects.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - string : The category. It's an empty string when `Glacier2.Server.Endpoints` is not configured.
             */
            abstract getCategoryForClient(current: Ice.Current): PromiseLike<string> | string;

            /**
             * Creates a session for the client (caller) with the router. If a {@link Glacier2.SessionManagerPrx} is configured,
             * a proxy to a {@link Glacier2.SessionPrx} object is returned to the client. Otherwise, null is returned and only an
             * internal session (i.e., not visible to the client) is created.
             * If a non-null session proxy is returned, it must be configured to route through the router that created it.
             * This occurs automatically when the router is configured as the client's default router at the time the
             * session proxy is created in the client application; otherwise, the client must configure the session proxy
             * explicitly.
             * @param userId The user ID.
             * @param password The password.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - __global_Glacier2.SessionPrx | null : A proxy for the newly created session, or null if no {@link Glacier2.SessionManagerPrx} is configured.
             * @throws {@link Glacier2.CannotCreateSessionException} Thrown when the session cannot be created.
             * @throws {@link Glacier2.PermissionDeniedException} Thrown when an authentication or authorization failure occurs.
             * @see Session
             * @see SessionManager
             * @see PermissionsVerifier
             */
            abstract createSession(userId: string, password: string, current: Ice.Current): PromiseLike<__global_Glacier2.SessionPrx | null> | __global_Glacier2.SessionPrx | null;

            /**
             * Creates a per-client session with the router. The user is authenticated through the SSL certificate(s)
             * associated with the connection. If an {@link Glacier2.SSLSessionManagerPrx} is configured, a proxy to a {@link Glacier2.SessionPrx}
             * object is returned to the client. Otherwise, null is returned and only an internal session (i.e., not
             * visible to the client) is created.
             * If a non-null session proxy is returned, it must be configured to route through the router that created it.
             * This occurs automatically when the router is configured as the client's default router at the time the
             * session proxy is created in the client application; otherwise, the client must configure the session proxy
             * explicitly.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - __global_Glacier2.SessionPrx | null : A proxy for the newly created session, or null if no {@link Glacier2.SSLSessionManagerPrx} is configured.
             * @throws {@link Glacier2.CannotCreateSessionException} Thrown when the session cannot be created.
             * @throws {@link Glacier2.PermissionDeniedException} Thrown when an authentication or authorization failure occurs.
             * @see Session
             * @see SessionManager
             * @see PermissionsVerifier
             */
            abstract createSessionFromSecureConnection(current: Ice.Current): PromiseLike<__global_Glacier2.SessionPrx | null> | __global_Glacier2.SessionPrx | null;

            /**
             * Keeps the session with this router alive.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             * @throws {@link Glacier2.SessionNotExistException} Thrown when no session exists for the caller (client).
             */
            abstract refreshSession(current: Ice.Current): PromiseLike<void> | void;

            /**
             * Destroys the session of the caller with this router.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             * @throws {@link Glacier2.SessionNotExistException} Thrown when no session exists for the caller (client).
             */
            abstract destroySession(current: Ice.Current): PromiseLike<void> | void;

            /**
             * Gets the idle timeout used by the server-side of the connection.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - bigint : The idle timeout (in seconds).
             */
            abstract getSessionTimeout(current: Ice.Current): PromiseLike<bigint> | bigint;

            /**
             * Gets the idle timeout used by the server-side of the connection.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch, which resolves to:
             * - number : The idle timeout (in seconds).
             */
            abstract getACMTimeout(current: Ice.Current): PromiseLike<number> | number;

            /**
             * Dispatches an incoming request to one of the methods of this generated class, based on the operation name carried by the request.
             *
             * @param request The incoming request.
             * @returns The outgoing response (when the selected method returns a value),
             * or a promise with the outgoing response (when the selected method returns a promise).
             * @remarks Ice marshals any exception thrown by this method into the response.
             */
            dispatch(request: Ice.IncomingRequest): Ice.OutgoingResponse | PromiseLike<Ice.OutgoingResponse>;

            /**
             * Obtains the Slice type ID of this type.
             * @returns The return value is always "::Glacier2::Router".
             */
            static ice_staticId(): string;
        }
    }
    import __global_Glacier2 = Glacier2;
    import __global_Ice = Ice;
}
