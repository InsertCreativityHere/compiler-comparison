// Copyright (c) ZeroC, Inc.

// slice2js version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'LocatorRegistry.ice'.</auto-generated>


declare module "@zeroc/ice" {
    namespace Ice {
        /**
         * The exception that is thrown when a server application tries to register endpoints for an object adapter that is
         * already active.
         */
        export class AdapterAlreadyActiveException extends Ice.UserException {}

        /**
         * The exception that is thrown when the provided replica group is invalid.
         */
        export class InvalidReplicaGroupIdException extends Ice.UserException {}

        /**
         * The exception that is thrown when a server was not found.
         */
        export class ServerNotFoundException extends Ice.UserException {}

        /**
         * A server application registers the endpoints of its indirect object adapters with the LocatorRegistry object.
         */
        export class LocatorRegistryPrx extends Ice.ObjectPrx {
            /**
             * Constructs a new LocatorRegistryPrx proxy.
             * @param communicator - The communicator for the new proxy.
             * @param proxyString - The string representation of the proxy.
             * @returns The new LocatorRegistryPrx proxy.
             * @throws ParseException - Thrown if the proxyString is not a valid proxy string.
             */
            constructor(communicator: Ice.Communicator, proxyString: string);

            /**
             * Registers or unregisters the endpoints of an object adapter.
             * @param id The adapter ID.
             * @param proxy A dummy proxy created by the object adapter. @p proxy carries the object adapter's endpoints.
             * The locator considers an object adapter to be active after it has registered its endpoints.
             * When @p proxy is null, the endpoints are unregistered and the locator considers the object adapter inactive.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             * @throws {@link Ice.AdapterAlreadyActiveException} Thrown when an object adapter with the same adapter ID has already
             * registered its endpoints. Since this operation is marked idempotent, this exception may be thrown when the
             * Ice client runtime retries an invocation with a non-null @p proxy.
             * @throws {@link Ice.AdapterNotFoundException} Thrown when the locator only allows registered object adapters to register
             * their endpoints and no object adapter with this adapter ID was registered with the locator.
             */
            setAdapterDirectProxy(id: string, proxy: Ice.ObjectPrx | null, context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Registers or unregisters the endpoints of an object adapter. This object adapter is a member of a replica
             * group.
             * @param adapterId The adapter ID.
             * @param replicaGroupId The replica group ID.
             * @param proxy A dummy proxy created by the object adapter. @p proxy carries the object adapter's endpoints.
             * The locator considers an object adapter to be active after it has registered its endpoints. When @p proxy is
             * null, the endpoints are unregistered and the locator considers the object adapter inactive.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             * @throws {@link Ice.AdapterAlreadyActiveException} Thrown when an object adapter with the same adapter ID has already
             * registered its endpoints. Since this operation is marked idempotent, this exception may be thrown when the
             * Ice client runtime retries an invocation with a non-null @p proxy.
             * @throws {@link Ice.AdapterNotFoundException} Thrown when the locator only allows registered object adapters to register
             * their endpoints and no object adapter with this adapter ID was registered with the locator.
             * @throws {@link Ice.InvalidReplicaGroupIdException} Thrown when the given replica group does not match the replica group
             * associated with the adapter ID in the locator's database.
             */
            setReplicatedAdapterDirectProxy(adapterId: string, replicaGroupId: string, proxy: Ice.ObjectPrx | null, context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Registers a proxy to the {@link Ice.ProcessPrx} object of a server application.
             * @param id The server ID.
             * @param proxy A proxy to the Process object of the server. This proxy is never null.
             * @param context The Context map to send with the invocation.
             * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
             * @throws {@link Ice.ServerNotFoundException} Thrown when the locator does not know a server application with this server
             * ID.
             */
            setServerProcessProxy(id: string, proxy: __global_Ice.ProcessPrx | null, context?: Map<string, string>): Ice.AsyncResult<void>;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet An optional facet name.
             * @returns A proxy with the requested type and facet, or null if the target proxy is null.
             */
            static uncheckedCast(prx: Ice.ObjectPrx, facet?: string): LocatorRegistryPrx;

            /**
             * Downcasts a proxy without confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet An optional facet name.
             * @returns A proxy with the requested type and facet, or null if the target proxy is null.
             */
            static uncheckedCast(prx: Ice.ObjectPrx | null, facet?: string): LocatorRegistryPrx | null;

            /**
             * Downcasts a proxy after confirming the target object's type via a remote invocation.
             * @param prx The target proxy.
             * @param facet An optional facet name.
             * @param context The request context.
             * @returns A proxy with the requested type and facet, or null if the target proxy is null or the target
             * object does not support the requested type.
             */
            static checkedCast(prx: Ice.ObjectPrx | null, facet?: string, context?: Map<string, string>): Ice.AsyncResult<LocatorRegistryPrx | null>;
        }

        /**
         * A server application registers the endpoints of its indirect object adapters with the LocatorRegistry object.
         */
        export abstract class LocatorRegistry extends Ice.Object {
            /**
             * Registers or unregisters the endpoints of an object adapter.
             * @param id The adapter ID.
             * @param proxy A dummy proxy created by the object adapter. @p proxy carries the object adapter's endpoints.
             * The locator considers an object adapter to be active after it has registered its endpoints.
             * When @p proxy is null, the endpoints are unregistered and the locator considers the object adapter inactive.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             * @throws {@link Ice.AdapterAlreadyActiveException} Thrown when an object adapter with the same adapter ID has already
             * registered its endpoints. Since this operation is marked idempotent, this exception may be thrown when the
             * Ice client runtime retries an invocation with a non-null @p proxy.
             * @throws {@link Ice.AdapterNotFoundException} Thrown when the locator only allows registered object adapters to register
             * their endpoints and no object adapter with this adapter ID was registered with the locator.
             */
            abstract setAdapterDirectProxy(id: string, proxy: Ice.ObjectPrx | null, current: Ice.Current): PromiseLike<void> | void;

            /**
             * Registers or unregisters the endpoints of an object adapter. This object adapter is a member of a replica
             * group.
             * @param adapterId The adapter ID.
             * @param replicaGroupId The replica group ID.
             * @param proxy A dummy proxy created by the object adapter. @p proxy carries the object adapter's endpoints.
             * The locator considers an object adapter to be active after it has registered its endpoints. When @p proxy is
             * null, the endpoints are unregistered and the locator considers the object adapter inactive.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             * @throws {@link Ice.AdapterAlreadyActiveException} Thrown when an object adapter with the same adapter ID has already
             * registered its endpoints. Since this operation is marked idempotent, this exception may be thrown when the
             * Ice client runtime retries an invocation with a non-null @p proxy.
             * @throws {@link Ice.AdapterNotFoundException} Thrown when the locator only allows registered object adapters to register
             * their endpoints and no object adapter with this adapter ID was registered with the locator.
             * @throws {@link Ice.InvalidReplicaGroupIdException} Thrown when the given replica group does not match the replica group
             * associated with the adapter ID in the locator's database.
             */
            abstract setReplicatedAdapterDirectProxy(adapterId: string, replicaGroupId: string, proxy: Ice.ObjectPrx | null, current: Ice.Current): PromiseLike<void> | void;

            /**
             * Registers a proxy to the {@link Ice.ProcessPrx} object of a server application.
             * @param id The server ID.
             * @param proxy A proxy to the Process object of the server. This proxy is never null.
             * @param current The Current object for the dispatch.
             * @returns A promise like object representing the result of the dispatch.
             * @throws {@link Ice.ServerNotFoundException} Thrown when the locator does not know a server application with this server
             * ID.
             */
            abstract setServerProcessProxy(id: string, proxy: __global_Ice.ProcessPrx | null, current: Ice.Current): PromiseLike<void> | void;

            /**
             * Dispatches an incoming request to one of the methods of this generated class, based on the operation name carried by the request.
             *
             * @param request The incoming request.
             * @returns The outgoing response (when the selected method returns a value),
             * or a promise with the outgoing response (when the selected method returns a promise).
             * @remarks Ice marshals any exception thrown by this method into the response.
             */
            dispatch(request: Ice.IncomingRequest): Ice.OutgoingResponse | PromiseLike<Ice.OutgoingResponse>;

            /**
             * Obtains the Slice type ID of this type.
             * @returns The return value is always "::Ice::LocatorRegistry".
             */
            static ice_staticId(): string;
        }
    }
    import __global_Ice = Ice;
}
