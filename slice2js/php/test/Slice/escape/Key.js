// Copyright (c) ZeroC, Inc.

// slice2js version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Key.ice'.</auto-generated>

/* eslint-disable */
/* jshint ignore: start */

import { Ice } from "@zeroc/ice";

export const and = {};

Object.defineProperty(and, 'require_once', {
    enumerable: true,
    value: 1
});

and.array = Ice.defineEnum([
    ['clone', 1]]);

and.endforHelper = Ice.StreamHelpers.generateSeqHelper(and.array._helper, false);

[and.endforeach, and.endforeachHelper] = Ice.defineDictionary(Ice.StringHelper, and.array._helper, false, undefined);

and.xor = class
{
    constructor(abstract = 0, var = and.require_once)
    {
        this.abstract = abstract;
        this.var = var;
    }

    _write(ostr)
    {
        ostr.writeInt(this.abstract);
        ostr.writeInt(this.var);
    }

    _read(istr)
    {
        this.abstract = istr.readInt();
        this.var = istr.readInt();
    }

    static get minWireSize()
    {
        return  8;
    }
};

Ice.defineStruct(and.xor, true, false);

and.endif = class extends Ice.UserException
{
    constructor(switch = 0, _cause = "")
    {
        super(_cause);
        this.switch = switch;
    }

    static get _parent()
    {
        return Ice.UserException;
    }

    static get _ice_id()
    {
        return "::and::endif";
    }

    _mostDerivedType()
    {
        return and.endif;
    }

    _writeMemberImpl(ostr)
    {
        ostr.writeInt(this.switch);
    }

    _readMemberImpl(istr)
    {
        this.switch = istr.readInt();
    }
};

Ice.defineClass(and.endif, "::and::endif");

and.endwhile = class extends and.endif
{
    constructor(switch, eval = 0, exit = and.require_once, _cause = "")
    {
        super(switch, _cause);
        this.eval = eval;
        this.exit = exit;
    }

    static get _parent()
    {
        return and.endif;
    }

    static get _ice_id()
    {
        return "::and::endwhile";
    }

    _mostDerivedType()
    {
        return and.endwhile;
    }

    _writeMemberImpl(ostr)
    {
        ostr.writeInt(this.eval);
        ostr.writeInt(this.exit);
    }

    _readMemberImpl(istr)
    {
        this.eval = istr.readInt();
        this.exit = istr.readInt();
    }
};

Ice.defineClass(and.endwhile, "::and::endwhile");

const iceC_and_function_ids = [
    "::Ice::Object",
    "::and::function"
];

and.function = class extends Ice.Object
{
};

and.functionPrx = class extends Ice.ObjectPrx
{
};

Ice.TypeRegistry.declareProxyType("and.functionPrx", and.functionPrx);

Ice.defineOperations(
    and.function,
    and.functionPrx,
    iceC_and_function_ids,
    "::and::function",
    {
        "continue": [, 0, , , [[3], [3]], , , , ]
    });

const iceC_and_die_ids = [
    "::Ice::Object",
    "::and::die"
];

and.die = class extends Ice.Object
{
};

and.diePrx = class extends Ice.ObjectPrx
{
};

Ice.TypeRegistry.declareProxyType("and.diePrx", and.diePrx);

Ice.defineOperations(
    and.die,
    and.diePrx,
    iceC_and_die_ids,
    "::and::die",
    {
        "do": [, 0, , , , , , , ]
    });

and.echo = class extends Ice.Value
{
    constructor(if = 0, empty = null)
    {
        super();
        this.if = if;
        this.empty = empty;
    }

    _iceWriteMemberImpl(ostr)
    {
        ostr.writeInt(this.if);
        ostr.writeProxy(this.empty);
    }

    _iceReadMemberImpl(istr)
    {
        this.if = istr.readInt();
        this.empty = istr.readProxy();
    }
};

Ice.defineClass(and.echo, "::and::echo");

Ice.TypeRegistry.declareValueType("and.echo", and.echo);

const iceC_and_enddeclare_ids = [
    "::Ice::Object",
    "::and::die",
    "::and::enddeclare",
    "::and::function"
];

and.enddeclare = class extends Ice.Object
{
    static get _iceImplements()
    {
        return [
            and.die,
            and.function
        ];
    }
};

and.enddeclarePrx = class extends Ice.ObjectPrx
{
    static get _implements()
    {
        return [
            and.diePrx,
            and.functionPrx];
    }
};

Ice.TypeRegistry.declareProxyType("and.enddeclarePrx", and.enddeclarePrx);

Ice.defineOperations(
    and.enddeclare,
    and.enddeclarePrx,
    iceC_and_enddeclare_ids,
    "::and::enddeclare",
    {
        "foreach": [, 0, , [and.array._helper], [["and.echo", true], ["and.functionPrx", , 1]], [[and.endforHelper], [and.endforeachHelper, , 2]],
        [
            and.endwhile,
            and.endif
        ], true, ]
    });
