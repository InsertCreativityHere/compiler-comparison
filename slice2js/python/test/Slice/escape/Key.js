// Copyright (c) ZeroC, Inc.

// slice2js version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Key.ice'.</auto-generated>

/* eslint-disable */
/* jshint ignore: start */

import { Ice } from "@zeroc/ice";

export const and = {};

and.assert = Ice.defineEnum([
    ['break', 0]]);

and.continue = class
{
    constructor(def = 0)
    {
        this.def = def;
    }

    _write(ostr)
    {
        ostr.writeInt(this.def);
    }

    _read(istr)
    {
        this.def = istr.readInt();
    }

    static get minWireSize()
    {
        return  4;
    }
};

Ice.defineStruct(and.continue, true, false);

const iceC_and_del_ids = [
    "::Ice::Object",
    "::and::del"
];

and.del = class extends Ice.Object
{
};

and.delPrx = class extends Ice.ObjectPrx
{
};

Ice.TypeRegistry.declareProxyType("and.delPrx", and.delPrx);

Ice.defineOperations(
    and.del,
    and.delPrx,
    iceC_and_del_ids,
    "::and::del",
    {
        "elif": [, 0, , , [[3]], [[3]], , , ]
    });

const iceC_and_exec_ids = [
    "::Ice::Object",
    "::and::exec"
];

and.exec = class extends Ice.Object
{
};

and.execPrx = class extends Ice.ObjectPrx
{
};

Ice.TypeRegistry.declareProxyType("and.execPrx", and.execPrx);

Ice.defineOperations(
    and.exec,
    and.execPrx,
    iceC_and_exec_ids,
    "::and::exec",
    {
        "finally": [, 0, , , , , , , ]
    });

and.for = class extends Ice.Value
{
    constructor(foo = 0, from = null)
    {
        super();
        this.foo = foo;
        this.from = from;
    }

    _iceWriteMemberImpl(ostr)
    {
        ostr.writeInt(this.foo);
        ostr.writeProxy(this.from);
    }

    _iceReadMemberImpl(istr)
    {
        this.foo = istr.readInt();
        this.from = istr.readProxy();
    }
};

Ice.defineClass(and.for, "::and::for");

Ice.TypeRegistry.declareValueType("and.for", and.for);

and.is = class extends Ice.UserException
{
    constructor(bar = 0, _cause = "")
    {
        super(_cause);
        this.bar = bar;
    }

    static get _parent()
    {
        return Ice.UserException;
    }

    static get _ice_id()
    {
        return "::and::is";
    }

    _mostDerivedType()
    {
        return and.is;
    }

    _writeMemberImpl(ostr)
    {
        ostr.writeInt(this.bar);
    }

    _readMemberImpl(istr)
    {
        this.bar = istr.readInt();
    }
};

Ice.defineClass(and.is, "::and::is");

and.not = class extends and.is
{
    constructor(bar, pass = 0, _cause = "")
    {
        super(bar, _cause);
        this.pass = pass;
    }

    static get _parent()
    {
        return and.is;
    }

    static get _ice_id()
    {
        return "::and::not";
    }

    _mostDerivedType()
    {
        return and.not;
    }

    _writeMemberImpl(ostr)
    {
        ostr.writeInt(this.pass);
    }

    _readMemberImpl(istr)
    {
        this.pass = istr.readInt();
    }
};

Ice.defineClass(and.not, "::and::not");

const iceC_and_if_ids = [
    "::Ice::Object",
    "::and::del",
    "::and::exec",
    "::and::if"
];

and.if = class extends Ice.Object
{
    static get _iceImplements()
    {
        return [
            and.exec,
            and.del
        ];
    }
};

and.ifPrx = class extends Ice.ObjectPrx
{
    static get _implements()
    {
        return [
            and.execPrx,
            and.delPrx];
    }
};

Ice.TypeRegistry.declareProxyType("and.ifPrx", and.ifPrx);

Ice.defineOperations(
    and.if,
    and.ifPrx,
    iceC_and_if_ids,
    "::and::if",
    {
        "raise": [, 0, , [and.assert._helper], [[and.continue], ["and.for", true], ["and.delPrx"], ["and.execPrx"], ["and.ifPrx"], [3]], ,
        [
            and.is
        ], true, ]
    });

and.importHelper = Ice.StreamHelpers.generateSeqHelper(and.assert._helper, false);

[and.in, and.inHelper] = Ice.defineDictionary(Ice.StringHelper, and.assert._helper, false, undefined);

Object.defineProperty(and, 'lambda', {
    enumerable: true,
    value: 0
});

and.EnumNone = Ice.defineEnum([
    ['None', 0]]);
