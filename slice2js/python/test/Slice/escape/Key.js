// Copyright (c) ZeroC, Inc.

// slice2js version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Key.ice'.</auto-generated>

/* eslint-disable */
/* jshint ignore: start */

import { Ice } from "@zeroc/ice";

export const Test = {};
Test.and = Test.and || {};

Test.and.assert = Ice.defineEnum([
    ['break', 0]]);

Test.and.continue = class {
    constructor(def = 0) {
        this.def = def;
    }

    _write(ostr) {
        ostr.writeInt(this.def);
    }

    _read(istr) {
        this.def = istr.readInt();
    }

    static get minWireSize() {
        return  4;
    }
};

Ice.defineStruct(Test.and.continue, true, false);

const iceC_Test_and_del_ids = [
    "::Ice::Object",
    "::Test::and::del"
];

Test.and.del = class extends Ice.Object {};

Test.and.delPrx = class extends Ice.ObjectPrx {};

Ice.TypeRegistry.declareProxyType("Test.and.delPrx", Test.and.delPrx);

Ice.defineOperations(
    Test.and.del,
    Test.and.delPrx,
    iceC_Test_and_del_ids,
    "::Test::and::del", {
        "elif": [, 0, , , [[3]], [[3]], , , ]
    });

const iceC_Test_and_exec_ids = [
    "::Ice::Object",
    "::Test::and::exec"
];

Test.and.exec = class extends Ice.Object {};

Test.and.execPrx = class extends Ice.ObjectPrx {};

Ice.TypeRegistry.declareProxyType("Test.and.execPrx", Test.and.execPrx);

Ice.defineOperations(
    Test.and.exec,
    Test.and.execPrx,
    iceC_Test_and_exec_ids,
    "::Test::and::exec", {
        "finally": [, 0, , , , , , , ]
    });

Test.and.for = class extends Ice.Value {
    constructor(foo = 0, from = null) {
        super();
        this.foo = foo;
        this.from = from;
    }

    _iceWriteMemberImpl(ostr) {
        ostr.writeInt(this.foo);
        ostr.writeProxy(this.from);
    }

    _iceReadMemberImpl(istr) {
        this.foo = istr.readInt();
        this.from = istr.readProxy();
    }
};

Ice.defineClass(Test.and.for, "::Test::and::for");

Ice.TypeRegistry.declareValueType("Test.and.for", Test.and.for);

Test.and.is = class extends Ice.UserException {
    constructor(bar = 0) {
        super();
        this.bar = bar;
    }

    static get _parent() {
        return Ice.UserException;
    }

    static get _ice_id() {
        return "::Test::and::is";
    }

    _mostDerivedType() {
        return Test.and.is;
    }

    _writeMemberImpl(ostr) {
        ostr.writeInt(this.bar);
    }

    _readMemberImpl(istr) {
        this.bar = istr.readInt();
    }
};

Ice.defineClass(Test.and.is, "::Test::and::is");

Test.and.not = class extends Test.and.is {
    constructor(bar, pass = 0) {
        super(bar);
        this.pass = pass;
    }

    static get _parent() {
        return Test.and.is;
    }

    static get _ice_id() {
        return "::Test::and::not";
    }

    _mostDerivedType() {
        return Test.and.not;
    }

    _writeMemberImpl(ostr) {
        ostr.writeInt(this.pass);
    }

    _readMemberImpl(istr) {
        this.pass = istr.readInt();
    }
};

Ice.defineClass(Test.and.not, "::Test::and::not");

const iceC_Test_and_if_ids = [
    "::Ice::Object",
    "::Test::and::del",
    "::Test::and::exec",
    "::Test::and::if"
];

Test.and.if = class extends Ice.Object {
    static get _iceImplements() {
        return [
            Test.and.exec,
            Test.and.del
        ];
    }
};

Test.and.ifPrx = class extends Ice.ObjectPrx {
    static get _implements() {
        return [
            Test.and.execPrx,
            Test.and.delPrx];
    }
};

Ice.TypeRegistry.declareProxyType("Test.and.ifPrx", Test.and.ifPrx);

Ice.defineOperations(
    Test.and.if,
    Test.and.ifPrx,
    iceC_Test_and_if_ids,
    "::Test::and::if", {
        "raise": [, 0, , [Test.and.assert._helper], [[Test.and.continue], ["Test.and.for", true], ["Test.and.delPrx"], ["Test.and.execPrx"], ["Test.and.ifPrx"], [3]], ,
        [
            Test.and.is
        ], true, ]
    });

Test.and.importHelper = Ice.StreamHelpers.generateSeqHelper(Test.and.assert._helper, false);

[Test.and.in, Test.and.inHelper] = Ice.defineDictionary(Ice.StringHelper, Test.and.assert._helper, false, undefined);

Object.defineProperty(Test.and, 'lambda', {
    enumerable: true,
    value: 0
});

Test.and.EnumNone = Ice.defineEnum([
    ['None', 0]]);
