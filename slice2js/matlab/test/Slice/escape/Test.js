// Copyright (c) ZeroC, Inc.

// slice2js version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Test.ice'.</auto-generated>

/* eslint-disable */
/* jshint ignore: start */

import { Ice } from "@zeroc/ice";

export const classdef = {};

classdef.persistent = Ice.defineEnum([
    ['break', 0], ['catch', 1], ['LAST', 2]]);

classdef.global = class
{
    constructor(case = classdef.persistent.catch, continue = 1, eq = 2)
    {
        this.case = case;
        this.continue = continue;
        this.eq = eq;
    }

    _write(ostr)
    {
        classdef.persistent._write(ostr, this.case);
        ostr.writeInt(this.continue);
        ostr.writeInt(this.eq);
    }

    _read(istr)
    {
        this.case = classdef.persistent._read(istr);
        this.continue = istr.readInt();
        this.eq = istr.readInt();
    }

    static get minWireSize()
    {
        return  9;
    }
};

Ice.defineStruct(classdef.global, true, true);

classdef.logical = class extends Ice.Value
{
    constructor(else = classdef.persistent.break, for = new classdef.global(), int64 = true)
    {
        super();
        this.else = else;
        this.for = for;
        this.int64 = int64;
    }

    _iceWriteMemberImpl(ostr)
    {
        classdef.persistent._write(ostr, this.else);
        classdef.global.write(ostr, this.for);
        ostr.writeBool(this.int64);
    }

    _iceReadMemberImpl(istr)
    {
        this.else = classdef.persistent._read(istr);
        this.for = classdef.global.read(istr, this.for);
        this.int64 = istr.readBool();
    }
};

Ice.defineClass(classdef.logical, "::classdef::logical");

Ice.TypeRegistry.declareValueType("classdef.logical", classdef.logical);

classdef.xor = class extends classdef.logical
{
    constructor(else, for, int64, return = 1)
    {
        super(else, for, int64);
        this.return = return;
    }

    _iceWriteMemberImpl(ostr)
    {
        ostr.writeInt(this.return);
    }

    _iceReadMemberImpl(istr)
    {
        this.return = istr.readInt();
    }
};

Ice.defineClass(classdef.xor, "::classdef::xor");

Ice.TypeRegistry.declareValueType("classdef.xor", classdef.xor);

classdef.parforHelper = Ice.StreamHelpers.generateSeqHelper(classdef.global, false);

[classdef.switch, classdef.switchHelper] = Ice.defineDictionary(Ice.IntHelper, classdef.global, false, undefined);

classdef.Base = class extends Ice.Value
{
    constructor(while = 1)
    {
        super();
        this.while = while;
    }

    _iceWriteMemberImpl(ostr)
    {
        ostr.writeInt(this.while);
    }

    _iceReadMemberImpl(istr)
    {
        this.while = istr.readInt();
    }
};

Ice.defineClass(classdef.Base, "::classdef::Base");

Ice.TypeRegistry.declareValueType("classdef.Base", classdef.Base);

classdef.Derived = class extends classdef.Base
{
    constructor(while, if = 2, spmd = null, otherwise = null)
    {
        super(while);
        this.if = if;
        this.spmd = spmd;
        this.otherwise = otherwise;
    }

    _iceWriteMemberImpl(ostr)
    {
        ostr.writeInt(this.if);
        classdef.parforHelper.write(ostr, this.spmd);
        classdef.switchHelper.write(ostr, this.otherwise);
    }

    _iceReadMemberImpl(istr)
    {
        this.if = istr.readInt();
        this.spmd = classdef.parforHelper.read(istr);
        this.otherwise = classdef.switchHelper.read(istr);
    }
};

Ice.defineClass(classdef.Derived, "::classdef::Derived");

Ice.TypeRegistry.declareValueType("classdef.Derived", classdef.Derived);

classdef.bitand = class extends Ice.UserException
{
    constructor(identifier = "1", message = "2", end = null, _cause = "")
    {
        super(_cause);
        this.identifier = identifier;
        this.message = message;
        this.end = end;
    }

    static get _parent()
    {
        return Ice.UserException;
    }

    static get _ice_id()
    {
        return "::classdef::bitand";
    }

    _mostDerivedType()
    {
        return classdef.bitand;
    }

    _writeMemberImpl(ostr)
    {
        ostr.writeString(this.identifier);
        ostr.writeString(this.message);
        ostr.writeValue(this.end);
    }

    _readMemberImpl(istr)
    {
        this.identifier = istr.readString();
        this.message = istr.readString();
        istr.readValue(obj => this.end = obj, Ice.TypeRegistry.getValueType("classdef.logical"));
    }

    _usesClasses()
    {
        return true;
    }
};

Ice.defineClass(classdef.bitand, "::classdef::bitand");

classdef.bitor = class extends classdef.bitand
{
    constructor(identifier, message, end, enumeration = 1, _cause = "")
    {
        super(identifier, message, end, _cause);
        this.enumeration = enumeration;
    }

    static get _parent()
    {
        return classdef.bitand;
    }

    static get _ice_id()
    {
        return "::classdef::bitor";
    }

    _mostDerivedType()
    {
        return classdef.bitor;
    }

    _writeMemberImpl(ostr)
    {
        ostr.writeInt(this.enumeration);
    }

    _readMemberImpl(istr)
    {
        this.enumeration = istr.readInt();
    }
};

Ice.defineClass(classdef.bitor, "::classdef::bitor");

const iceC_classdef_operations_ids = [
    "::Ice::Object",
    "::classdef::operations"
];

classdef.operations = class extends Ice.Object
{
};

classdef.operationsPrx = class extends Ice.ObjectPrx
{
};

Ice.TypeRegistry.declareProxyType("classdef.operationsPrx", classdef.operationsPrx);

Ice.defineOperations(
    classdef.operations,
    classdef.operationsPrx,
    iceC_classdef_operations_ids,
    "::classdef::operations",
    {
        "events": [, 0, , , , , , , ],
        "function": [, 0, , , , , , , ]
    });

Object.defineProperty(classdef, 'methods', {
    enumerable: true,
    value: 1
});
