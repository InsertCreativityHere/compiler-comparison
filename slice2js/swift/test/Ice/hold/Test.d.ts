// Copyright (c) ZeroC, Inc.

// slice2js version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Test.ice'.</auto-generated>

import * as __module__zeroc_ice from "@zeroc/ice";

export namespace Test
{
    export class HoldPrx extends __module__zeroc_ice.Ice.ObjectPrx
    {
        /**
         * Constructs a new HoldPrx proxy.
         * @param communicator - The communicator for the new proxy.
         * @param proxyString - The string representation of the proxy.
         * @returns The new HoldPrx proxy.
         * @throws ParseException - Thrown if the proxyString is not a valid proxy string.
         */
        constructor(communicator: __module__zeroc_ice.Ice.Communicator, proxyString: string);

        /**
         * Constructs a new HoldPrx proxy from an ObjectPrx. The new proxy is a clone of the
         * provided proxy.
         * @param prx - The proxy to clone.
         * @returns The new HoldPrx proxy.
         */
        constructor(prx: __module__zeroc_ice.Ice.ObjectPrx);

        /**
         * Puts the adapter on hold, and optionally reactivates it.
         * @param delay When less than 0, puts the adapter on hold indefinitely. When 0, puts the adapter on hold and
         * immediately reactivates it. When greater than 0, starts a background task that sleeps for delay
         * milliseconds, puts the adapter on hold and then immediately reactivates it.
         * @param context The Context map to send with the invocation.
         * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
         */
        putOnHold(delay: number, context?: Map<string, string>): __module__zeroc_ice.Ice.AsyncResult<void>;

        /**
         * Starts a background task that calls waitForHold and activate on the adapter.
         * @param context The Context map to send with the invocation.
         * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
         */
        waitForHold(context?: Map<string, string>): __module__zeroc_ice.Ice.AsyncResult<void>;

        /**
         * Shuts down the server.
         * @param context The Context map to send with the invocation.
         * @returns An {@link Ice.AsyncResult} object representing the result of the invocation.
         */
        shutdown(context?: Map<string, string>): __module__zeroc_ice.Ice.AsyncResult<void>;

        /**
         * Downcasts a proxy without confirming the target object's type via a remote invocation.
         * @param prx The target proxy.
         * @returns A proxy with the requested type.
         */
        static uncheckedCast(prx: __module__zeroc_ice.Ice.ObjectPrx, facet?: string): HoldPrx;

        /**
         * Downcasts a proxy after confirming the target object's type via a remote invocation.
         * @param prx The target proxy.
         * @param facet A facet name.
         * @param context The request context.
         * @returns A proxy with the requested type and facet, or nil if the target proxy is nil or the target
         * object does not support the requested type.
         */
        static checkedCast(prx: __module__zeroc_ice.Ice.ObjectPrx, facet?: string, context?: Map<string, string>): __module__zeroc_ice.Ice.AsyncResult<HoldPrx | null>;
    }

    export abstract class Hold extends __module__zeroc_ice.Ice.Object
    {
        /**
         * Puts the adapter on hold, and optionally reactivates it.
         * @param delay When less than 0, puts the adapter on hold indefinitely. When 0, puts the adapter on hold and
         * immediately reactivates it. When greater than 0, starts a background task that sleeps for delay
         * milliseconds, puts the adapter on hold and then immediately reactivates it.
         * @param current The Current object for the dispatch.
         * @returns A promise like object representing the result of the dispatch.
         */
        abstract putOnHold(delay: number, current: __module__zeroc_ice.Ice.Current): PromiseLike<void>|void;

        /**
         * Starts a background task that calls waitForHold and activate on the adapter.
         * @param current The Current object for the dispatch.
         * @returns A promise like object representing the result of the dispatch.
         */
        abstract waitForHold(current: __module__zeroc_ice.Ice.Current): PromiseLike<void>|void;

        /**
         * Shuts down the server.
         * @param current The Current object for the dispatch.
         * @returns A promise like object representing the result of the dispatch.
         */
        abstract shutdown(current: __module__zeroc_ice.Ice.Current): PromiseLike<void>|void;

        /**
         * Obtains the Slice type ID of this type.
         * @returns The return value is always "::Test::Hold".
         */
        static ice_staticId(): string;
    }
}
