// This file was generated by ice2slice (3.8.0-alpha.0) from: D:/Code/Workspace/ice/python/test/Slice/escape/Key.ice

// Use Slice1 mode for compatibility with ZeroC Ice.
mode = Slice1

module Test::and

enum assert {
    break
}

compact struct continue {
    def: int32
}

interface del {
    elif(else: int32) -> int32
}

[cs::type("Test.and.delProxy")]
custom delProxy

interface exec {
    finally()
}

[cs::type("Test.and.execProxy")]
custom execProxy

class for {
    foo: int32
    from: execProxy?
}

exception is {
    bar: int32
}

exception not : is {
    pass: int32
}

interface if : exec, del {
    raise(else: continue, return: for?, while: delProxy?, yield: execProxy?, or: ifProxy?, global: int32) -> assert throws is
}

[cs::type("Test.and.ifProxy")]
custom ifProxy

typealias import =  Sequence<assert>

typealias in =  Dictionary<string, assert>

// ice2slice could not convert:
// const int lambda = 0

enum EnumNone {
    None
}
