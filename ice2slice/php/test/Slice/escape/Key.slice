// This file was generated by ice2slice (3.8.0-alpha.0) from: D:/Code/Workspace/temp/ice/php/test/Slice/escape/Key.ice

// Use Slice1 mode for compatibility with ZeroC Ice.
mode = Slice1

module and

// ice2slice could not convert:
// const int require_once = 1

enum array {
    clone = 1
}

typealias endfor =  Sequence<array>

typealias endforeach =  Dictionary<string, array>

compact struct xor {
    abstract: int32
    var: int32
}

exception endif {
    switch: int32
}

exception endwhile : endif {
    eval: int32
    exit: int32
}

interface function {
    continue(declare: int32, default: int32)
}

[cs::type("and.functionProxy")]
custom functionProxy

interface die {
    do()
}

[cs::type("and.dieProxy")]
custom dieProxy

class echo {
    if: int32
    empty: dieProxy?
}

interface enddeclare : die, function {
    foreach(global: echo?, tag(1) include: functionProxy?) -> (new: endfor, tag(2) static: endforeach?, returnValue: array) throws (endif, endwhile)
}

[cs::type("and.enddeclareProxy")]
custom enddeclareProxy
