// This file was generated by ice2slice (3.8.0-alpha.0) from: D:/Code/Workspace/ice/java/test/src/main/java/test/Slice/escape/Key.ice

// Use Slice1 mode for compatibility with ZeroC Ice.
mode = Slice1

module abstract

enum assert {
    boolean
}

compact struct break {
    case: int32
}

interface catch {
    checkedCast(clone: int32) -> int32
}

[cs::type("abstract.catchProxy")]
custom catchProxy

interface default {
    do()
}

[cs::type("abstract.defaultProxy")]
custom defaultProxy

class notify {
    if: int32
    equals: defaultProxy?
}

exception hashCode {
    if: int32
}

exception clone : hashCode {
    native: string
}

interface finalize : default, catch {
    notify(notifyAll: break, null: notify?, package: finalizeProxy?, return: defaultProxy?, super: int32) -> assert throws (hashCode, clone)
}

[cs::type("abstract.finalizeProxy")]
custom finalizeProxy

typealias for =  Sequence<assert>

typealias goto =  Dictionary<string, assert>

// ice2slice could not convert:
// const int synchronized = 0
