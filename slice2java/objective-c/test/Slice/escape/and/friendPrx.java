//
// Copyright (c) ZeroC, Inc. All rights reserved.
//
//
// Ice version 3.7.10
//
// <auto-generated>
//
// Generated from file `Key.ice'
//
// Warning: do not edit this file.
//
// </auto-generated>
//

package and;

public interface friendPrx extends com.zeroc.Ice.ObjectPrx
{
    default auto _goto(_continue _if, auto d, delete inline, com.zeroc.Ice.Value _private, _do mutable, breakPrx namespace, charPrx _new, switchPrx not, doPrx operator, int or, int _protected, int _public, int register)
        throws sizeof,
               _return
    {
        return _goto(_if, d, inline, _private, mutable, namespace, _new, not, operator, or, _protected, _public, register, com.zeroc.Ice.ObjectPrx.noExplicitContext);
    }

    default auto _goto(_continue _if, auto d, delete inline, com.zeroc.Ice.Value _private, _do mutable, breakPrx namespace, charPrx _new, switchPrx not, doPrx operator, int or, int _protected, int _public, int register, java.util.Map<String, String> context)
        throws sizeof,
               _return
    {
        try
        {
            return _iceI_gotoAsync(_if, d, inline, _private, mutable, namespace, _new, not, operator, or, _protected, _public, register, context, true).waitForResponseOrUserEx();
        }
        catch(sizeof ex)
        {
            throw ex;
        }
        catch(_return ex)
        {
            throw ex;
        }
        catch(com.zeroc.Ice.UserException ex)
        {
            throw new com.zeroc.Ice.UnknownUserException(ex.ice_id(), ex);
        }
    }

    default java.util.concurrent.CompletableFuture<auto> gotoAsync(_continue _if, auto d, delete inline, com.zeroc.Ice.Value _private, _do mutable, breakPrx namespace, charPrx _new, switchPrx not, doPrx operator, int or, int _protected, int _public, int register)
    {
        return _iceI_gotoAsync(_if, d, inline, _private, mutable, namespace, _new, not, operator, or, _protected, _public, register, com.zeroc.Ice.ObjectPrx.noExplicitContext, false);
    }

    default java.util.concurrent.CompletableFuture<auto> gotoAsync(_continue _if, auto d, delete inline, com.zeroc.Ice.Value _private, _do mutable, breakPrx namespace, charPrx _new, switchPrx not, doPrx operator, int or, int _protected, int _public, int register, java.util.Map<String, String> context)
    {
        return _iceI_gotoAsync(_if, d, inline, _private, mutable, namespace, _new, not, operator, or, _protected, _public, register, context, false);
    }

    /**
     * @hidden
     * @param iceP_if -
     * @param iceP_d -
     * @param iceP_inline -
     * @param iceP_private -
     * @param iceP_mutable -
     * @param iceP_namespace -
     * @param iceP_new -
     * @param iceP_not -
     * @param iceP_operator -
     * @param iceP_or -
     * @param iceP_protected -
     * @param iceP_public -
     * @param iceP_register -
     * @param context -
     * @param sync -
     * @return -
     **/
    default com.zeroc.IceInternal.OutgoingAsync<auto> _iceI_gotoAsync(_continue iceP_if, auto iceP_d, delete iceP_inline, com.zeroc.Ice.Value iceP_private, _do iceP_mutable, breakPrx iceP_namespace, charPrx iceP_new, switchPrx iceP_not, doPrx iceP_operator, int iceP_or, int iceP_protected, int iceP_public, int iceP_register, java.util.Map<String, String> context, boolean sync)
    {
        com.zeroc.IceInternal.OutgoingAsync<auto> f = new com.zeroc.IceInternal.OutgoingAsync<>(this, "goto", null, sync, _iceE_goto);
        f.invoke(true, context, null, ostr -> {
                     _continue.ice_write(ostr, iceP_if);
                     auto.ice_write(ostr, iceP_d);
                     delete.ice_write(ostr, iceP_inline);
                     ostr.writeValue(iceP_private);
                     ostr.writeValue(iceP_mutable);
                     ostr.writeProxy(iceP_namespace);
                     ostr.writeProxy(iceP_new);
                     ostr.writeProxy(iceP_not);
                     ostr.writeProxy(iceP_operator);
                     ostr.writeInt(iceP_or);
                     ostr.writeInt(iceP_protected);
                     ostr.writeInt(iceP_public);
                     ostr.writeInt(iceP_register);
                     ostr.writePendingValues();
                 }, istr -> {
                     auto ret;
                     ret = auto.ice_read(istr);
                     return ret;
                 });
        return f;
    }

    /** @hidden */
    static final Class<?>[] _iceE_goto =
    {
        sizeof.class,
        _return.class
    };

    default void objc(int bycopy, int byref, int id, int IMP, int in, int inout, int nil, int NO, int oneway, int SEL, int _super, int YES)
    {
        objc(bycopy, byref, id, IMP, in, inout, nil, NO, oneway, SEL, _super, YES, com.zeroc.Ice.ObjectPrx.noExplicitContext);
    }

    default void objc(int bycopy, int byref, int id, int IMP, int in, int inout, int nil, int NO, int oneway, int SEL, int _super, int YES, java.util.Map<String, String> context)
    {
        _iceI_objcAsync(bycopy, byref, id, IMP, in, inout, nil, NO, oneway, SEL, _super, YES, context, true).waitForResponse();
    }

    default java.util.concurrent.CompletableFuture<Void> objcAsync(int bycopy, int byref, int id, int IMP, int in, int inout, int nil, int NO, int oneway, int SEL, int _super, int YES)
    {
        return _iceI_objcAsync(bycopy, byref, id, IMP, in, inout, nil, NO, oneway, SEL, _super, YES, com.zeroc.Ice.ObjectPrx.noExplicitContext, false);
    }

    default java.util.concurrent.CompletableFuture<Void> objcAsync(int bycopy, int byref, int id, int IMP, int in, int inout, int nil, int NO, int oneway, int SEL, int _super, int YES, java.util.Map<String, String> context)
    {
        return _iceI_objcAsync(bycopy, byref, id, IMP, in, inout, nil, NO, oneway, SEL, _super, YES, context, false);
    }

    /**
     * @hidden
     * @param iceP_bycopy -
     * @param iceP_byref -
     * @param iceP_id -
     * @param iceP_IMP -
     * @param iceP_in -
     * @param iceP_inout -
     * @param iceP_nil -
     * @param iceP_NO -
     * @param iceP_oneway -
     * @param iceP_SEL -
     * @param iceP_super -
     * @param iceP_YES -
     * @param context -
     * @param sync -
     * @return -
     **/
    default com.zeroc.IceInternal.OutgoingAsync<Void> _iceI_objcAsync(int iceP_bycopy, int iceP_byref, int iceP_id, int iceP_IMP, int iceP_in, int iceP_inout, int iceP_nil, int iceP_NO, int iceP_oneway, int iceP_SEL, int iceP_super, int iceP_YES, java.util.Map<String, String> context, boolean sync)
    {
        com.zeroc.IceInternal.OutgoingAsync<Void> f = new com.zeroc.IceInternal.OutgoingAsync<>(this, "objc", null, sync, null);
        f.invoke(false, context, null, ostr -> {
                     ostr.writeInt(iceP_bycopy);
                     ostr.writeInt(iceP_byref);
                     ostr.writeInt(iceP_id);
                     ostr.writeInt(iceP_IMP);
                     ostr.writeInt(iceP_in);
                     ostr.writeInt(iceP_inout);
                     ostr.writeInt(iceP_nil);
                     ostr.writeInt(iceP_NO);
                     ostr.writeInt(iceP_oneway);
                     ostr.writeInt(iceP_SEL);
                     ostr.writeInt(iceP_super);
                     ostr.writeInt(iceP_YES);
                 }, null);
        return f;
    }

    /**
     * Contacts the remote server to verify that the object implements this type.
     * Raises a local exception if a communication error occurs.
     * @param obj The untyped proxy.
     * @return A proxy for this type, or null if the object does not support this type.
     **/
    static friendPrx checkedCast(com.zeroc.Ice.ObjectPrx obj)
    {
        return com.zeroc.Ice.ObjectPrx._checkedCast(obj, ice_staticId(), friendPrx.class, _friendPrxI.class);
    }

    /**
     * Contacts the remote server to verify that the object implements this type.
     * Raises a local exception if a communication error occurs.
     * @param obj The untyped proxy.
     * @param context The Context map to send with the invocation.
     * @return A proxy for this type, or null if the object does not support this type.
     **/
    static friendPrx checkedCast(com.zeroc.Ice.ObjectPrx obj, java.util.Map<String, String> context)
    {
        return com.zeroc.Ice.ObjectPrx._checkedCast(obj, context, ice_staticId(), friendPrx.class, _friendPrxI.class);
    }

    /**
     * Contacts the remote server to verify that a facet of the object implements this type.
     * Raises a local exception if a communication error occurs.
     * @param obj The untyped proxy.
     * @param facet The name of the desired facet.
     * @return A proxy for this type, or null if the object does not support this type.
     **/
    static friendPrx checkedCast(com.zeroc.Ice.ObjectPrx obj, String facet)
    {
        return com.zeroc.Ice.ObjectPrx._checkedCast(obj, facet, ice_staticId(), friendPrx.class, _friendPrxI.class);
    }

    /**
     * Contacts the remote server to verify that a facet of the object implements this type.
     * Raises a local exception if a communication error occurs.
     * @param obj The untyped proxy.
     * @param facet The name of the desired facet.
     * @param context The Context map to send with the invocation.
     * @return A proxy for this type, or null if the object does not support this type.
     **/
    static friendPrx checkedCast(com.zeroc.Ice.ObjectPrx obj, String facet, java.util.Map<String, String> context)
    {
        return com.zeroc.Ice.ObjectPrx._checkedCast(obj, facet, context, ice_staticId(), friendPrx.class, _friendPrxI.class);
    }

    /**
     * Downcasts the given proxy to this type without contacting the remote server.
     * @param obj The untyped proxy.
     * @return A proxy for this type.
     **/
    static friendPrx uncheckedCast(com.zeroc.Ice.ObjectPrx obj)
    {
        return com.zeroc.Ice.ObjectPrx._uncheckedCast(obj, friendPrx.class, _friendPrxI.class);
    }

    /**
     * Downcasts the given proxy to this type without contacting the remote server.
     * @param obj The untyped proxy.
     * @param facet The name of the desired facet.
     * @return A proxy for this type.
     **/
    static friendPrx uncheckedCast(com.zeroc.Ice.ObjectPrx obj, String facet)
    {
        return com.zeroc.Ice.ObjectPrx._uncheckedCast(obj, facet, friendPrx.class, _friendPrxI.class);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for the per-proxy context.
     * @param newContext The context for the new proxy.
     * @return A proxy with the specified per-proxy context.
     **/
    @Override
    default friendPrx ice_context(java.util.Map<String, String> newContext)
    {
        return (friendPrx)_ice_context(newContext);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for the adapter ID.
     * @param newAdapterId The adapter ID for the new proxy.
     * @return A proxy with the specified adapter ID.
     **/
    @Override
    default friendPrx ice_adapterId(String newAdapterId)
    {
        return (friendPrx)_ice_adapterId(newAdapterId);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for the endpoints.
     * @param newEndpoints The endpoints for the new proxy.
     * @return A proxy with the specified endpoints.
     **/
    @Override
    default friendPrx ice_endpoints(com.zeroc.Ice.Endpoint[] newEndpoints)
    {
        return (friendPrx)_ice_endpoints(newEndpoints);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for the locator cache timeout.
     * @param newTimeout The new locator cache timeout (in seconds).
     * @return A proxy with the specified locator cache timeout.
     **/
    @Override
    default friendPrx ice_locatorCacheTimeout(int newTimeout)
    {
        return (friendPrx)_ice_locatorCacheTimeout(newTimeout);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for the invocation timeout.
     * @param newTimeout The new invocation timeout (in seconds).
     * @return A proxy with the specified invocation timeout.
     **/
    @Override
    default friendPrx ice_invocationTimeout(int newTimeout)
    {
        return (friendPrx)_ice_invocationTimeout(newTimeout);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for connection caching.
     * @param newCache <code>true</code> if the new proxy should cache connections; <code>false</code> otherwise.
     * @return A proxy with the specified caching policy.
     **/
    @Override
    default friendPrx ice_connectionCached(boolean newCache)
    {
        return (friendPrx)_ice_connectionCached(newCache);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for the endpoint selection policy.
     * @param newType The new endpoint selection policy.
     * @return A proxy with the specified endpoint selection policy.
     **/
    @Override
    default friendPrx ice_endpointSelection(com.zeroc.Ice.EndpointSelectionType newType)
    {
        return (friendPrx)_ice_endpointSelection(newType);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for how it selects endpoints.
     * @param b If <code>b</code> is <code>true</code>, only endpoints that use a secure transport are
     * used by the new proxy. If <code>b</code> is false, the returned proxy uses both secure and
     * insecure endpoints.
     * @return A proxy with the specified selection policy.
     **/
    @Override
    default friendPrx ice_secure(boolean b)
    {
        return (friendPrx)_ice_secure(b);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for the encoding used to marshal parameters.
     * @param e The encoding version to use to marshal request parameters.
     * @return A proxy with the specified encoding version.
     **/
    @Override
    default friendPrx ice_encodingVersion(com.zeroc.Ice.EncodingVersion e)
    {
        return (friendPrx)_ice_encodingVersion(e);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for its endpoint selection policy.
     * @param b If <code>b</code> is <code>true</code>, the new proxy will use secure endpoints for invocations
     * and only use insecure endpoints if an invocation cannot be made via secure endpoints. If <code>b</code> is
     * <code>false</code>, the proxy prefers insecure endpoints to secure ones.
     * @return A proxy with the specified selection policy.
     **/
    @Override
    default friendPrx ice_preferSecure(boolean b)
    {
        return (friendPrx)_ice_preferSecure(b);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for the router.
     * @param router The router for the new proxy.
     * @return A proxy with the specified router.
     **/
    @Override
    default friendPrx ice_router(com.zeroc.Ice.RouterPrx router)
    {
        return (friendPrx)_ice_router(router);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for the locator.
     * @param locator The locator for the new proxy.
     * @return A proxy with the specified locator.
     **/
    @Override
    default friendPrx ice_locator(com.zeroc.Ice.LocatorPrx locator)
    {
        return (friendPrx)_ice_locator(locator);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for collocation optimization.
     * @param b <code>true</code> if the new proxy enables collocation optimization; <code>false</code> otherwise.
     * @return A proxy with the specified collocation optimization.
     **/
    @Override
    default friendPrx ice_collocationOptimized(boolean b)
    {
        return (friendPrx)_ice_collocationOptimized(b);
    }

    /**
     * Returns a proxy that is identical to this proxy, but uses twoway invocations.
     * @return A proxy that uses twoway invocations.
     **/
    @Override
    default friendPrx ice_twoway()
    {
        return (friendPrx)_ice_twoway();
    }

    /**
     * Returns a proxy that is identical to this proxy, but uses oneway invocations.
     * @return A proxy that uses oneway invocations.
     **/
    @Override
    default friendPrx ice_oneway()
    {
        return (friendPrx)_ice_oneway();
    }

    /**
     * Returns a proxy that is identical to this proxy, but uses batch oneway invocations.
     * @return A proxy that uses batch oneway invocations.
     **/
    @Override
    default friendPrx ice_batchOneway()
    {
        return (friendPrx)_ice_batchOneway();
    }

    /**
     * Returns a proxy that is identical to this proxy, but uses datagram invocations.
     * @return A proxy that uses datagram invocations.
     **/
    @Override
    default friendPrx ice_datagram()
    {
        return (friendPrx)_ice_datagram();
    }

    /**
     * Returns a proxy that is identical to this proxy, but uses batch datagram invocations.
     * @return A proxy that uses batch datagram invocations.
     **/
    @Override
    default friendPrx ice_batchDatagram()
    {
        return (friendPrx)_ice_batchDatagram();
    }

    /**
     * Returns a proxy that is identical to this proxy, except for compression.
     * @param co <code>true</code> enables compression for the new proxy; <code>false</code> disables compression.
     * @return A proxy with the specified compression setting.
     **/
    @Override
    default friendPrx ice_compress(boolean co)
    {
        return (friendPrx)_ice_compress(co);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for its connection timeout setting.
     * @param t The connection timeout for the proxy in milliseconds.
     * @return A proxy with the specified timeout.
     **/
    @Override
    default friendPrx ice_timeout(int t)
    {
        return (friendPrx)_ice_timeout(t);
    }

    /**
     * Returns a proxy that is identical to this proxy, except for its connection ID.
     * @param connectionId The connection ID for the new proxy. An empty string removes the connection ID.
     * @return A proxy with the specified connection ID.
     **/
    @Override
    default friendPrx ice_connectionId(String connectionId)
    {
        return (friendPrx)_ice_connectionId(connectionId);
    }

    /**
     * Returns a proxy that is identical to this proxy, except it's a fixed proxy bound
     * the given connection.@param connection The fixed proxy connection.
     * @return A fixed proxy bound to the given connection.
     **/
    @Override
    default friendPrx ice_fixed(com.zeroc.Ice.Connection connection)
    {
        return (friendPrx)_ice_fixed(connection);
    }

    static String ice_staticId()
    {
        return "::and::friend";
    }
}
