# Copyright (c) ZeroC, Inc.

# slice2py version 3.8.0-alpha.0
# <auto-generated>Generated from Slice file 'Key.ice'.</auto-generated>

import Ice
import IcePy
import builtins as _builtins

# Start of module and
_M_and = Ice.openModule("and")
__name__ = "and"

_M_and.require_once = 1

if "array" not in _M_and.__dict__:
    _M_and.array = None
    class array(Ice.EnumBase):

        def __init__(self, _n, _v):
            Ice.EnumBase.__init__(self, _n, _v)

        def valueOf(self, _n):
            if _n in self._enumerators:
                return self._enumerators[_n]
            return None
        valueOf = classmethod(valueOf)

        def __repr__(self):
            if self._value in _M_and.array._enumerators:
                return f"and.array.{self._name}"
            else:
                return f"and.array({self._name!r}, {self._value!r})"

    array.clone = array("clone", 1)
    array._enumerators = { 1:array.clone }

    _M_and._t_array = IcePy.defineEnum("::and::array", array, (), array._enumerators)

    _M_and.array = array
    del array

if "_t_endfor" not in _M_and.__dict__:
    _M_and._t_endfor = IcePy.defineSequence("::and::endfor", (), _M_and._t_array)

if "_t_endforeach" not in _M_and.__dict__:
    _M_and._t_endforeach = IcePy.defineDictionary("::and::endforeach", (), IcePy._t_string, _M_and._t_array)

if "xor" not in _M_and.__dict__:
    _M_and.xor = None
    class xor(object):
        def __init__(self, abstract=0, var=_M_and.require_once):
            self.abstract = abstract
            self.var = var

        def __hash__(self):
            _h = 0
            _h = 5 * _h + _builtins.hash(self.abstract)
            _h = 5 * _h + _builtins.hash(self.var)
            return _h % 0x7fffffff

        def __compare(self, other):
            if other is None:
                return 1
            elif not isinstance(other, _M_and.xor):
                return NotImplemented
            else:
                if self.abstract is None or other.abstract is None:
                    if self.abstract != other.abstract:
                        return (-1 if self.abstract is None else 1)
                else:
                    if self.abstract < other.abstract:
                        return -1
                    elif self.abstract > other.abstract:
                        return 1
                if self.var is None or other.var is None:
                    if self.var != other.var:
                        return (-1 if self.var is None else 1)
                else:
                    if self.var < other.var:
                        return -1
                    elif self.var > other.var:
                        return 1
                return 0

        def __lt__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r < 0

        def __le__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r <= 0

        def __gt__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r > 0

        def __ge__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r >= 0

        def __eq__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r == 0

        def __ne__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r != 0

        def __repr__(self):
            return f"and.xor({Ice.Util.format_fields(abstract=self.abstract, var=self.var)})"

        def __str__(self):
            return repr(self)

    _M_and._t_xor = IcePy.defineStruct("::and::xor", xor, (), (
        ("abstract", (), IcePy._t_int),
        ("var", (), IcePy._t_int)
    ))

    _M_and.xor = xor
    del xor

if "endif" not in _M_and.__dict__:
    _M_and.endif = None
    class endif(Ice.UserException):
        def __init__(self, switch=0):
            self.switch = switch

        def __repr__(self):
            return f"and.endif({Ice.Util.format_fields(switch=self.switch)})"

        _ice_id = "::and::endif"

    _M_and._t_endif = IcePy.defineException("::and::endif", endif, (), None, (("switch", (), IcePy._t_int, False, 0),))
    endif._ice_type = _M_and._t_endif

    _M_and.endif = endif
    del endif

if "endwhile" not in _M_and.__dict__:
    _M_and.endwhile = None
    class endwhile(_M_and.endif):
        def __init__(self, switch=0, eval=0, exit=_M_and.require_once):
            _M_and.endif.__init__(self, switch)
            self.eval = eval
            self.exit = exit

        def __repr__(self):
            return f"and.endwhile({Ice.Util.format_fields(switch=self.switch, eval=self.eval, exit=self.exit)})"

        _ice_id = "::and::endwhile"

    _M_and._t_endwhile = IcePy.defineException("::and::endwhile", endwhile, (), _M_and._t_endif, (
        ("eval", (), IcePy._t_int, False, 0),
        ("exit", (), IcePy._t_int, False, 0)
    ))
    endwhile._ice_type = _M_and._t_endwhile

    _M_and.endwhile = endwhile
    del endwhile

if "function" not in _M_and.__dict__:
    _M_and._t_functionPrx = IcePy.declareProxy("::and::function")

if "functionPrx" not in _M_and.__dict__:
    _M_and.functionPrx = None
    class functionPrx(Ice.ObjectPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new functionPrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def continue(self, declare, default, context=None):
            return _M_and.function._op_continue.invoke(self, ((declare, default), context))

        def continueAsync(self, declare, default, context=None):
            return _M_and.function._op_continue.invokeAsync(self, ((declare, default), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_and.functionPrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_and.functionPrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_and.functionPrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::and::function"
    _M_and._t_functionPrx = IcePy.defineProxy("::and::function", functionPrx)

    _M_and.functionPrx = functionPrx
    del functionPrx

    _M_and.function = None
    class function(Ice.Object):

        def ice_ids(self, current):
            return ("::Ice::Object", "::and::function")

        def ice_id(self, current):
            return "::and::function"

        @staticmethod
        def ice_staticId():
            return "::and::function"

        def continue(self, declare, default, current):
            raise NotImplementedError("servant method 'continue' not implemented")

    function._op_continue = IcePy.Operation("continue", "continue", Ice.OperationMode.Normal, None, (), (((), IcePy._t_int, False, 0), ((), IcePy._t_int, False, 0)), (), None, ())

    _M_and.function = function
    del function

if "die" not in _M_and.__dict__:
    _M_and._t_diePrx = IcePy.declareProxy("::and::die")

if "diePrx" not in _M_and.__dict__:
    _M_and.diePrx = None
    class diePrx(Ice.ObjectPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new diePrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def do(self, context=None):
            return _M_and.die._op_do.invoke(self, ((), context))

        def doAsync(self, context=None):
            return _M_and.die._op_do.invokeAsync(self, ((), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_and.diePrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_and.diePrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_and.diePrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::and::die"
    _M_and._t_diePrx = IcePy.defineProxy("::and::die", diePrx)

    _M_and.diePrx = diePrx
    del diePrx

    _M_and.die = None
    class die(Ice.Object):

        def ice_ids(self, current):
            return ("::Ice::Object", "::and::die")

        def ice_id(self, current):
            return "::and::die"

        @staticmethod
        def ice_staticId():
            return "::and::die"

        def do(self, current):
            raise NotImplementedError("servant method 'do' not implemented")

    die._op_do = IcePy.Operation("do", "do", Ice.OperationMode.Normal, None, (), (), (), None, ())

    _M_and.die = die
    del die

if "echo" not in _M_and.__dict__:
    _M_and._t_echo = IcePy.declareValue("::and::echo")

if "echo" not in _M_and.__dict__:
    _M_and.echo = None
    class echo(Ice.Value):
        def __init__(self, if=0, empty=None):
            self.if = if
            self.empty = empty

        def ice_id(self):
            return "::and::echo"

        @staticmethod
        def ice_staticId():
            return "::and::echo"

        def __repr__(self):
            return f"and.echo({Ice.Util.format_fields(if=self.if, empty=self.empty)})"

    _M_and._t_echo = IcePy.defineValue("::and::echo", echo, -1, (), False, None, (
        ("if", (), IcePy._t_int, False, 0),
        ("empty", (), _M_and._t_diePrx, False, 0)
    ))
    echo._ice_type = _M_and._t_echo

    _M_and.echo = echo
    del echo

if "enddeclare" not in _M_and.__dict__:
    _M_and._t_enddeclarePrx = IcePy.declareProxy("::and::enddeclare")

if "enddeclarePrx" not in _M_and.__dict__:
    _M_and.enddeclarePrx = None
    class enddeclarePrx(_M_and.diePrx, _M_and.functionPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new enddeclarePrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def foreach(self, global, include=None, context=None):
            return _M_and.enddeclare._op_foreach.invoke(self, ((global, include), context))

        def foreachAsync(self, global, include, context=None):
            return _M_and.enddeclare._op_foreach.invokeAsync(self, ((global, include), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_and.enddeclarePrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_and.enddeclarePrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_and.enddeclarePrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::and::enddeclare"
    _M_and._t_enddeclarePrx = IcePy.defineProxy("::and::enddeclare", enddeclarePrx)

    _M_and.enddeclarePrx = enddeclarePrx
    del enddeclarePrx

    _M_and.enddeclare = None
    class enddeclare(_M_and.die, _M_and.function):

        def ice_ids(self, current):
            return ("::Ice::Object", "::and::die", "::and::enddeclare", "::and::function")

        def ice_id(self, current):
            return "::and::enddeclare"

        @staticmethod
        def ice_staticId():
            return "::and::enddeclare"

        def foreach(self, global, include, current):
            raise NotImplementedError("servant method 'foreach' not implemented")

    enddeclare._op_foreach = IcePy.Operation("foreach", "foreach", Ice.OperationMode.Normal, None, (), (((), _M_and._t_echo, False, 0), ((), _M_and._t_functionPrx, True, 1)), (((), _M_and._t_endfor, False, 0), ((), _M_and._t_endforeach, True, 2)), ((), _M_and._t_array, False, 0), (_M_and._t_endif, _M_and._t_endwhile))

    _M_and.enddeclare = enddeclare
    del enddeclare

# End of module and
