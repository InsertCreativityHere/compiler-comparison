# Copyright (c) ZeroC, Inc.

# slice2py version 3.8.0-alpha.0
# <auto-generated>Generated from Slice file 'Key.ice'.</auto-generated>

import Ice
import IcePy
import builtins as _builtins

# Start of module escaped_and
_M_escaped_and = Ice.openModule("escaped_and")
__name__ = "escaped_and"

if "_assert" not in _M_escaped_and.__dict__:
    _M_escaped_and._assert = None
    class _assert(Ice.EnumBase):

        def __init__(self, _n, _v):
            Ice.EnumBase.__init__(self, _n, _v)

        def valueOf(self, _n):
            if _n in self._enumerators:
                return self._enumerators[_n]
            return None
        valueOf = classmethod(valueOf)

        def __repr__(self):
            if self._value in _M_escaped_and._assert._enumerators:
                return f"escaped_and._assert.{self._name}"
            else:
                return f"escaped_and._assert({self._name!r}, {self._value!r})"

    _assert._break = _assert("break", 0)
    _assert._enumerators = { 0:_assert._break }

    _M_escaped_and._t__assert = IcePy.defineEnum("::and::assert", _assert, ("python:identifier:_assert",), _assert._enumerators)

    _M_escaped_and._assert = _assert
    del _assert

if "_continue" not in _M_escaped_and.__dict__:
    _M_escaped_and._continue = None
    class _continue(object):
        def __init__(self, _def=0):
            self._def = _def

        def __hash__(self):
            _h = 0
            _h = 5 * _h + _builtins.hash(self._def)
            return _h % 0x7fffffff

        def __compare(self, other):
            if other is None:
                return 1
            elif not isinstance(other, _M_escaped_and._continue):
                return NotImplemented
            else:
                if self._def is None or other._def is None:
                    if self._def != other._def:
                        return (-1 if self._def is None else 1)
                else:
                    if self._def < other._def:
                        return -1
                    elif self._def > other._def:
                        return 1
                return 0

        def __lt__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r < 0

        def __le__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r <= 0

        def __gt__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r > 0

        def __ge__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r >= 0

        def __eq__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r == 0

        def __ne__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r != 0

        def __repr__(self):
            return f"escaped_and._continue({Ice.Util.format_fields(_def=self._def)})"

        def __str__(self):
            return repr(self)

    _M_escaped_and._t__continue = IcePy.defineStruct("::and::continue", _continue, ("python:identifier:_continue",), (("_def", ("python:identifier:_def",), IcePy._t_int),))

    _M_escaped_and._continue = _continue
    del _continue

if "_del" not in _M_escaped_and.__dict__:
    _M_escaped_and._t__delPrx = IcePy.declareProxy("::and::del")

if "_delPrx" not in _M_escaped_and.__dict__:
    _M_escaped_and._delPrx = None
    class _delPrx(Ice.ObjectPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new _delPrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def _elif(self, _else, context=None):
            return _M_escaped_and._del._op_elif.invoke(self, ((_else, ), context))

        def _elifAsync(self, _else, context=None):
            return _M_escaped_and._del._op_elif.invokeAsync(self, ((_else, ), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_escaped_and._delPrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_escaped_and._delPrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_escaped_and._delPrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::and::del"
    _M_escaped_and._t__delPrx = IcePy.defineProxy("::and::del", _delPrx)

    _M_escaped_and._delPrx = _delPrx
    del _delPrx

    _M_escaped_and._del = None
    class _del(Ice.Object):

        def ice_ids(self, current):
            return ("::Ice::Object", "::and::del")

        def ice_id(self, current):
            return "::and::del"

        @staticmethod
        def ice_staticId():
            return "::and::del"

        def _elif(self, _else, current):
            raise NotImplementedError("servant method '_elif' not implemented")

    _del._op_elif = IcePy.Operation("elif", "_elif", Ice.OperationMode.Normal, None, ("python:identifier:_elif",), ((("python:identifier:_else",), IcePy._t_int, False, 0),), ((("python:identifier:_except",), IcePy._t_int, False, 0),), None, ())

    _M_escaped_and._del = _del
    del _del

if "_exec" not in _M_escaped_and.__dict__:
    _M_escaped_and._t__execPrx = IcePy.declareProxy("::and::exec")

if "_execPrx" not in _M_escaped_and.__dict__:
    _M_escaped_and._execPrx = None
    class _execPrx(Ice.ObjectPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new _execPrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def _finally(self, context=None):
            return _M_escaped_and._exec._op_finally.invoke(self, ((), context))

        def _finallyAsync(self, context=None):
            return _M_escaped_and._exec._op_finally.invokeAsync(self, ((), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_escaped_and._execPrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_escaped_and._execPrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_escaped_and._execPrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::and::exec"
    _M_escaped_and._t__execPrx = IcePy.defineProxy("::and::exec", _execPrx)

    _M_escaped_and._execPrx = _execPrx
    del _execPrx

    _M_escaped_and._exec = None
    class _exec(Ice.Object):

        def ice_ids(self, current):
            return ("::Ice::Object", "::and::exec")

        def ice_id(self, current):
            return "::and::exec"

        @staticmethod
        def ice_staticId():
            return "::and::exec"

        def _finally(self, current):
            raise NotImplementedError("servant method '_finally' not implemented")

    _exec._op_finally = IcePy.Operation("finally", "_finally", Ice.OperationMode.Normal, None, ("python:identifier:_finally",), (), (), None, ())

    _M_escaped_and._exec = _exec
    del _exec

if "_for" not in _M_escaped_and.__dict__:
    _M_escaped_and._t__for = IcePy.declareValue("::and::for")

if "_for" not in _M_escaped_and.__dict__:
    _M_escaped_and._for = None
    class _for(Ice.Value):
        def __init__(self, foo=0, _from=None):
            self.foo = foo
            self._from = _from

        def ice_id(self):
            return "::and::for"

        @staticmethod
        def ice_staticId():
            return "::and::for"

        def __repr__(self):
            return f"escaped_and._for({Ice.Util.format_fields(foo=self.foo, _from=self._from)})"

    _M_escaped_and._t__for = IcePy.defineValue("::and::for", _for, -1, ("python:identifier:_for",), False, None, (
        ("foo", (), IcePy._t_int, False, 0),
        ("_from", ("python:identifier:_from",), _M_escaped_and._t__execPrx, False, 0)
    ))
    _for._ice_type = _M_escaped_and._t__for

    _M_escaped_and._for = _for
    del _for

if "_is" not in _M_escaped_and.__dict__:
    _M_escaped_and._is = None
    class _is(Ice.UserException):
        def __init__(self, bar=0):
            self.bar = bar

        def __repr__(self):
            return f"escaped_and._is({Ice.Util.format_fields(bar=self.bar)})"

        _ice_id = "::and::is"

    _M_escaped_and._t__is = IcePy.defineException("::and::is", _is, ("python:identifier:_is",), None, (("bar", (), IcePy._t_int, False, 0),))
    _is._ice_type = _M_escaped_and._t__is

    _M_escaped_and._is = _is
    del _is

if "_not" not in _M_escaped_and.__dict__:
    _M_escaped_and._not = None
    class _not(_M_escaped_and._is):
        def __init__(self, bar=0, _pass=0):
            _M_escaped_and._is.__init__(self, bar)
            self._pass = _pass

        def __repr__(self):
            return f"escaped_and._not({Ice.Util.format_fields(bar=self.bar, _pass=self._pass)})"

        _ice_id = "::and::not"

    _M_escaped_and._t__not = IcePy.defineException("::and::not", _not, ("python:identifier:_not",), _M_escaped_and._t__is, (("_pass", ("python:identifier:_pass",), IcePy._t_int, False, 0),))
    _not._ice_type = _M_escaped_and._t__not

    _M_escaped_and._not = _not
    del _not

if "_if" not in _M_escaped_and.__dict__:
    _M_escaped_and._t__ifPrx = IcePy.declareProxy("::and::if")

if "_ifPrx" not in _M_escaped_and.__dict__:
    _M_escaped_and._ifPrx = None
    class _ifPrx(_M_escaped_and._execPrx, _M_escaped_and._delPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new _ifPrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def _raise(self, _else, _return, _while, _yield, _or, _global, context=None):
            return _M_escaped_and._if._op_raise.invoke(self, ((_else, _return, _while, _yield, _or, _global), context))

        def _raiseAsync(self, _else, _return, _while, _yield, _or, _global, context=None):
            return _M_escaped_and._if._op_raise.invokeAsync(self, ((_else, _return, _while, _yield, _or, _global), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_escaped_and._ifPrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_escaped_and._ifPrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_escaped_and._ifPrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::and::if"
    _M_escaped_and._t__ifPrx = IcePy.defineProxy("::and::if", _ifPrx)

    _M_escaped_and._ifPrx = _ifPrx
    del _ifPrx

    _M_escaped_and._if = None
    class _if(_M_escaped_and._exec, _M_escaped_and._del):

        def ice_ids(self, current):
            return ("::Ice::Object", "::and::del", "::and::exec", "::and::if")

        def ice_id(self, current):
            return "::and::if"

        @staticmethod
        def ice_staticId():
            return "::and::if"

        def _raise(self, _else, _return, _while, _yield, _or, _global, current):
            raise NotImplementedError("servant method '_raise' not implemented")

    _if._op_raise = IcePy.Operation("raise", "_raise", Ice.OperationMode.Normal, None, ("python:identifier:_raise",), ((("python:identifier:_else",), _M_escaped_and._t__continue, False, 0), (("python:identifier:_return",), _M_escaped_and._t__for, False, 0), (("python:identifier:_while",), _M_escaped_and._t__delPrx, False, 0), (("python:identifier:_yield",), _M_escaped_and._t__execPrx, False, 0), (("python:identifier:_or",), _M_escaped_and._t__ifPrx, False, 0), (("python:identifier:_global",), IcePy._t_int, False, 0)), (), ((), _M_escaped_and._t__assert, False, 0), (_M_escaped_and._t__is,))

    _M_escaped_and._if = _if
    del _if

if "_t__import" not in _M_escaped_and.__dict__:
    _M_escaped_and._t__import = IcePy.defineSequence("::and::import", ("python:identifier:_import",), _M_escaped_and._t__assert)

if "_t__in" not in _M_escaped_and.__dict__:
    _M_escaped_and._t__in = IcePy.defineDictionary("::and::in", ("python:identifier:_in",), IcePy._t_string, _M_escaped_and._t__assert)

_M_escaped_and._lambda = 0

if "EnumNone" not in _M_escaped_and.__dict__:
    _M_escaped_and.EnumNone = None
    class EnumNone(Ice.EnumBase):

        def __init__(self, _n, _v):
            Ice.EnumBase.__init__(self, _n, _v)

        def valueOf(self, _n):
            if _n in self._enumerators:
                return self._enumerators[_n]
            return None
        valueOf = classmethod(valueOf)

        def __repr__(self):
            if self._value in _M_escaped_and.EnumNone._enumerators:
                return f"escaped_and.EnumNone.{self._name}"
            else:
                return f"escaped_and.EnumNone({self._name!r}, {self._value!r})"

    EnumNone._None = EnumNone("None", 0)
    EnumNone._enumerators = { 0:EnumNone._None }

    _M_escaped_and._t_EnumNone = IcePy.defineEnum("::and::EnumNone", EnumNone, (), EnumNone._enumerators)

    _M_escaped_and.EnumNone = EnumNone
    del EnumNone

# End of module escaped_and
