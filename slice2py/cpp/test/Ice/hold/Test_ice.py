# Copyright (c) ZeroC, Inc.

# slice2py version 3.8.0-alpha.0
# <auto-generated>Generated from Slice file 'Test.ice'.</auto-generated>

import Ice
import IcePy
import builtins as _builtins

# Start of module Test
_M_Test = Ice.openModule("Test")
__name__ = "Test"

if "Hold" not in _M_Test.__dict__:
    _M_Test._t_HoldPrx = IcePy.declareProxy("::Test::Hold")

if "HoldPrx" not in _M_Test.__dict__:
    _M_Test.HoldPrx = None
    class HoldPrx(Ice.ObjectPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new HoldPrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def putOnHold(self, delay, context=None):
            """
            Puts the adapter on hold, and optionally reactivates it.
            
            Parameters
            ----------
            delay : int
                When less than 0, puts the adapter on hold indefinitely. When 0, puts the adapter on hold and
                immediately reactivates it. When greater than 0, starts a background task that sleeps for delay
                milliseconds, puts the adapter on hold and then immediately reactivates it.
            context : dict[str, str]
                The request context for the invocation.
            """
            return _M_Test.Hold._op_putOnHold.invoke(self, ((delay, ), context))

        def putOnHoldAsync(self, delay, context=None):
            """
            Puts the adapter on hold, and optionally reactivates it.
            
            Parameters
            ----------
            delay : int
                When less than 0, puts the adapter on hold indefinitely. When 0, puts the adapter on hold and
                immediately reactivates it. When greater than 0, starts a background task that sleeps for delay
                milliseconds, puts the adapter on hold and then immediately reactivates it.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable
                An awaitable that is completed when the invocation completes.
            """
            return _M_Test.Hold._op_putOnHold.invokeAsync(self, ((delay, ), context))

        def waitForHold(self, context=None):
            """
            Starts a background task that calls waitForHold and activate on the adapter.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            """
            return _M_Test.Hold._op_waitForHold.invoke(self, ((), context))

        def waitForHoldAsync(self, context=None):
            """
            Starts a background task that calls waitForHold and activate on the adapter.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable
                An awaitable that is completed when the invocation completes.
            """
            return _M_Test.Hold._op_waitForHold.invokeAsync(self, ((), context))

        def setOneway(self, value, expected, context=None):
            """
            Saves value as the last value.
            
            Parameters
            ----------
            value : int
                The new value.
            expected : int
                The current value as expected by the caller.
            context : dict[str, str]
                The request context for the invocation.
            """
            return _M_Test.Hold._op_setOneway.invoke(self, ((value, expected), context))

        def setOnewayAsync(self, value, expected, context=None):
            """
            Saves value as the last value.
            
            Parameters
            ----------
            value : int
                The new value.
            expected : int
                The current value as expected by the caller.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable
                An awaitable that is completed when the invocation completes.
            """
            return _M_Test.Hold._op_setOneway.invokeAsync(self, ((value, expected), context))

        def set(self, value, delay, context=None):
            """
            Saves value as the last value after a delay.
            
            Parameters
            ----------
            value : int
                The new value.
            delay : int
                The delay in milliseconds.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            int
                The previous value.
            """
            return _M_Test.Hold._op_set.invoke(self, ((value, delay), context))

        def setAsync(self, value, delay, context=None):
            """
            Saves value as the last value after a delay.
            
            Parameters
            ----------
            value : int
                The new value.
            delay : int
                The delay in milliseconds.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable of int
                The previous value.
            """
            return _M_Test.Hold._op_set.invokeAsync(self, ((value, delay), context))

        def shutdown(self, context=None):
            """
            Shuts down the server.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            """
            return _M_Test.Hold._op_shutdown.invoke(self, ((), context))

        def shutdownAsync(self, context=None):
            """
            Shuts down the server.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable
                An awaitable that is completed when the invocation completes.
            """
            return _M_Test.Hold._op_shutdown.invokeAsync(self, ((), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_Test.HoldPrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_Test.HoldPrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_Test.HoldPrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::Test::Hold"
    _M_Test._t_HoldPrx = IcePy.defineProxy("::Test::Hold", HoldPrx)

    _M_Test.HoldPrx = HoldPrx
    del HoldPrx

    _M_Test.Hold = None
    class Hold(Ice.Object):

        def ice_ids(self, current):
            return ("::Ice::Object", "::Test::Hold")

        def ice_id(self, current):
            return "::Test::Hold"

        @staticmethod
        def ice_staticId():
            return "::Test::Hold"

        def putOnHold(self, delay, current):
            """
            Puts the adapter on hold, and optionally reactivates it.
            
            Parameters
            ----------
            delay : int
                When less than 0, puts the adapter on hold indefinitely. When 0, puts the adapter on hold and
                immediately reactivates it. When greater than 0, starts a background task that sleeps for delay
                milliseconds, puts the adapter on hold and then immediately reactivates it.
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            None or awaitable
                None or an awaitable that completes when the dispatch completes.
            """
            raise NotImplementedError("servant method 'putOnHold' not implemented")

        def waitForHold(self, current):
            """
            Starts a background task that calls waitForHold and activate on the adapter.
            
            Parameters
            ----------
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            None or awaitable
                None or an awaitable that completes when the dispatch completes.
            """
            raise NotImplementedError("servant method 'waitForHold' not implemented")

        def setOneway(self, value, expected, current):
            """
            Saves value as the last value.
            
            Parameters
            ----------
            value : int
                The new value.
            expected : int
                The current value as expected by the caller.
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            None or awaitable
                None or an awaitable that completes when the dispatch completes.
            """
            raise NotImplementedError("servant method 'setOneway' not implemented")

        def set(self, value, delay, current):
            """
            Saves value as the last value after a delay.
            
            Parameters
            ----------
            value : int
                The new value.
            delay : int
                The delay in milliseconds.
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            int or awaitable of int
                The previous value.
            """
            raise NotImplementedError("servant method 'set' not implemented")

        def shutdown(self, current):
            """
            Shuts down the server.
            
            Parameters
            ----------
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            None or awaitable
                None or an awaitable that completes when the dispatch completes.
            """
            raise NotImplementedError("servant method 'shutdown' not implemented")

    Hold._op_putOnHold = IcePy.Operation("putOnHold", "putOnHold", Ice.OperationMode.Normal, None, (), (((), IcePy._t_int, False, 0),), (), None, ())
    Hold._op_waitForHold = IcePy.Operation("waitForHold", "waitForHold", Ice.OperationMode.Normal, None, (), (), (), None, ())
    Hold._op_setOneway = IcePy.Operation("setOneway", "setOneway", Ice.OperationMode.Normal, None, (), (((), IcePy._t_int, False, 0), ((), IcePy._t_int, False, 0)), (), None, ())
    Hold._op_set = IcePy.Operation("set", "set", Ice.OperationMode.Normal, None, (), (((), IcePy._t_int, False, 0), ((), IcePy._t_int, False, 0)), (), ((), IcePy._t_int, False, 0), ())
    Hold._op_shutdown = IcePy.Operation("shutdown", "shutdown", Ice.OperationMode.Normal, None, (), (), (), None, ())

    _M_Test.Hold = Hold
    del Hold

# End of module Test
