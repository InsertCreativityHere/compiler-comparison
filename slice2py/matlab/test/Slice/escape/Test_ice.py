# Copyright (c) ZeroC, Inc.

# slice2py version 3.8.0-alpha.0
# <auto-generated>Generated from Slice file 'Test.ice'.</auto-generated>

import Ice
import IcePy
import builtins as _builtins

# Start of module classdef
_M_classdef = Ice.openModule("classdef")
__name__ = "classdef"

if "persistent" not in _M_classdef.__dict__:
    _M_classdef.persistent = None
    class persistent(Ice.EnumBase):

        def __init__(self, _n, _v):
            Ice.EnumBase.__init__(self, _n, _v)

        def valueOf(self, _n):
            if _n in self._enumerators:
                return self._enumerators[_n]
            return None
        valueOf = classmethod(valueOf)

        def __repr__(self):
            if self._value in _M_classdef.persistent._enumerators:
                return f"classdef.persistent.{self._name}"
            else:
                return f"classdef.persistent({self._name!r}, {self._value!r})"

    persistent.break = persistent("break", 0)
    persistent.catch = persistent("catch", 1)
    persistent.LAST = persistent("LAST", 2)
    persistent._enumerators = { 0:persistent.break, 1:persistent.catch, 2:persistent.LAST }

    _M_classdef._t_persistent = IcePy.defineEnum("::classdef::persistent", persistent, (), persistent._enumerators)

    _M_classdef.persistent = persistent
    del persistent

if "global" not in _M_classdef.__dict__:
    _M_classdef.global = None
    class global(object):
        def __init__(self, case=_M_classdef.persistent.catch, continue=1, eq=2):
            self.case = case
            self.continue = continue
            self.eq = eq

        def __hash__(self):
            _h = 0
            _h = 5 * _h + _builtins.hash(self.case)
            _h = 5 * _h + _builtins.hash(self.continue)
            _h = 5 * _h + _builtins.hash(self.eq)
            return _h % 0x7fffffff

        def __compare(self, other):
            if other is None:
                return 1
            elif not isinstance(other, _M_classdef.global):
                return NotImplemented
            else:
                if self.case is None or other.case is None:
                    if self.case != other.case:
                        return (-1 if self.case is None else 1)
                else:
                    if self.case < other.case:
                        return -1
                    elif self.case > other.case:
                        return 1
                if self.continue is None or other.continue is None:
                    if self.continue != other.continue:
                        return (-1 if self.continue is None else 1)
                else:
                    if self.continue < other.continue:
                        return -1
                    elif self.continue > other.continue:
                        return 1
                if self.eq is None or other.eq is None:
                    if self.eq != other.eq:
                        return (-1 if self.eq is None else 1)
                else:
                    if self.eq < other.eq:
                        return -1
                    elif self.eq > other.eq:
                        return 1
                return 0

        def __lt__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r < 0

        def __le__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r <= 0

        def __gt__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r > 0

        def __ge__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r >= 0

        def __eq__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r == 0

        def __ne__(self, other):
            r = self.__compare(other)
            if r is NotImplemented:
                return r
            else:
                return r != 0

        def __repr__(self):
            return f"classdef.global({Ice.Util.format_fields(case=self.case, continue=self.continue, eq=self.eq)})"

        def __str__(self):
            return repr(self)

    _M_classdef._t_global = IcePy.defineStruct("::classdef::global", global, (), (
        ("case", (), _M_classdef._t_persistent),
        ("continue", (), IcePy._t_int),
        ("eq", (), IcePy._t_int)
    ))

    _M_classdef.global = global
    del global

if "logical" not in _M_classdef.__dict__:
    _M_classdef._t_logical = IcePy.declareValue("::classdef::logical")

if "logical" not in _M_classdef.__dict__:
    _M_classdef.logical = None
    class logical(Ice.Value):
        def __init__(self, else=_M_classdef.persistent.break, for=None, int64=True):
            self.else = else
            self.for = for if for is not None else _M_classdef.global()
            self.int64 = int64

        def ice_id(self):
            return "::classdef::logical"

        @staticmethod
        def ice_staticId():
            return "::classdef::logical"

        def __repr__(self):
            return f"classdef.logical({Ice.Util.format_fields(else=self.else, for=self.for, int64=self.int64)})"

    _M_classdef._t_logical = IcePy.defineValue("::classdef::logical", logical, -1, (), False, None, (
        ("else", (), _M_classdef._t_persistent, False, 0),
        ("for", (), _M_classdef._t_global, False, 0),
        ("int64", (), IcePy._t_bool, False, 0)
    ))
    logical._ice_type = _M_classdef._t_logical

    _M_classdef.logical = logical
    del logical

if "xor" not in _M_classdef.__dict__:
    _M_classdef._t_xor = IcePy.declareValue("::classdef::xor")

if "xor" not in _M_classdef.__dict__:
    _M_classdef.xor = None
    class xor(_M_classdef.logical):
        def __init__(self, else=_M_classdef.persistent.break, for=None, int64=True, return=1):
            _M_classdef.logical.__init__(self, else, for, int64)
            self.return = return

        def ice_id(self):
            return "::classdef::xor"

        @staticmethod
        def ice_staticId():
            return "::classdef::xor"

        def __repr__(self):
            return f"classdef.xor({Ice.Util.format_fields(else=self.else, for=self.for, int64=self.int64, return=self.return)})"

    _M_classdef._t_xor = IcePy.defineValue("::classdef::xor", xor, -1, (), False, _M_classdef._t_logical, (("return", (), IcePy._t_int, False, 0),))
    xor._ice_type = _M_classdef._t_xor

    _M_classdef.xor = xor
    del xor

if "_t_parfor" not in _M_classdef.__dict__:
    _M_classdef._t_parfor = IcePy.defineSequence("::classdef::parfor", (), _M_classdef._t_global)

if "_t_switch" not in _M_classdef.__dict__:
    _M_classdef._t_switch = IcePy.defineDictionary("::classdef::switch", (), IcePy._t_int, _M_classdef._t_global)

if "Base" not in _M_classdef.__dict__:
    _M_classdef._t_Base = IcePy.declareValue("::classdef::Base")

if "Base" not in _M_classdef.__dict__:
    _M_classdef.Base = None
    class Base(Ice.Value):
        def __init__(self, while=1):
            self.while = while

        def ice_id(self):
            return "::classdef::Base"

        @staticmethod
        def ice_staticId():
            return "::classdef::Base"

        def __repr__(self):
            return f"classdef.Base({Ice.Util.format_fields(while=self.while)})"

    _M_classdef._t_Base = IcePy.defineValue("::classdef::Base", Base, -1, (), False, None, (("while", (), IcePy._t_int, False, 0),))
    Base._ice_type = _M_classdef._t_Base

    _M_classdef.Base = Base
    del Base

if "Derived" not in _M_classdef.__dict__:
    _M_classdef._t_Derived = IcePy.declareValue("::classdef::Derived")

if "Derived" not in _M_classdef.__dict__:
    _M_classdef.Derived = None
    class Derived(_M_classdef.Base):
        def __init__(self, while=1, if=2, spmd=None, otherwise=None):
            _M_classdef.Base.__init__(self, while)
            self.if = if
            self.spmd = spmd
            self.otherwise = otherwise

        def ice_id(self):
            return "::classdef::Derived"

        @staticmethod
        def ice_staticId():
            return "::classdef::Derived"

        def __repr__(self):
            return f"classdef.Derived({Ice.Util.format_fields(while=self.while, if=self.if, spmd=self.spmd, otherwise=self.otherwise)})"

    _M_classdef._t_Derived = IcePy.defineValue("::classdef::Derived", Derived, -1, (), False, _M_classdef._t_Base, (
        ("if", (), IcePy._t_int, False, 0),
        ("spmd", (), _M_classdef._t_parfor, False, 0),
        ("otherwise", (), _M_classdef._t_switch, False, 0)
    ))
    Derived._ice_type = _M_classdef._t_Derived

    _M_classdef.Derived = Derived
    del Derived

if "bitand" not in _M_classdef.__dict__:
    _M_classdef.bitand = None
    class bitand(Ice.UserException):
        def __init__(self, identifier="1", message="2", end=None):
            self.identifier = identifier
            self.message = message
            self.end = end

        def __repr__(self):
            return f"classdef.bitand({Ice.Util.format_fields(identifier=self.identifier, message=self.message, end=self.end)})"

        _ice_id = "::classdef::bitand"

    _M_classdef._t_bitand = IcePy.defineException("::classdef::bitand", bitand, (), None, (
        ("identifier", (), IcePy._t_string, False, 0),
        ("message", (), IcePy._t_string, False, 0),
        ("end", (), _M_classdef._t_logical, False, 0)
    ))
    bitand._ice_type = _M_classdef._t_bitand

    _M_classdef.bitand = bitand
    del bitand

if "bitor" not in _M_classdef.__dict__:
    _M_classdef.bitor = None
    class bitor(_M_classdef.bitand):
        def __init__(self, identifier="1", message="2", end=None, enumeration=1):
            _M_classdef.bitand.__init__(self, identifier, message, end)
            self.enumeration = enumeration

        def __repr__(self):
            return f"classdef.bitor({Ice.Util.format_fields(identifier=self.identifier, message=self.message, end=self.end, enumeration=self.enumeration)})"

        _ice_id = "::classdef::bitor"

    _M_classdef._t_bitor = IcePy.defineException("::classdef::bitor", bitor, (), _M_classdef._t_bitand, (("enumeration", (), IcePy._t_int, False, 0),))
    bitor._ice_type = _M_classdef._t_bitor

    _M_classdef.bitor = bitor
    del bitor

if "operations" not in _M_classdef.__dict__:
    _M_classdef._t_operationsPrx = IcePy.declareProxy("::classdef::operations")

if "operationsPrx" not in _M_classdef.__dict__:
    _M_classdef.operationsPrx = None
    class operationsPrx(Ice.ObjectPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new operationsPrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def events(self, context=None):
            return _M_classdef.operations._op_events.invoke(self, ((), context))

        def eventsAsync(self, context=None):
            return _M_classdef.operations._op_events.invokeAsync(self, ((), context))

        def function(self, context=None):
            return _M_classdef.operations._op_function.invoke(self, ((), context))

        def functionAsync(self, context=None):
            return _M_classdef.operations._op_function.invokeAsync(self, ((), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_classdef.operationsPrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_classdef.operationsPrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_classdef.operationsPrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::classdef::operations"
    _M_classdef._t_operationsPrx = IcePy.defineProxy("::classdef::operations", operationsPrx)

    _M_classdef.operationsPrx = operationsPrx
    del operationsPrx

    _M_classdef.operations = None
    class operations(Ice.Object):

        def ice_ids(self, current):
            return ("::Ice::Object", "::classdef::operations")

        def ice_id(self, current):
            return "::classdef::operations"

        @staticmethod
        def ice_staticId():
            return "::classdef::operations"

        def events(self, current):
            raise NotImplementedError("servant method 'events' not implemented")

        def function(self, current):
            raise NotImplementedError("servant method 'function' not implemented")

    operations._op_events = IcePy.Operation("events", "events", Ice.OperationMode.Normal, None, (), (), (), None, ())
    operations._op_function = IcePy.Operation("function", "function", Ice.OperationMode.Normal, None, (), (), (), None, ())

    _M_classdef.operations = operations
    del operations

_M_classdef.methods = 1

# End of module classdef
