# Copyright (c) ZeroC, Inc.

# slice2py version 3.8.0-alpha.0
# <auto-generated>Generated from Slice file 'Router.ice'.</auto-generated>

import Ice
import IcePy
import builtins as _builtins
import Ice.Router_ice
import Glacier2.PermissionsVerifier_ice
import Glacier2.Session_ice

# Included module Ice
_M_Ice = Ice.openModule("Ice")

# Included module Glacier2
_M_Glacier2 = Ice.openModule("Glacier2")

# Start of module Glacier2
__name__ = "Glacier2"
_M_Glacier2.__doc__ = """
Communicate through firewalls and across NATs.
"""

if "SessionNotExistException" not in _M_Glacier2.__dict__:
    _M_Glacier2.SessionNotExistException = None
    class SessionNotExistException(Ice.UserException):
        """
        The exception that is thrown when a client tries to destroy a session with a router, but no session exists for
        this client.
        """
        def __init__(self):
            pass

        def __repr__(self):
            return f"Glacier2.SessionNotExistException()"

        _ice_id = "::Glacier2::SessionNotExistException"

    _M_Glacier2._t_SessionNotExistException = IcePy.defineException("::Glacier2::SessionNotExistException", SessionNotExistException, (), None, ())
    SessionNotExistException._ice_type = _M_Glacier2._t_SessionNotExistException

    _M_Glacier2.SessionNotExistException = SessionNotExistException
    del SessionNotExistException

if "Router" not in _M_Glacier2.__dict__:
    _M_Glacier2._t_RouterPrx = IcePy.declareProxy("::Glacier2::Router")

if "RouterPrx" not in _M_Glacier2.__dict__:
    _M_Glacier2.RouterPrx = None
    class RouterPrx(_M_Ice.RouterPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new RouterPrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def getCategoryForClient(self, context=None):
            """
            Gets a unique category that identifies the client (caller) in the router. This category must be used in the
            identities of all the client's callback objects.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            str
                The category. It's an empty string when `Glacier2.Server.Endpoints` is not configured.
            """
            return _M_Glacier2.Router._op_getCategoryForClient.invoke(self, ((), context))

        def getCategoryForClientAsync(self, context=None):
            """
            Gets a unique category that identifies the client (caller) in the router. This category must be used in the
            identities of all the client's callback objects.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable of str
                The category. It's an empty string when `Glacier2.Server.Endpoints` is not configured.
            """
            return _M_Glacier2.Router._op_getCategoryForClient.invokeAsync(self, ((), context))

        def createSession(self, userId, password, context=None):
            """
            Creates a session for the client (caller) with the router. If a :class:`Glacier2.SessionManagerPrx` is configured,
            a proxy to a :class:`Glacier2.SessionPrx` object is returned to the client. Otherwise, null is returned and only an
            internal session (i.e., not visible to the client) is created.
            If a non-null session proxy is returned, it must be configured to route through the router that created it.
            This occurs automatically when the router is configured as the client's default router at the time the
            session proxy is created in the client application; otherwise, the client must configure the session proxy
            explicitly.
            
            Parameters
            ----------
            userId : str
                The user ID.
            password : str
                The password.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            (Glacier2.SessionPrx or None)
                A proxy for the newly created session, or null if no :class:`Glacier2.SessionManagerPrx` is configured.
            
            Raises
            ------
            CannotCreateSessionException
                Thrown when the session cannot be created.
            PermissionDeniedException
                Thrown when an authentication or authorization failure occurs.
            """
            return _M_Glacier2.Router._op_createSession.invoke(self, ((userId, password), context))

        def createSessionAsync(self, userId, password, context=None):
            """
            Creates a session for the client (caller) with the router. If a :class:`Glacier2.SessionManagerPrx` is configured,
            a proxy to a :class:`Glacier2.SessionPrx` object is returned to the client. Otherwise, null is returned and only an
            internal session (i.e., not visible to the client) is created.
            If a non-null session proxy is returned, it must be configured to route through the router that created it.
            This occurs automatically when the router is configured as the client's default router at the time the
            session proxy is created in the client application; otherwise, the client must configure the session proxy
            explicitly.
            
            Parameters
            ----------
            userId : str
                The user ID.
            password : str
                The password.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable of (Glacier2.SessionPrx or None)
                A proxy for the newly created session, or null if no :class:`Glacier2.SessionManagerPrx` is configured.
            """
            return _M_Glacier2.Router._op_createSession.invokeAsync(self, ((userId, password), context))

        def createSessionFromSecureConnection(self, context=None):
            """
            Creates a per-client session with the router. The user is authenticated through the SSL certificate(s)
            associated with the connection. If an :class:`Glacier2.SSLSessionManagerPrx` is configured, a proxy to a :class:`Glacier2.SessionPrx`
            object is returned to the client. Otherwise, null is returned and only an internal session (i.e., not
            visible to the client) is created.
            If a non-null session proxy is returned, it must be configured to route through the router that created it.
            This occurs automatically when the router is configured as the client's default router at the time the
            session proxy is created in the client application; otherwise, the client must configure the session proxy
            explicitly.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            (Glacier2.SessionPrx or None)
                A proxy for the newly created session, or null if no :class:`Glacier2.SSLSessionManagerPrx` is configured.
            
            Raises
            ------
            CannotCreateSessionException
                Thrown when the session cannot be created.
            PermissionDeniedException
                Thrown when an authentication or authorization failure occurs.
            """
            return _M_Glacier2.Router._op_createSessionFromSecureConnection.invoke(self, ((), context))

        def createSessionFromSecureConnectionAsync(self, context=None):
            """
            Creates a per-client session with the router. The user is authenticated through the SSL certificate(s)
            associated with the connection. If an :class:`Glacier2.SSLSessionManagerPrx` is configured, a proxy to a :class:`Glacier2.SessionPrx`
            object is returned to the client. Otherwise, null is returned and only an internal session (i.e., not
            visible to the client) is created.
            If a non-null session proxy is returned, it must be configured to route through the router that created it.
            This occurs automatically when the router is configured as the client's default router at the time the
            session proxy is created in the client application; otherwise, the client must configure the session proxy
            explicitly.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable of (Glacier2.SessionPrx or None)
                A proxy for the newly created session, or null if no :class:`Glacier2.SSLSessionManagerPrx` is configured.
            """
            return _M_Glacier2.Router._op_createSessionFromSecureConnection.invokeAsync(self, ((), context))

        def refreshSession(self, context=None):
            """
            Keeps the session with this router alive.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Raises
            ------
            SessionNotExistException
                Thrown when no session exists for the caller (client).
            """
            return _M_Glacier2.Router._op_refreshSession.invoke(self, ((), context))

        def refreshSessionAsync(self, context=None):
            """
            Keeps the session with this router alive.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable
                An awaitable that is completed when the invocation completes.
            """
            return _M_Glacier2.Router._op_refreshSession.invokeAsync(self, ((), context))

        def destroySession(self, context=None):
            """
            Destroys the session of the caller with this router.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Raises
            ------
            SessionNotExistException
                Thrown when no session exists for the caller (client).
            """
            return _M_Glacier2.Router._op_destroySession.invoke(self, ((), context))

        def destroySessionAsync(self, context=None):
            """
            Destroys the session of the caller with this router.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable
                An awaitable that is completed when the invocation completes.
            """
            return _M_Glacier2.Router._op_destroySession.invokeAsync(self, ((), context))

        def getSessionTimeout(self, context=None):
            """
            Gets the idle timeout used by the server-side of the connection.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            int
                The idle timeout (in seconds).
            """
            return _M_Glacier2.Router._op_getSessionTimeout.invoke(self, ((), context))

        def getSessionTimeoutAsync(self, context=None):
            """
            Gets the idle timeout used by the server-side of the connection.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable of int
                The idle timeout (in seconds).
            """
            return _M_Glacier2.Router._op_getSessionTimeout.invokeAsync(self, ((), context))

        def getACMTimeout(self, context=None):
            """
            Gets the idle timeout used by the server-side of the connection.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            int
                The idle timeout (in seconds).
            """
            return _M_Glacier2.Router._op_getACMTimeout.invoke(self, ((), context))

        def getACMTimeoutAsync(self, context=None):
            """
            Gets the idle timeout used by the server-side of the connection.
            
            Parameters
            ----------
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable of int
                The idle timeout (in seconds).
            """
            return _M_Glacier2.Router._op_getACMTimeout.invokeAsync(self, ((), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_Glacier2.RouterPrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_Glacier2.RouterPrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_Glacier2.RouterPrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::Glacier2::Router"
    _M_Glacier2._t_RouterPrx = IcePy.defineProxy("::Glacier2::Router", RouterPrx)

    _M_Glacier2.RouterPrx = RouterPrx
    del RouterPrx

    _M_Glacier2.Router = None
    class Router(_M_Ice.Router):

        def ice_ids(self, current):
            return ("::Glacier2::Router", "::Ice::Object", "::Ice::Router")

        def ice_id(self, current):
            return "::Glacier2::Router"

        @staticmethod
        def ice_staticId():
            return "::Glacier2::Router"

        def getCategoryForClient(self, current):
            """
            Gets a unique category that identifies the client (caller) in the router. This category must be used in the
            identities of all the client's callback objects.
            
            Parameters
            ----------
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            str or awaitable of str
                The category. It's an empty string when `Glacier2.Server.Endpoints` is not configured.
            """
            raise NotImplementedError("servant method 'getCategoryForClient' not implemented")

        def createSession(self, userId, password, current):
            """
            Creates a session for the client (caller) with the router. If a :class:`Glacier2.SessionManagerPrx` is configured,
            a proxy to a :class:`Glacier2.SessionPrx` object is returned to the client. Otherwise, null is returned and only an
            internal session (i.e., not visible to the client) is created.
            If a non-null session proxy is returned, it must be configured to route through the router that created it.
            This occurs automatically when the router is configured as the client's default router at the time the
            session proxy is created in the client application; otherwise, the client must configure the session proxy
            explicitly.
            
            Parameters
            ----------
            userId : str
                The user ID.
            password : str
                The password.
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            (Glacier2.SessionPrx or None) or awaitable of (Glacier2.SessionPrx or None)
                A proxy for the newly created session, or null if no :class:`Glacier2.SessionManagerPrx` is configured.
            
            Raises
            ------
            CannotCreateSessionException
                Thrown when the session cannot be created.
            PermissionDeniedException
                Thrown when an authentication or authorization failure occurs.
            """
            raise NotImplementedError("servant method 'createSession' not implemented")

        def createSessionFromSecureConnection(self, current):
            """
            Creates a per-client session with the router. The user is authenticated through the SSL certificate(s)
            associated with the connection. If an :class:`Glacier2.SSLSessionManagerPrx` is configured, a proxy to a :class:`Glacier2.SessionPrx`
            object is returned to the client. Otherwise, null is returned and only an internal session (i.e., not
            visible to the client) is created.
            If a non-null session proxy is returned, it must be configured to route through the router that created it.
            This occurs automatically when the router is configured as the client's default router at the time the
            session proxy is created in the client application; otherwise, the client must configure the session proxy
            explicitly.
            
            Parameters
            ----------
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            (Glacier2.SessionPrx or None) or awaitable of (Glacier2.SessionPrx or None)
                A proxy for the newly created session, or null if no :class:`Glacier2.SSLSessionManagerPrx` is configured.
            
            Raises
            ------
            CannotCreateSessionException
                Thrown when the session cannot be created.
            PermissionDeniedException
                Thrown when an authentication or authorization failure occurs.
            """
            raise NotImplementedError("servant method 'createSessionFromSecureConnection' not implemented")

        def refreshSession(self, current):
            """
            Keeps the session with this router alive.
            
            Parameters
            ----------
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            None or awaitable
                None or an awaitable that completes when the dispatch completes.
            
            Raises
            ------
            SessionNotExistException
                Thrown when no session exists for the caller (client).
            """
            raise NotImplementedError("servant method 'refreshSession' not implemented")

        def destroySession(self, current):
            """
            Destroys the session of the caller with this router.
            
            Parameters
            ----------
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            None or awaitable
                None or an awaitable that completes when the dispatch completes.
            
            Raises
            ------
            SessionNotExistException
                Thrown when no session exists for the caller (client).
            """
            raise NotImplementedError("servant method 'destroySession' not implemented")

        def getSessionTimeout(self, current):
            """
            Gets the idle timeout used by the server-side of the connection.
            
            Parameters
            ----------
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            int or awaitable of int
                The idle timeout (in seconds).
            """
            raise NotImplementedError("servant method 'getSessionTimeout' not implemented")

        def getACMTimeout(self, current):
            """
            Gets the idle timeout used by the server-side of the connection.
            
            Parameters
            ----------
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            int or awaitable of int
                The idle timeout (in seconds).
            """
            raise NotImplementedError("servant method 'getACMTimeout' not implemented")

    Router._op_getCategoryForClient = IcePy.Operation("getCategoryForClient", "getCategoryForClient", Ice.OperationMode.Idempotent, None, (), (), (), ((), IcePy._t_string, False, 0), ())
    Router._op_createSession = IcePy.Operation("createSession", "createSession", Ice.OperationMode.Normal, None, (), (((), IcePy._t_string, False, 0), ((), IcePy._t_string, False, 0)), (), ((), _M_Glacier2._t_SessionPrx, False, 0), (_M_Glacier2._t_PermissionDeniedException, _M_Glacier2._t_CannotCreateSessionException))
    Router._op_createSessionFromSecureConnection = IcePy.Operation("createSessionFromSecureConnection", "createSessionFromSecureConnection", Ice.OperationMode.Normal, None, (), (), (), ((), _M_Glacier2._t_SessionPrx, False, 0), (_M_Glacier2._t_PermissionDeniedException, _M_Glacier2._t_CannotCreateSessionException))
    Router._op_refreshSession = IcePy.Operation("refreshSession", "refreshSession", Ice.OperationMode.Normal, None, (), (), (), None, (_M_Glacier2._t_SessionNotExistException,))
    Router._op_refreshSession.deprecate("As of Ice 3.8, this operation does nothing.")
    Router._op_destroySession = IcePy.Operation("destroySession", "destroySession", Ice.OperationMode.Normal, None, (), (), (), None, (_M_Glacier2._t_SessionNotExistException,))
    Router._op_getSessionTimeout = IcePy.Operation("getSessionTimeout", "getSessionTimeout", Ice.OperationMode.Idempotent, None, (), (), (), ((), IcePy._t_long, False, 0), ())
    Router._op_getACMTimeout = IcePy.Operation("getACMTimeout", "getACMTimeout", Ice.OperationMode.Idempotent, None, (), (), (), ((), IcePy._t_int, False, 0), ())

    _M_Glacier2.Router = Router
    del Router

# End of module Glacier2
