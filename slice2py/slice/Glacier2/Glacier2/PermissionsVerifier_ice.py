# Copyright (c) ZeroC, Inc.

# slice2py version 3.8.0-alpha.0
# <auto-generated>Generated from Slice file 'PermissionsVerifier.ice'.</auto-generated>

import Ice
import IcePy
import builtins as _builtins
import Glacier2.SSLInfo_ice

# Included module Ice
_M_Ice = Ice.openModule("Ice")

# Included module Glacier2
_M_Glacier2 = Ice.openModule("Glacier2")

# Start of module Glacier2
__name__ = "Glacier2"

if "PermissionDeniedException" not in _M_Glacier2.__dict__:
    _M_Glacier2.PermissionDeniedException = None
    class PermissionDeniedException(Ice.UserException):
        """
        The exception that is thrown when a client is not allowed to create a session.
        
        Attributes
        ----------
        reason : str
            The reason why permission was denied.
        """
        def __init__(self, reason=""):
            self.reason = reason

        def __repr__(self):
            return f"Glacier2.PermissionDeniedException({Ice.Util.format_fields(reason=self.reason)})"

        _ice_id = "::Glacier2::PermissionDeniedException"

    _M_Glacier2._t_PermissionDeniedException = IcePy.defineException("::Glacier2::PermissionDeniedException", PermissionDeniedException, (), None, (("reason", (), IcePy._t_string, False, 0),))
    PermissionDeniedException._ice_type = _M_Glacier2._t_PermissionDeniedException

    _M_Glacier2.PermissionDeniedException = PermissionDeniedException
    del PermissionDeniedException

if "PermissionsVerifier" not in _M_Glacier2.__dict__:
    _M_Glacier2._t_PermissionsVerifierPrx = IcePy.declareProxy("::Glacier2::PermissionsVerifier")

if "PermissionsVerifierPrx" not in _M_Glacier2.__dict__:
    _M_Glacier2.PermissionsVerifierPrx = None
    class PermissionsVerifierPrx(Ice.ObjectPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new PermissionsVerifierPrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def checkPermissions(self, userId, password, context=None):
            """
            Checks if a user is authorized to establish a session.
            
            Parameters
            ----------
            userId : str
                The user ID.
            password : str
                The user's password.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            tuple of (bool, str)
            
                A tuple containing:
                    - bool `true` if access is granted, `false` otherwise.
                    - str The reason why access was denied.
            
            Raises
            ------
            PermissionDeniedException
                Thrown when the user access is denied. This exception can be thrown
                instead of returning `false` with a reason set in the reason out parameter.
            """
            return _M_Glacier2.PermissionsVerifier._op_checkPermissions.invoke(self, ((userId, password), context))

        def checkPermissionsAsync(self, userId, password, context=None):
            """
            Checks if a user is authorized to establish a session.
            
            Parameters
            ----------
            userId : str
                The user ID.
            password : str
                The user's password.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable of tuple of (bool, str)
            
                A tuple containing:
                    - bool `true` if access is granted, `false` otherwise.
                    - str The reason why access was denied.
            """
            return _M_Glacier2.PermissionsVerifier._op_checkPermissions.invokeAsync(self, ((userId, password), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_Glacier2.PermissionsVerifierPrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_Glacier2.PermissionsVerifierPrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_Glacier2.PermissionsVerifierPrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::Glacier2::PermissionsVerifier"
    _M_Glacier2._t_PermissionsVerifierPrx = IcePy.defineProxy("::Glacier2::PermissionsVerifier", PermissionsVerifierPrx)

    _M_Glacier2.PermissionsVerifierPrx = PermissionsVerifierPrx
    del PermissionsVerifierPrx

    _M_Glacier2.PermissionsVerifier = None
    class PermissionsVerifier(Ice.Object):

        def ice_ids(self, current):
            return ("::Glacier2::PermissionsVerifier", "::Ice::Object")

        def ice_id(self, current):
            return "::Glacier2::PermissionsVerifier"

        @staticmethod
        def ice_staticId():
            return "::Glacier2::PermissionsVerifier"

        def checkPermissions(self, userId, password, current):
            """
            Checks if a user is authorized to establish a session.
            
            Parameters
            ----------
            userId : str
                The user ID.
            password : str
                The user's password.
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            tuple of (bool, str) or awaitable of tuple of (bool, str)
            
                A tuple containing:
                    - bool `true` if access is granted, `false` otherwise.
                    - str The reason why access was denied.
            
            Raises
            ------
            PermissionDeniedException
                Thrown when the user access is denied. This exception can be thrown
                instead of returning `false` with a reason set in the reason out parameter.
            """
            raise NotImplementedError("servant method 'checkPermissions' not implemented")

    PermissionsVerifier._op_checkPermissions = IcePy.Operation("checkPermissions", "checkPermissions", Ice.OperationMode.Idempotent, None, (), (((), IcePy._t_string, False, 0), ((), IcePy._t_string, False, 0)), (((), IcePy._t_string, False, 0),), ((), IcePy._t_bool, False, 0), (_M_Glacier2._t_PermissionDeniedException,))

    _M_Glacier2.PermissionsVerifier = PermissionsVerifier
    del PermissionsVerifier

if "SSLPermissionsVerifier" not in _M_Glacier2.__dict__:
    _M_Glacier2._t_SSLPermissionsVerifierPrx = IcePy.declareProxy("::Glacier2::SSLPermissionsVerifier")

if "SSLPermissionsVerifierPrx" not in _M_Glacier2.__dict__:
    _M_Glacier2.SSLPermissionsVerifierPrx = None
    class SSLPermissionsVerifierPrx(Ice.ObjectPrx):

        def __init__(self, communicator, proxyString):
            """
            Creates a new SSLPermissionsVerifierPrx proxy
            
            Parameters
            ----------
            communicator : Ice.Communicator
                The communicator of the new proxy.
            proxyString : str
                The string representation of the proxy.
            
            Raises
            ------
            ParseException
                Thrown when proxyString is not a valid proxy string.
            """
            super().__init__(communicator, proxyString)

        def authorize(self, info, context=None):
            """
            Checks if a user is authorized to establish a session.
            
            Parameters
            ----------
            info : Glacier2.SSLInfo
                The SSL information.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            tuple of (bool, str)
            
                A tuple containing:
                    - bool `true` if access is granted, `false` otherwise.
                    - str The reason why access was denied.
            
            Raises
            ------
            PermissionDeniedException
                Thrown when the user access is denied. This exception can be thrown
                instead of returning `false` with a reason set in the reason out parameter.
            """
            return _M_Glacier2.SSLPermissionsVerifier._op_authorize.invoke(self, ((info, ), context))

        def authorizeAsync(self, info, context=None):
            """
            Checks if a user is authorized to establish a session.
            
            Parameters
            ----------
            info : Glacier2.SSLInfo
                The SSL information.
            context : dict[str, str]
                The request context for the invocation.
            
            Returns
            -------
            awaitable of tuple of (bool, str)
            
                A tuple containing:
                    - bool `true` if access is granted, `false` otherwise.
                    - str The reason why access was denied.
            """
            return _M_Glacier2.SSLPermissionsVerifier._op_authorize.invokeAsync(self, ((info, ), context))

        @staticmethod
        def checkedCast(proxy, facet=None, context=None):
            return Ice.checkedCast(_M_Glacier2.SSLPermissionsVerifierPrx, proxy, facet, context)

        @staticmethod
        def checkedCastAsync(proxy, facet=None, context=None):
            return Ice.checkedCastAsync(_M_Glacier2.SSLPermissionsVerifierPrx, proxy, facet, context)

        @staticmethod
        def uncheckedCast(proxy, facet=None):
            return Ice.uncheckedCast(_M_Glacier2.SSLPermissionsVerifierPrx, proxy, facet)

        @staticmethod
        def ice_staticId():
            return "::Glacier2::SSLPermissionsVerifier"
    _M_Glacier2._t_SSLPermissionsVerifierPrx = IcePy.defineProxy("::Glacier2::SSLPermissionsVerifier", SSLPermissionsVerifierPrx)

    _M_Glacier2.SSLPermissionsVerifierPrx = SSLPermissionsVerifierPrx
    del SSLPermissionsVerifierPrx

    _M_Glacier2.SSLPermissionsVerifier = None
    class SSLPermissionsVerifier(Ice.Object):

        def ice_ids(self, current):
            return ("::Glacier2::SSLPermissionsVerifier", "::Ice::Object")

        def ice_id(self, current):
            return "::Glacier2::SSLPermissionsVerifier"

        @staticmethod
        def ice_staticId():
            return "::Glacier2::SSLPermissionsVerifier"

        def authorize(self, info, current):
            """
            Checks if a user is authorized to establish a session.
            
            Parameters
            ----------
            info : Glacier2.SSLInfo
                The SSL information.
            current : Ice.Current
                The Current object for the dispatch.
            
            Returns
            -------
            tuple of (bool, str) or awaitable of tuple of (bool, str)
            
                A tuple containing:
                    - bool `true` if access is granted, `false` otherwise.
                    - str The reason why access was denied.
            
            Raises
            ------
            PermissionDeniedException
                Thrown when the user access is denied. This exception can be thrown
                instead of returning `false` with a reason set in the reason out parameter.
            """
            raise NotImplementedError("servant method 'authorize' not implemented")

    SSLPermissionsVerifier._op_authorize = IcePy.Operation("authorize", "authorize", Ice.OperationMode.Idempotent, None, (), (((), _M_Glacier2._t_SSLInfo, False, 0),), (((), IcePy._t_string, False, 0),), ((), IcePy._t_bool, False, 0), (_M_Glacier2._t_PermissionDeniedException,))

    _M_Glacier2.SSLPermissionsVerifier = SSLPermissionsVerifier
    del SSLPermissionsVerifier

# End of module Glacier2
