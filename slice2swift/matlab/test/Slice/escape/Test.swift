// Copyright (c) ZeroC, Inc.

// slice2swift version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Test.ice'.</auto-generated>

import Foundation
import Ice

public enum persistent: Swift.UInt8, Swift.Sendable {
    case break = 0
    case catch = 1
    case LAST = 2
    public init() {
        self = .break
    }
}

/// An `Ice.InputStream` extension to read `persistent` enumerated values from the stream.
public extension Ice.InputStream {
    /// Read an enumerated value.
    ///
    /// - Returns:  The enumerated value.
    func read() throws -> persistent {
        let rawValue: Swift.UInt8 = try read(enumMaxValue: 2)
        guard let val = persistent(rawValue: rawValue) else {
            throw Ice.MarshalException("invalid enum value")
        }
        return val
    }

    /// Read an optional enumerated value from the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    ///
    /// - Returns: The enumerated value.
    func read(tag: Swift.Int32) throws -> persistent? {
        guard try readOptional(tag: tag, expectedFormat: .Size) else {
            return nil
        }
        return try read() as persistent
    }
}

/// An `Ice.OutputStream` extension to write `persistent` enumerated values to the stream.
public extension Ice.OutputStream {
    /// Writes an enumerated value to the stream.
    ///
    /// - Parameter v: The enumerator to write.
    func write(_ v: persistent) {
        write(enum: v.rawValue, maxValue: 2)
    }

    /// Writes an optional enumerated value to the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    /// - Parameter value: The enumerator to write.
    func write(tag: Swift.Int32, value: persistent?) {
        guard let v = value else {
            return
        }
        write(tag: tag, val: v.rawValue, maxValue: 2)
    }
}

public struct global: Swift.Hashable, Swift.Sendable {
    public var case: persistent = persistent.catch
    public var continue: Swift.Int32 = 1
    public var eq: Swift.Int32 = 2

    public init() {}

    public init(case: persistent = persistent.catch, continue: Swift.Int32 = 1, eq: Swift.Int32 = 2) {
        self.case = case
        self.continue = continue
        self.eq = eq
    }
}

/// An `Ice.InputStream` extension to read `global` structured values from the stream.
public extension Ice.InputStream {
    /// Read a `global` structured value from the stream.
    ///
    /// - Returns: The structured value read from the stream.
    func read() throws -> sending global {
        var v = global()
        v.case = try self.read()
        v.continue = try self.read()
        v.eq = try self.read()
        return v
    }

    /// Read an optional `global?` structured value from the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    ///
    /// - Returns: The structured value read from the stream.
    func read(tag: Swift.Int32) throws -> sending global? {
        guard try readOptional(tag: tag, expectedFormat: .FSize) else {
            return nil
        }
        try skip(4)
        return try read() as global
    }
}

/// An `Ice.OutputStream` extension to write `global` structured values from the stream.
public extension Ice.OutputStream {
    /// Write a `global` structured value to the stream.
    ///
    /// - Parameter v: The value to write to the stream.
    func write(_ v: global) {
        self.write(v.case)
        self.write(v.continue)
        self.write(v.eq)
    }

    /// Write an optional `global?` structured value to the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    /// - Parameter value: The value to write to the stream.
    func write(tag: Swift.Int32, value: global?) {
        if let v = value {
            if writeOptional(tag: tag, format: .FSize) {
                let pos = startSize()
                write(v)
                endSize(position: pos)
            }
        }
    }
}

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_classdef_logical() -> AnyObject.Type {
        logical.self
    }
}

open class logical: Ice.Value {
    public var else: persistent = persistent.break
    public var for: global = global()
    public var int64: Swift.Bool = true

    public required init() {}

    public init(else: persistent = persistent.break, for: global, int64: Swift.Bool = true) {
        self.else = else
        self.for = for
        self.int64 = int64
    }

    /// - Returns: The Slice type ID of the interface supported by this object.
    open override class func ice_staticId() -> Swift.String { "::classdef::logical" }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        nonisolated(unsafe) let iceP_self = self
        iceP_self.else = try istr.read()
        iceP_self.for = try istr.read()
        iceP_self.int64 = try istr.read()
        try istr.endSlice()
    }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: logical.ice_staticId(), compactId: -1, last: true)
        ostr.write(self.else)
        ostr.write(self.for)
        ostr.write(self.int64)
        ostr.endSlice()
    }
}

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_classdef_xor() -> AnyObject.Type {
        xor.self
    }
}

open class xor: logical {
    public var return: Swift.Int32 = 1

    public required init() {
        super.init()
    }

    public init(else: persistent = persistent.break, for: global, int64: Swift.Bool = true, return: Swift.Int32 = 1) {
        self.return = return
        super.init(else: else, for: for, int64: int64)
    }

    /// - Returns: The Slice type ID of the interface supported by this object.
    open override class func ice_staticId() -> Swift.String { "::classdef::xor" }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        nonisolated(unsafe) let iceP_self = self
        iceP_self.return = try istr.read()
        try istr.endSlice()
        try super._iceReadImpl(from: istr);
    }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: xor.ice_staticId(), compactId: -1, last: false)
        ostr.write(self.return)
        ostr.endSlice()
        super._iceWriteImpl(to: ostr);
    }
}

public typealias parfor = [global]

/// Helper class to read and write `parfor` sequence values from
/// `Ice.InputStream` and `Ice.OutputStream`.
public struct parforHelper {
    /// Read a `parfor` sequence from the stream.
    ///
    /// - Parameter istr: The stream to read from.
    ///
    /// - Returns: The sequence read from the stream.
    public static func read(from istr: Ice.InputStream) throws -> sending parfor {
        let sz = try istr.readAndCheckSeqSize(minSize: 9)
        var v = parfor()
        v.reserveCapacity(sz)
        for _ in 0 ..< sz {
            let j: global = try istr.read()
            v.append(j)
        }
        return v
    }

    /// Read an optional `parfor?` sequence from the stream.
    ///
    /// - Parameter istr: The stream to read from.
    /// - Parameter tag: The numeric tag associated with the value.
    ///
    /// - Returns: The sequence read from the stream.
    public static func read(from istr: Ice.InputStream, tag: Swift.Int32) throws -> sending parfor? {
        guard try istr.readOptional(tag: tag, expectedFormat: .FSize) else {
            return nil
        }
        try istr.skip(4)
        return try read(from: istr)
    }

    /// Write a `parfor` sequence to the stream.
    ///
    /// - Parameter ostr: The stream to write to.
    /// - Parameter v: The sequence value to write to the stream.
    public static func write(to ostr: Ice.OutputStream, value v: parfor) {
        ostr.write(size: v.count)
        for item in v {
            ostr.write(item)
        }
    }

    /// Write an optional `parfor?` sequence to the stream.
    ///
    /// - Parameters:
    ///   - ostr: The stream to write to.
    ///   - tag: The numeric tag associated with the value.
    ///   - v: The sequence value to write to the stream.
    public static func write(to ostr: Ice.OutputStream,  tag: Swift.Int32, value v: parfor?) {
        guard let val = v else {
            return
        }
        if ostr.writeOptional(tag: tag, format: .FSize) {
            let pos = ostr.startSize()
            write(to: ostr, value: val)
            ostr.endSize(position: pos)
        }
    }
}

public typealias switch = [Swift.Int32: global]

/// Helper class to read and write `switch` dictionary values from
/// `Ice.InputStream` and `Ice.OutputStream`.
public struct switchHelper {
    /// Read a `switch` dictionary from the stream.
    ///
    /// - Parameter istr: The stream to read from.
    ///
    /// - Returns: The dictionary read from the stream.
    public static func read(from istr: Ice.InputStream) throws -> sending switch {
        let sz = try Swift.Int(istr.readSize())
        var v = switch()
        for _ in 0 ..< sz {
            let key: Swift.Int32 = try istr.read()
            let value: global = try istr.read()
            v[key] = value
        }
        return v
    }

    /// Read an optional `switch?` dictionary from the stream.
    ///
    /// - Parameter istr: The stream to read from.
    /// - Parameter tag: The numeric tag associated with the value.
    ///
    /// - Returns: The dictionary read from the stream.
    public static func read(from istr: Ice.InputStream, tag: Swift.Int32) throws -> sending switch? {
        guard try istr.readOptional(tag: tag, expectedFormat: .FSize) else {
            return nil
        }
        try istr.skip(4)
        return try read(from: istr)
    }

    /// Write a `switch` dictionary to the stream.
    ///
    /// - Parameter ostr: The stream to write to.
    /// - Parameter v: The dictionary value to write to the stream.
    public static func write(to ostr: Ice.OutputStream, value v: switch) {
        ostr.write(size: v.count)
        for (key, value) in v {
            ostr.write(key)
            ostr.write(value)
        }
    }

    /// Write an optional `switch?` dictionary to the stream.
    ///
    /// - Parameters:
    ///   - ostr: The stream to write to.
    ///   - tag: The numeric tag associated with the value.
    ///   - v: The dictionary value to write to the stream.
    public static func write(to ostr: Ice.OutputStream, tag: Swift.Int32, value v: switch?) {
        guard let val = v else {
            return
        }
        if ostr.writeOptional(tag: tag, format: .FSize) {
            let pos = ostr.startSize()
            write(to: ostr, value: val)
            ostr.endSize(position: pos)
        }
    }
}

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_classdef_Base() -> AnyObject.Type {
        Base.self
    }
}

open class Base: Ice.Value {
    public var while: Swift.Int32 = 1

    public required init() {}

    public init(while: Swift.Int32 = 1) {
        self.while = while
    }

    /// - Returns: The Slice type ID of the interface supported by this object.
    open override class func ice_staticId() -> Swift.String { "::classdef::Base" }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        nonisolated(unsafe) let iceP_self = self
        iceP_self.while = try istr.read()
        try istr.endSlice()
    }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: Base.ice_staticId(), compactId: -1, last: true)
        ostr.write(self.while)
        ostr.endSlice()
    }
}

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_classdef_Derived() -> AnyObject.Type {
        Derived.self
    }
}

open class Derived: Base {
    public var if: Swift.Int32 = 2
    public var spmd: parfor = parfor()
    public var otherwise: switch = switch()

    public required init() {
        super.init()
    }

    public init(while: Swift.Int32 = 1, if: Swift.Int32 = 2, spmd: parfor, otherwise: switch) {
        self.if = if
        self.spmd = spmd
        self.otherwise = otherwise
        super.init(while: while)
    }

    /// - Returns: The Slice type ID of the interface supported by this object.
    open override class func ice_staticId() -> Swift.String { "::classdef::Derived" }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        nonisolated(unsafe) let iceP_self = self
        iceP_self.if = try istr.read()
        iceP_self.spmd = try parforHelper.read(from: istr)
        iceP_self.otherwise = try switchHelper.read(from: istr)
        try istr.endSlice()
        try super._iceReadImpl(from: istr);
    }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: Derived.ice_staticId(), compactId: -1, last: false)
        ostr.write(self.if)
        parforHelper.write(to: ostr, value: self.spmd)
        switchHelper.write(to: ostr, value: self.otherwise)
        ostr.endSlice()
        super._iceWriteImpl(to: ostr);
    }
}

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_classdef_bitand() -> AnyObject.Type {
        bitand.self
    }
}

open class bitand: Ice.UserException, @unchecked Sendable {
    public var identifier: Swift.String = "1"
    public var message: Swift.String = "2"
    public var end: logical? = nil

    public required init() {}

    public init(identifier: Swift.String = "1", message: Swift.String = "2", end: logical?) {
        self.identifier = identifier
        self.message = message
        self.end = end
    }

    /// - Returns: The Slice type ID of this exception.
    open override class func ice_staticId() -> Swift.String { "::classdef::bitand" }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: bitand.ice_staticId(), compactId: -1, last: true)
        ostr.write(self.identifier)
        ostr.write(self.message)
        ostr.write(self.end)
        ostr.endSlice()
    }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        self.identifier = try istr.read()
        self.message = try istr.read()
        try istr.read(logical.self) { self.end = $0 }
        try istr.endSlice()
    }

    open override func _usesClasses() -> Swift.Bool {
        return true
    }
}

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_classdef_bitor() -> AnyObject.Type {
        bitor.self
    }
}

open class bitor: bitand, @unchecked Sendable {
    public var enumeration: Swift.Int32 = 1

    public required init() {
        super.init()
    }

    public init(identifier: Swift.String = "1", message: Swift.String = "2", end: logical?, enumeration: Swift.Int32 = 1) {
        self.enumeration = enumeration
        super.init(identifier: identifier, message: message, end: end)
    }

    /// - Returns: The Slice type ID of this exception.
    open override class func ice_staticId() -> Swift.String { "::classdef::bitor" }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: bitor.ice_staticId(), compactId: -1, last: false)
        ostr.write(self.enumeration)
        ostr.endSlice()
        super._iceWriteImpl(to: ostr);
    }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        self.enumeration = try istr.read()
        try istr.endSlice()
        try super._iceReadImpl(from: istr);
    }
}

/// Traits for Slice interface 'operations'.
public struct operationsTraits: Ice.SliceTraits {
    public static let staticIds = ["::Ice::Object", "::classdef::operations"]
    public static let staticId = "::classdef::operations"
}

public protocol operationsPrx: Ice.ObjectPrx {}

private final class operationsPrxI: Ice.ObjectPrxI, operationsPrx, @unchecked Sendable {
    public override class func ice_staticId() -> Swift.String {
        return operationsTraits.staticId
    }
}

/// Makes a new proxy from a communicator and a proxy string.
///
/// - Parameters:
///    - communicator: The communicator of the new proxy.
///    - proxyString: The proxy string to parse.
///    - type: The type of the new proxy.
///
/// - Throws: `Ice.ParseException` if the proxy string is invalid.
///
/// - Returns: A new proxy with the requested type.
public func makeProxy(communicator: Ice.Communicator, proxyString: String, type: operationsPrx.Protocol) throws -> operationsPrx {
    try communicator.makeProxyImpl(proxyString) as operationsPrxI
}

/// Casts a proxy to the requested type. This call contacts the server and verifies that the object
/// implements this type.
///
/// It will throw a local exception if a communication error occurs. You can optionally supply a
/// facet name and a context map.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///   - context: The optional context dictionary for the remote invocation.
///
/// - Returns: A proxy with the requested type or nil if the objet does not support this type.
///
/// - Throws: `Ice.LocalException` if a communication error occurs.
public func checkedCast(prx: Ice.ObjectPrx, type: operationsPrx.Protocol, facet: Swift.String? = nil, context: Ice.Context? = nil) async throws -> operationsPrx? {
    return try await operationsPrxI.checkedCast(prx: prx, facet: facet, context: context) as operationsPrxI?
}

/// Downcasts the given proxy to this type without contacting the remote server.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///
/// - Returns: A proxy with the requested type.
public func uncheckedCast(prx: Ice.ObjectPrx, type: operationsPrx.Protocol, facet: Swift.String? = nil) -> operationsPrx {
    return operationsPrxI.uncheckedCast(prx: prx, facet: facet) as operationsPrxI
}

/// Returns the Slice type id of the interface associated with this proxy type.
///
/// - Parameter type:  The proxy type to retrieve the type id.
///
/// - Returns: The type id of the interface associated with this proxy type.
public func ice_staticId(_ type: operationsPrx.Protocol) -> Swift.String {
    return operationsTraits.staticId
}

/// Extension to `Ice.InputStream` class to support reading proxies of type
/// `operationsPrx`.
public extension Ice.InputStream {
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(_ type: operationsPrx.Protocol) throws -> operationsPrx? {
        return try read() as operationsPrxI?
    }
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter tag:  The numeric tag associated with the value.
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(tag: Swift.Int32, type: operationsPrx.Protocol) throws -> operationsPrx? {
        return try read(tag: tag) as operationsPrxI?
    }
}

public extension operationsPrx {
    func events(obj iceP_obj: Ice.ObjectPrx?, context iceP_context: Swift.String, context: Ice.Context? = nil) async throws {
        return try await _impl._invoke(operation: "events",
                                       mode: .normal,
                                       write: { ostr in
                                           ostr.write(iceP_obj)
                                           ostr.write(iceP_context)
                                       },
                                       context: context)
    }

    func function(context: Ice.Context? = nil) async throws {
        return try await _impl._invoke(operation: "function",
                                       mode: .normal,
                                       context: context)
    }
}

public let methods: Swift.Int32 = 1

public protocol operations: Ice.Dispatcher {
    func events(obj: Ice.ObjectPrx?, context: Swift.String, current: Ice.Current) async throws

    func function(current: Ice.Current) async throws
}

extension operations {
    private static var defaultObject: Ice.Object {
        Ice.DefaultObject<operationsTraits>()
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// - Parameter request: The incoming request.
    /// - Returns: The outgoing response.
    public func dispatch(_ request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try await Self.dispatch(self, request: request)
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// Call this static method from the `dispatch` method of your servant class when you want to reuse a base servant class in a derived servant class.
    /// - Parameters:
    ///   - servant: The servant to dispatch the request to.
    ///   - request: The incoming request.
    /// - Returns: The outgoing response.
    public static func dispatch(_ servant: operations, request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        switch request.current.operation {
        case "events":
            try await servant._iceD_events(request)
        case "function":
            try await servant._iceD_function(request)
        case "ice_id":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_id(request)
        case "ice_ids":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ids(request)
        case "ice_isA":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_isA(request)
        case "ice_ping":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ping(request)
        default:
            throw Ice.OperationNotExistException()
        }
    }

    public func _iceD_events(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try request.current.checkNonIdempotent()
        let istr = request.inputStream
        _ = try istr.startEncapsulation()
        let iceP_obj: Ice.ObjectPrx? = try istr.read(Ice.ObjectPrx.self)
        let iceP_context: Swift.String = try istr.read()
        try await self.events(obj: iceP_obj, context: iceP_context, current: request.current)
        return request.current.makeEmptyOutgoingResponse()
    }

    public func _iceD_function(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try request.current.checkNonIdempotent()
        _ = try request.inputStream.skipEmptyEncapsulation()
        try await self.function(current: request.current)
        return request.current.makeEmptyOutgoingResponse()
    }
}
