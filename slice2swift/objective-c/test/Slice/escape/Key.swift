//
// Copyright (c) ZeroC, Inc. All rights reserved.
//
//
// Ice version 3.7.10
//
// <auto-generated>
//
// Generated from file `Key.ice'
//
// Warning: do not edit this file.
//
// </auto-generated>
//

import Foundation
import Ice
import PromiseKit

public enum `continue`: Swift.UInt8 {
    /// asm
    case asm = 0
    public init() {
        self = .asm
    }
}

/// An `Ice.InputStream` extension to read ``continue`` enumerated values from the stream.
public extension Ice.InputStream {
    /// Read an enumerated value.
    ///
    /// - returns: ``continue`` - The enumarated value.
    func read() throws -> `continue` {
        let rawValue: Swift.UInt8 = try read(enumMaxValue: 0)
        guard let val = `continue`(rawValue: rawValue) else {
            throw Ice.MarshalException(reason: "invalid enum value")
        }
        return val
    }

    /// Read an optional enumerated value from the stream.
    ///
    /// - parameter tag: `Int32` - The numeric tag associated with the value.
    ///
    /// - returns: ``continue`` - The enumerated value.
    func read(tag: Swift.Int32) throws -> `continue`? {
        guard try readOptional(tag: tag, expectedFormat: .Size) else {
            return nil
        }
        return try read() as `continue`
    }
}

/// An `Ice.OutputStream` extension to write ``continue`` enumerated values to the stream.
public extension Ice.OutputStream {
    /// Writes an enumerated value to the stream.
    ///
    /// parameter _: ``continue`` - The enumerator to write.
    func write(_ v: `continue`) {
        write(enum: v.rawValue, maxValue: 0)
    }

    /// Writes an optional enumerated value to the stream.
    ///
    /// parameter tag: `Int32` - The numeric tag associated with the value.
    ///
    /// parameter _: ``continue`` - The enumerator to write.
    func write(tag: Swift.Int32, value: `continue`?) {
        guard let v = value else {
            return
        }
        write(tag: tag, val: v.rawValue, maxValue: 0)
    }
}

public struct auto: Swift.Hashable {
    public var `default`: Swift.Int32 = 0

    public init() {}

    public init(`default`: Swift.Int32) {
        self.`default` = `default`
    }
}

/// An `Ice.InputStream` extension to read `auto` structured values from the stream.
public extension Ice.InputStream {
    /// Read a `auto` structured value from the stream.
    ///
    /// - returns: `auto` - The structured value read from the stream.
    func read() throws -> auto {
        var v = auto()
        v.`default` = try self.read()
        return v
    }

    /// Read an optional `auto?` structured value from the stream.
    ///
    /// - parameter tag: `Swift.Int32` - The numeric tag associated with the value.
    ///
    /// - returns: `auto?` - The structured value read from the stream.
    func read(tag: Swift.Int32) throws -> auto? {
        guard try readOptional(tag: tag, expectedFormat: .VSize) else {
            return nil
        }
        try skipSize()
        return try read() as auto
    }
}

/// An `Ice.OutputStream` extension to write `auto` structured values from the stream.
public extension Ice.OutputStream {
    /// Write a `auto` structured value to the stream.
    ///
    /// - parameter _: `auto` - The value to write to the stream.
    func write(_ v: auto) {
        self.write(v.`default`)
    }

    /// Write an optional `auto?` structured value to the stream.
    ///
    /// - parameter tag: `Swift.Int32` - The numeric tag associated with the value.
    ///
    /// - parameter value: `auto?` - The value to write to the stream.
    func write(tag: Swift.Int32, value: auto?) {
        if let v = value {
            if writeOptional(tag: tag, format: .VSize) {
                write(size: 4)
                write(v)
            }
        }
    }
}

public struct delete: Swift.Hashable {
    public var `else`: Swift.String = ""

    public init() {}

    public init(`else`: Swift.String) {
        self.`else` = `else`
    }
}

/// An `Ice.InputStream` extension to read `delete` structured values from the stream.
public extension Ice.InputStream {
    /// Read a `delete` structured value from the stream.
    ///
    /// - returns: `delete` - The structured value read from the stream.
    func read() throws -> delete {
        var v = delete()
        v.`else` = try self.read()
        return v
    }

    /// Read an optional `delete?` structured value from the stream.
    ///
    /// - parameter tag: `Swift.Int32` - The numeric tag associated with the value.
    ///
    /// - returns: `delete?` - The structured value read from the stream.
    func read(tag: Swift.Int32) throws -> delete? {
        guard try readOptional(tag: tag, expectedFormat: .FSize) else {
            return nil
        }
        try skip(4)
        return try read() as delete
    }
}

/// An `Ice.OutputStream` extension to write `delete` structured values from the stream.
public extension Ice.OutputStream {
    /// Write a `delete` structured value to the stream.
    ///
    /// - parameter _: `delete` - The value to write to the stream.
    func write(_ v: delete) {
        self.write(v.`else`)
    }

    /// Write an optional `delete?` structured value to the stream.
    ///
    /// - parameter tag: `Swift.Int32` - The numeric tag associated with the value.
    ///
    /// - parameter value: `delete?` - The value to write to the stream.
    func write(tag: Swift.Int32, value: delete?) {
        if let v = value {
            if writeOptional(tag: tag, format: .FSize) {
                let pos = startSize()
                write(v)
                endSize(position: pos)
            }
        }
    }
}

/// Traits for Slice interface ``break``.
public struct breakTraits: Ice.SliceTraits {
    public static let staticIds = ["::Ice::Object", "::and::break"]
    public static let staticId = "::and::break"
}

/// Traits for Slice interface `char`.
public struct charTraits: Ice.SliceTraits {
    public static let staticIds = ["::Ice::Object", "::and::char"]
    public static let staticId = "::and::char"
}

/// Traits for Slice interface ``switch``.
public struct switchTraits: Ice.SliceTraits {
    public static let staticIds = ["::Ice::Object", "::and::switch"]
    public static let staticId = "::and::switch"
}

/// Traits for Slice class ``do``.
public struct doTraits: Ice.SliceTraits {
    public static let staticIds = ["::Ice::Object", "::and::break", "::and::char", "::and::do", "::and::switch"]
    public static let staticId = "::and::do"
}

public typealias extern = [auto]

/// Helper class to read and write `extern` sequence values from
/// `Ice.InputStream` and `Ice.OutputStream`.
public struct externHelper {
    /// Read a `extern` sequence from the stream.
    ///
    /// - parameter istr: `Ice.InputStream` - The stream to read from.
    ///
    /// - returns: `extern` - The sequence read from the stream.
    public static func read(from istr: Ice.InputStream) throws -> extern {
        let sz = try istr.readAndCheckSeqSize(minSize: 4)
        var v = extern()
        v.reserveCapacity(sz)
        for _ in 0 ..< sz {
            let j: auto = try istr.read()
            v.append(j)
        }
        return v
    }
    /// Read an optional `extern?` sequence from the stream.
    ///
    /// - parameter istr: `Ice.InputStream` - The stream to read from.
    ///
    /// - parameter tag: `Swift.Int32` - The numeric tag associated with the value.
    ///
    /// - returns: `extern` - The sequence read from the stream.
    public static func read(from istr: Ice.InputStream, tag: Swift.Int32) throws -> extern? {
        guard try istr.readOptional(tag: tag, expectedFormat: .VSize) else {
            return nil
        }
        try istr.skipSize()
        return try read(from: istr)
    }

    /// Wite a `extern` sequence to the stream.
    ///
    /// - parameter ostr: `Ice.OuputStream` - The stream to write to.
    ///
    /// - parameter value: `extern` - The sequence value to write to the stream.
    public static func write(to ostr: Ice.OutputStream, value v: extern) {
        ostr.write(size: v.count)
        for item in v {
            ostr.write(item)
        }
    }

    /// Wite an optional `extern?` sequence to the stream.
    ///
    /// - parameter ostr: `Ice.OuputStream` - The stream to write to.
    ///
    /// - parameter tag: `Int32` - The numeric tag associated with the value.
    ///
    /// - parameter value: `extern` The sequence value to write to the stream.
    public static func write(to ostr: Ice.OutputStream,  tag: Swift.Int32, value v: extern?) {
        guard let val = v else {
            return
        }
        if ostr.writeOptionalVSize(tag: tag, len: val.count, elemSize: 4) {
            write(to: ostr, value: val)
        }
    }
}

public typealias `for` = [Swift.String: auto]

/// Helper class to read and write ``for`` dictionary values from
/// `Ice.InputStream` and `Ice.OutputStream`.
public struct forHelper {
    /// Read a ``for`` dictionary from the stream.
    ///
    /// - parameter istr: `Ice.InputStream` - The stream to read from.
    ///
    /// - returns: ``for`` - The dictionary read from the stream.
    public static func read(from istr: Ice.InputStream) throws -> `for` {
        let sz = try Swift.Int(istr.readSize())
        var v = `for`()
        for _ in 0 ..< sz {
            let key: Swift.String = try istr.read()
            let value: auto = try istr.read()
            v[key] = value
        }
        return v
    }
    /// Read an optional ``for`?` dictionary from the stream.
    ///
    /// - parameter istr: `Ice.InputStream` - The stream to read from.
    ///
    /// - parameter tag: `Int32` - The numeric tag associated with the value.
    ///
    /// - returns: ``for`` - The dictionary read from the stream.
    public static func read(from istr: Ice.InputStream, tag: Swift.Int32) throws -> `for`? {
        guard try istr.readOptional(tag: tag, expectedFormat: .FSize) else {
            return nil
        }
        try istr.skip(4)
        return try read(from: istr)
    }

    /// Wite a ``for`` dictionary to the stream.
    ///
    /// - parameter ostr: `Ice.OuputStream` - The stream to write to.
    ///
    /// - parameter value: ``for`` - The dictionary value to write to the stream.
    public static func write(to ostr: Ice.OutputStream, value v: `for`) {
        ostr.write(size: v.count)
        for (key, value) in v {
            ostr.write(key)
            ostr.write(value)
        }
    }

    /// Wite an optional ``for`?` dictionary to the stream.
    ///
    /// - parameter ostr: `Ice.OuputStream` - The stream to write to.
    ///
    /// - parameter tag: `Int32` - The numeric tag associated with the value.
    ///
    /// - parameter value: ``for`` - The dictionary value to write to the stream.
    public static func write(to ostr: Ice.OutputStream, tag: Swift.Int32, value v: `for`?) {
        guard let val = v else {
            return
        }
        if ostr.writeOptional(tag: tag, format: .FSize) {
            let pos = ostr.startSize()
            write(to: ostr, value: val)
            ostr.endSize(position: pos)
        }
    }
}

/// :nodoc:
public class return_TypeResolver: Ice.UserExceptionTypeResolver {
    public override func type() -> Ice.UserException.Type {
        return `return`.self
    }
}

public extension Ice.ClassResolver {
    @objc static func and_return() -> Ice.UserExceptionTypeResolver {
        return return_TypeResolver()
    }
}

open class `return`: Ice.UserException {
    public var signed: Swift.Int32 = 0

    public required init() {}

    public init(signed: Swift.Int32) {
        self.signed = signed
    }

    /// Returns the Slice type ID of this exception.
    ///
    /// - returns: `Swift.String` - the Slice type ID of this exception.
    open override class func ice_staticId() -> Swift.String {
        return "::and::return"
    }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: `return`.ice_staticId(), compactId: -1, last: true)
        ostr.write(self.signed)
        ostr.endSlice()
    }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        self.signed = try istr.read()
        try istr.endSlice()
    }
}

/// :nodoc:
public class sizeof_TypeResolver: Ice.UserExceptionTypeResolver {
    public override func type() -> Ice.UserException.Type {
        return sizeof.self
    }
}

public extension Ice.ClassResolver {
    @objc static func and_sizeof() -> Ice.UserExceptionTypeResolver {
        return sizeof_TypeResolver()
    }
}

open class sizeof: `return` {
    public var `static`: Swift.Int32 = 0
    public var `switch`: Swift.Int32 = 0

    public required init() {
        super.init()
    }

    public init(signed: Swift.Int32, `static`: Swift.Int32, `switch`: Swift.Int32) {
        self.`static` = `static`
        self.`switch` = `switch`
        super.init(signed: signed)
    }

    /// Returns the Slice type ID of this exception.
    ///
    /// - returns: `Swift.String` - the Slice type ID of this exception.
    open override class func ice_staticId() -> Swift.String {
        return "::and::sizeof"
    }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: sizeof.ice_staticId(), compactId: -1, last: false)
        ostr.write(self.`static`)
        ostr.write(self.`switch`)
        ostr.endSlice()
        super._iceWriteImpl(to: ostr);
    }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        self.`static` = try istr.read()
        self.`switch` = try istr.read()
        try istr.endSlice()
        try super._iceReadImpl(from: istr);
    }
}
public let template: Swift.Int32 = 0

public let this: Swift.Int32 = 0

public let `throw`: Swift.Int32 = 0

public let typedef: Swift.Int32 = 0

public let typeid: Swift.Int32 = 0

public let typename: Swift.Int32 = 0

public let union: Swift.Int32 = 0

public let unsigned: Swift.Int32 = 0

public let using: Swift.Int32 = 0

public let virtual: Swift.Int32 = 0

public let `while`: Swift.Int32 = 0

public let xor: Swift.Int32 = 0


/// breakPrx overview.
///
/// breakPrx Methods:
///
///  - `case`: 
///
///  - caseAsync: 
public protocol breakPrx: Ice.ObjectPrx {}

private final class breakPrxI: Ice.ObjectPrxI, breakPrx {
    public override class func ice_staticId() -> Swift.String {
        return breakTraits.staticId
    }
}

/// Casts a proxy to the requested type. This call contacts the server and verifies that the object
/// implements this type.
///
/// It will throw a local exception if a communication error occurs. You can optionally supply a
/// facet name and a context map.
///
/// - parameter prx: `Ice.ObjectPrx` - The proxy to be cast.
///
/// - parameter type: `breakPrx.Protocol` - The proxy type to cast to.
///
/// - parameter facet: `String` - The optional name of the desired facet.
///
/// - parameter context: `Ice.Context` The optional context dictionary for the remote invocation.
///
/// - returns: `breakPrx` - A proxy with the requested type or nil if the objet does not
///   support this type.
///
/// - throws: `Ice.LocalException` if a communication error occurs.
public func checkedCast(prx: Ice.ObjectPrx, type: breakPrx.Protocol, facet: Swift.String? = nil, context: Ice.Context? = nil) throws -> breakPrx? {
    return try breakPrxI.checkedCast(prx: prx, facet: facet, context: context) as breakPrxI?
}

/// Downcasts the given proxy to this type without contacting the remote server.
///
/// - parameter prx: `Ice.ObjectPrx` The proxy to be cast.
///
/// - parameter type: `breakPrx.Protocol` - The proxy type to cast to.
///
/// - parameter facet: `String` - The optional name of the desired facet
///
/// - returns: `breakPrx` - A proxy with the requested type
public func uncheckedCast(prx: Ice.ObjectPrx, type: breakPrx.Protocol, facet: Swift.String? = nil) -> breakPrx {
    return breakPrxI.uncheckedCast(prx: prx, facet: facet) as breakPrxI
}

/// Returns the Slice type id of the interface or class associated with this proxy type.
///
/// parameter type: `breakPrx.Protocol` -  The proxy type to retrieve the type id.
///
/// returns: `String` - The type id of the interface or class associated with this proxy type.
public func ice_staticId(_ type: breakPrx.Protocol) -> Swift.String {
    return breakTraits.staticId
}

/// Extension to `Ice.InputStream` class to support reading proxy of type
/// `breakPrx`.
public extension Ice.InputStream {
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - parameter type: `breakPrx.Protocol` - The type of the proxy to be extracted.
    ///
    /// - returns: `breakPrx?` - The extracted proxy
    func read(_ type: breakPrx.Protocol) throws -> breakPrx? {
        return try read() as breakPrxI?
    }
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - parameter tag: `Int32` - The numeric tag associated with the value.
    ///
    /// - parameter type: `breakPrx.Protocol` - The type of the proxy to be extracted.
    ///
    /// - returns: `breakPrx` - The extracted proxy.
    func read(tag: Swift.Int32, type: breakPrx.Protocol) throws -> breakPrx? {
        return try read(tag: tag) as breakPrxI?
    }
}

/// breakPrx overview.
///
/// breakPrx Methods:
///
///  - `case`: 
///
///  - caseAsync: 
public extension breakPrx {
    ///
    /// - parameter _: `Swift.Int32`
    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    ///
    /// - returns: `Swift.Int32`
    func `case`(_ iceP_catch: Swift.Int32, context: Ice.Context? = nil) throws -> Swift.Int32 {
        return try _impl._invoke(operation: "case",
                                 mode: .Normal,
                                 write: { ostr in
                                     ostr.write(iceP_catch)
                                 },
                                 read: { istr in
                                     let iceP_try: Swift.Int32 = try istr.read()
                                     return iceP_try
                                 },
                                 context: context)
    }

    ///
    /// - parameter _: `Swift.Int32`
    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    ///
    /// - parameter sentOn: `Dispatch.DispatchQueue?` - Optional dispatch queue used to
    ///   dispatch the sent callback.
    ///
    /// - parameter sentFlags: `Dispatch.DispatchWorkItemFlags?` - Optional dispatch flags used
    ///   to dispatch the sent callback
    ///
    /// - parameter sent: `((Swift.Bool) -> Swift.Void)` - Optional sent callback.
    ///
    /// - returns: `PromiseKit.Promise<Swift.Int32>` - The result of the operation
    func caseAsync(_ iceP_catch: Swift.Int32, context: Ice.Context? = nil, sentOn: Dispatch.DispatchQueue? = nil, sentFlags: Dispatch.DispatchWorkItemFlags? = nil, sent: ((Swift.Bool) -> Swift.Void)? = nil) -> PromiseKit.Promise<Swift.Int32> {
        return _impl._invokeAsync(operation: "case",
                                  mode: .Normal,
                                  write: { ostr in
                                      ostr.write(iceP_catch)
                                  },
                                  read: { istr in
                                      let iceP_try: Swift.Int32 = try istr.read()
                                      return iceP_try
                                  },
                                  context: context,
                                  sentOn: sentOn,
                                  sentFlags: sentFlags,
                                  sent: sent)
    }
}

/// charPrx overview.
///
/// charPrx Methods:
///
///  - explicit: 
///
///  - explicitAsync: 
public protocol charPrx: Ice.ObjectPrx {}

private final class charPrxI: Ice.ObjectPrxI, charPrx {
    public override class func ice_staticId() -> Swift.String {
        return charTraits.staticId
    }
}

/// Casts a proxy to the requested type. This call contacts the server and verifies that the object
/// implements this type.
///
/// It will throw a local exception if a communication error occurs. You can optionally supply a
/// facet name and a context map.
///
/// - parameter prx: `Ice.ObjectPrx` - The proxy to be cast.
///
/// - parameter type: `charPrx.Protocol` - The proxy type to cast to.
///
/// - parameter facet: `String` - The optional name of the desired facet.
///
/// - parameter context: `Ice.Context` The optional context dictionary for the remote invocation.
///
/// - returns: `charPrx` - A proxy with the requested type or nil if the objet does not
///   support this type.
///
/// - throws: `Ice.LocalException` if a communication error occurs.
public func checkedCast(prx: Ice.ObjectPrx, type: charPrx.Protocol, facet: Swift.String? = nil, context: Ice.Context? = nil) throws -> charPrx? {
    return try charPrxI.checkedCast(prx: prx, facet: facet, context: context) as charPrxI?
}

/// Downcasts the given proxy to this type without contacting the remote server.
///
/// - parameter prx: `Ice.ObjectPrx` The proxy to be cast.
///
/// - parameter type: `charPrx.Protocol` - The proxy type to cast to.
///
/// - parameter facet: `String` - The optional name of the desired facet
///
/// - returns: `charPrx` - A proxy with the requested type
public func uncheckedCast(prx: Ice.ObjectPrx, type: charPrx.Protocol, facet: Swift.String? = nil) -> charPrx {
    return charPrxI.uncheckedCast(prx: prx, facet: facet) as charPrxI
}

/// Returns the Slice type id of the interface or class associated with this proxy type.
///
/// parameter type: `charPrx.Protocol` -  The proxy type to retrieve the type id.
///
/// returns: `String` - The type id of the interface or class associated with this proxy type.
public func ice_staticId(_ type: charPrx.Protocol) -> Swift.String {
    return charTraits.staticId
}

/// Extension to `Ice.InputStream` class to support reading proxy of type
/// `charPrx`.
public extension Ice.InputStream {
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - parameter type: `charPrx.Protocol` - The type of the proxy to be extracted.
    ///
    /// - returns: `charPrx?` - The extracted proxy
    func read(_ type: charPrx.Protocol) throws -> charPrx? {
        return try read() as charPrxI?
    }
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - parameter tag: `Int32` - The numeric tag associated with the value.
    ///
    /// - parameter type: `charPrx.Protocol` - The type of the proxy to be extracted.
    ///
    /// - returns: `charPrx` - The extracted proxy.
    func read(tag: Swift.Int32, type: charPrx.Protocol) throws -> charPrx? {
        return try read(tag: tag) as charPrxI?
    }
}

/// charPrx overview.
///
/// charPrx Methods:
///
///  - explicit: 
///
///  - explicitAsync: 
public extension charPrx {
    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    func explicit(context: Ice.Context? = nil) throws {
        try _impl._invoke(operation: "explicit",
                          mode: .Normal,
                          context: context)
    }

    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    ///
    /// - parameter sentOn: `Dispatch.DispatchQueue?` - Optional dispatch queue used to
    ///   dispatch the sent callback.
    ///
    /// - parameter sentFlags: `Dispatch.DispatchWorkItemFlags?` - Optional dispatch flags used
    ///   to dispatch the sent callback
    ///
    /// - parameter sent: `((Swift.Bool) -> Swift.Void)` - Optional sent callback.
    ///
    /// - returns: `PromiseKit.Promise<>` - The result of the operation
    func explicitAsync(context: Ice.Context? = nil, sentOn: Dispatch.DispatchQueue? = nil, sentFlags: Dispatch.DispatchWorkItemFlags? = nil, sent: ((Swift.Bool) -> Swift.Void)? = nil) -> PromiseKit.Promise<Swift.Void> {
        return _impl._invokeAsync(operation: "explicit",
                                  mode: .Normal,
                                  context: context,
                                  sentOn: sentOn,
                                  sentFlags: sentFlags,
                                  sent: sent)
    }
}

/// switchPrx overview.
///
/// switchPrx Methods:
///
///  - foo: 
///
///  - fooAsync: 
///
///  - foo2: 
///
///  - foo2Async: 
///
///  - foo3: 
///
///  - foo3Async: 
public protocol switchPrx: Ice.ObjectPrx {}

private final class switchPrxI: Ice.ObjectPrxI, switchPrx {
    public override class func ice_staticId() -> Swift.String {
        return switchTraits.staticId
    }
}

/// Casts a proxy to the requested type. This call contacts the server and verifies that the object
/// implements this type.
///
/// It will throw a local exception if a communication error occurs. You can optionally supply a
/// facet name and a context map.
///
/// - parameter prx: `Ice.ObjectPrx` - The proxy to be cast.
///
/// - parameter type: `switchPrx.Protocol` - The proxy type to cast to.
///
/// - parameter facet: `String` - The optional name of the desired facet.
///
/// - parameter context: `Ice.Context` The optional context dictionary for the remote invocation.
///
/// - returns: `switchPrx` - A proxy with the requested type or nil if the objet does not
///   support this type.
///
/// - throws: `Ice.LocalException` if a communication error occurs.
public func checkedCast(prx: Ice.ObjectPrx, type: switchPrx.Protocol, facet: Swift.String? = nil, context: Ice.Context? = nil) throws -> switchPrx? {
    return try switchPrxI.checkedCast(prx: prx, facet: facet, context: context) as switchPrxI?
}

/// Downcasts the given proxy to this type without contacting the remote server.
///
/// - parameter prx: `Ice.ObjectPrx` The proxy to be cast.
///
/// - parameter type: `switchPrx.Protocol` - The proxy type to cast to.
///
/// - parameter facet: `String` - The optional name of the desired facet
///
/// - returns: `switchPrx` - A proxy with the requested type
public func uncheckedCast(prx: Ice.ObjectPrx, type: switchPrx.Protocol, facet: Swift.String? = nil) -> switchPrx {
    return switchPrxI.uncheckedCast(prx: prx, facet: facet) as switchPrxI
}

/// Returns the Slice type id of the interface or class associated with this proxy type.
///
/// parameter type: `switchPrx.Protocol` -  The proxy type to retrieve the type id.
///
/// returns: `String` - The type id of the interface or class associated with this proxy type.
public func ice_staticId(_ type: switchPrx.Protocol) -> Swift.String {
    return switchTraits.staticId
}

/// Extension to `Ice.InputStream` class to support reading proxy of type
/// `switchPrx`.
public extension Ice.InputStream {
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - parameter type: `switchPrx.Protocol` - The type of the proxy to be extracted.
    ///
    /// - returns: `switchPrx?` - The extracted proxy
    func read(_ type: switchPrx.Protocol) throws -> switchPrx? {
        return try read() as switchPrxI?
    }
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - parameter tag: `Int32` - The numeric tag associated with the value.
    ///
    /// - parameter type: `switchPrx.Protocol` - The type of the proxy to be extracted.
    ///
    /// - returns: `switchPrx` - The extracted proxy.
    func read(tag: Swift.Int32, type: switchPrx.Protocol) throws -> switchPrx? {
        return try read(tag: tag) as switchPrxI?
    }
}

/// switchPrx overview.
///
/// switchPrx Methods:
///
///  - foo: 
///
///  - fooAsync: 
///
///  - foo2: 
///
///  - foo2Async: 
///
///  - foo3: 
///
///  - foo3Async: 
public extension switchPrx {
    ///
    /// - parameter _: `charPrx?`
    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    ///
    /// - returns: `Swift.Int32`
    func foo(_ iceP_export: charPrx?, context: Ice.Context? = nil) throws -> Swift.Int32 {
        return try _impl._invoke(operation: "foo",
                                 mode: .Normal,
                                 write: { ostr in
                                     ostr.write(iceP_export)
                                 },
                                 read: { istr in
                                     let iceP_volatile: Swift.Int32 = try istr.read()
                                     return iceP_volatile
                                 },
                                 context: context)
    }

    ///
    /// - parameter _: `charPrx?`
    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    ///
    /// - parameter sentOn: `Dispatch.DispatchQueue?` - Optional dispatch queue used to
    ///   dispatch the sent callback.
    ///
    /// - parameter sentFlags: `Dispatch.DispatchWorkItemFlags?` - Optional dispatch flags used
    ///   to dispatch the sent callback
    ///
    /// - parameter sent: `((Swift.Bool) -> Swift.Void)` - Optional sent callback.
    ///
    /// - returns: `PromiseKit.Promise<Swift.Int32>` - The result of the operation
    func fooAsync(_ iceP_export: charPrx?, context: Ice.Context? = nil, sentOn: Dispatch.DispatchQueue? = nil, sentFlags: Dispatch.DispatchWorkItemFlags? = nil, sent: ((Swift.Bool) -> Swift.Void)? = nil) -> PromiseKit.Promise<Swift.Int32> {
        return _impl._invokeAsync(operation: "foo",
                                  mode: .Normal,
                                  write: { ostr in
                                      ostr.write(iceP_export)
                                  },
                                  read: { istr in
                                      let iceP_volatile: Swift.Int32 = try istr.read()
                                      return iceP_volatile
                                  },
                                  context: context,
                                  sentOn: sentOn,
                                  sentFlags: sentFlags,
                                  sent: sent)
    }

    ///
    /// - parameter export: `charPrx?`
    ///
    /// - parameter YES: `Swift.Int32`
    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    ///
    /// - returns: `Swift.Int32`
    func foo2(export iceP_export: charPrx?, YES iceP_YES: Swift.Int32, context: Ice.Context? = nil) throws -> Swift.Int32 {
        return try _impl._invoke(operation: "foo2",
                                 mode: .Normal,
                                 write: { ostr in
                                     ostr.write(iceP_export)
                                     ostr.write(iceP_YES)
                                 },
                                 read: { istr in
                                     let iceP_nil: Swift.Int32 = try istr.read()
                                     return iceP_nil
                                 },
                                 context: context)
    }

    ///
    /// - parameter export: `charPrx?`
    ///
    /// - parameter YES: `Swift.Int32`
    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    ///
    /// - parameter sentOn: `Dispatch.DispatchQueue?` - Optional dispatch queue used to
    ///   dispatch the sent callback.
    ///
    /// - parameter sentFlags: `Dispatch.DispatchWorkItemFlags?` - Optional dispatch flags used
    ///   to dispatch the sent callback
    ///
    /// - parameter sent: `((Swift.Bool) -> Swift.Void)` - Optional sent callback.
    ///
    /// - returns: `PromiseKit.Promise<Swift.Int32>` - The result of the operation
    func foo2Async(export iceP_export: charPrx?, YES iceP_YES: Swift.Int32, context: Ice.Context? = nil, sentOn: Dispatch.DispatchQueue? = nil, sentFlags: Dispatch.DispatchWorkItemFlags? = nil, sent: ((Swift.Bool) -> Swift.Void)? = nil) -> PromiseKit.Promise<Swift.Int32> {
        return _impl._invokeAsync(operation: "foo2",
                                  mode: .Normal,
                                  write: { ostr in
                                      ostr.write(iceP_export)
                                      ostr.write(iceP_YES)
                                  },
                                  read: { istr in
                                      let iceP_nil: Swift.Int32 = try istr.read()
                                      return iceP_nil
                                  },
                                  context: context,
                                  sentOn: sentOn,
                                  sentFlags: sentFlags,
                                  sent: sent)
    }

    ///
    /// - parameter _: `charPrx?`
    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    ///
    /// - returns: `Swift.Int32`
    func foo3(_ iceP_export: charPrx?, context: Ice.Context? = nil) throws -> Swift.Int32 {
        return try _impl._invoke(operation: "foo3",
                                 mode: .Normal,
                                 write: { ostr in
                                     ostr.write(iceP_export)
                                 },
                                 read: { istr in
                                     let iceP_nil: Swift.Int32 = try istr.read()
                                     return iceP_nil
                                 },
                                 context: context)
    }

    ///
    /// - parameter _: `charPrx?`
    ///
    /// - parameter context: `Ice.Context` - Optional request context.
    ///
    /// - parameter sentOn: `Dispatch.DispatchQueue?` - Optional dispatch queue used to
    ///   dispatch the sent callback.
    ///
    /// - parameter sentFlags: `Dispatch.DispatchWorkItemFlags?` - Optional dispatch flags used
    ///   to dispatch the sent callback
    ///
    /// - parameter sent: `((Swift.Bool) -> Swift.Void)` - Optional sent callback.
    ///
    /// - returns: `PromiseKit.Promise<Swift.Int32>` - The result of the operation
    func foo3Async(_ iceP_export: charPrx?, context: Ice.Context? = nil, sentOn: Dispatch.DispatchQueue? = nil, sentFlags: Dispatch.DispatchWorkItemFlags? = nil, sent: ((Swift.Bool) -> Swift.Void)? = nil) -> PromiseKit.Promise<Swift.Int32> {
        return _impl._invokeAsync(operation: "foo3",
                                  mode: .Normal,
                                  write: { ostr in
                                      ostr.write(iceP_export)
                                  },
                                  read: { istr in
                                      let iceP_nil: Swift.Int32 = try istr.read()
                                      return iceP_nil
                                  },
                                  context: context,
                                  sentOn: sentOn,
                                  sentFlags: sentFlags,
                                  sent: sent)
    }
}

/// doPrx overview.
public protocol doPrx: charPrx, breakPrx, switchPrx {}

private final class doPrxI: Ice.ObjectPrxI, doPrx {
    public override class func ice_staticId() -> Swift.String {
        return doTraits.staticId
    }
}

/// Casts a proxy to the requested type. This call contacts the server and verifies that the object
/// implements this type.
///
/// It will throw a local exception if a communication error occurs. You can optionally supply a
/// facet name and a context map.
///
/// - parameter prx: `Ice.ObjectPrx` - The proxy to be cast.
///
/// - parameter type: `doPrx.Protocol` - The proxy type to cast to.
///
/// - parameter facet: `String` - The optional name of the desired facet.
///
/// - parameter context: `Ice.Context` The optional context dictionary for the remote invocation.
///
/// - returns: `doPrx` - A proxy with the requested type or nil if the objet does not
///   support this type.
///
/// - throws: `Ice.LocalException` if a communication error occurs.
public func checkedCast(prx: Ice.ObjectPrx, type: doPrx.Protocol, facet: Swift.String? = nil, context: Ice.Context? = nil) throws -> doPrx? {
    return try doPrxI.checkedCast(prx: prx, facet: facet, context: context) as doPrxI?
}

/// Downcasts the given proxy to this type without contacting the remote server.
///
/// - parameter prx: `Ice.ObjectPrx` The proxy to be cast.
///
/// - parameter type: `doPrx.Protocol` - The proxy type to cast to.
///
/// - parameter facet: `String` - The optional name of the desired facet
///
/// - returns: `doPrx` - A proxy with the requested type
public func uncheckedCast(prx: Ice.ObjectPrx, type: doPrx.Protocol, facet: Swift.String? = nil) -> doPrx {
    return doPrxI.uncheckedCast(prx: prx, facet: facet) as doPrxI
}

/// Returns the Slice type id of the interface or class associated with this proxy type.
///
/// parameter type: `doPrx.Protocol` -  The proxy type to retrieve the type id.
///
/// returns: `String` - The type id of the interface or class associated with this proxy type.
public func ice_staticId(_ type: doPrx.Protocol) -> Swift.String {
    return doTraits.staticId
}

/// Extension to `Ice.InputStream` class to support reading proxy of type
/// `doPrx`.
public extension Ice.InputStream {
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - parameter type: `doPrx.Protocol` - The type of the proxy to be extracted.
    ///
    /// - returns: `doPrx?` - The extracted proxy
    func read(_ type: doPrx.Protocol) throws -> doPrx? {
        return try read() as doPrxI?
    }
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - parameter tag: `Int32` - The numeric tag associated with the value.
    ///
    /// - parameter type: `doPrx.Protocol` - The type of the proxy to be extracted.
    ///
    /// - returns: `doPrx` - The extracted proxy.
    func read(tag: Swift.Int32, type: doPrx.Protocol) throws -> doPrx? {
        return try read(tag: tag) as doPrxI?
    }
}

/// doPrx overview.
public extension doPrx {}

/// :nodoc:
public class do_TypeResolver: Ice.ValueTypeResolver {
    public override func type() -> Ice.Value.Type {
        return `do`.self
    }
}

public extension Ice.ClassResolver {
    @objc static func and_do() -> Ice.ValueTypeResolver {
        return do_TypeResolver()
    }
}

open class `do`: Ice.Value {
    public var `if`: Swift.Int32 = 0

    public required init() {}

    public init(`if`: Swift.Int32) {
        self.`if` = `if`
    }

    /// Returns the Slice type ID of the most-derived interface supported by this object.
    ///
    /// - returns: `String` - The Slice type ID of the most-derived interface supported by this object
    open override func ice_id() -> Swift.String {
        return doTraits.staticId
    }

    /// Returns the Slice type ID of the interface supported by this object.
    ///
    /// - returns: `String` - The Slice type ID of the interface supported by this object.
    open override class func ice_staticId() -> Swift.String {
        return doTraits.staticId
    }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        self.`if` = try istr.read()
        try istr.endSlice()
    }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: doTraits.staticId, compactId: -1, last: true)
        ostr.write(self.`if`)
        ostr.endSlice()
    }
}


/// Dispatcher for ``break`` servants.
public struct breakDisp: Ice.Disp {
    public let servant: `break`
    private static let defaultObject = Ice.ObjectI<breakTraits>()

    public init(_ servant: `break`) {
        self.servant = servant
    }

    public func dispatch(request: Ice.Request, current: Ice.Current) throws -> PromiseKit.Promise<Ice.OutputStream>? {
        request.startOver()
        switch current.operation {
        case "case":
            return try servant._iceD_case(incoming: request, current: current)
        case "ice_id":
            return try (servant as? Object ?? breakDisp.defaultObject)._iceD_ice_id(incoming: request, current: current)
        case "ice_ids":
            return try (servant as? Object ?? breakDisp.defaultObject)._iceD_ice_ids(incoming: request, current: current)
        case "ice_isA":
            return try (servant as? Object ?? breakDisp.defaultObject)._iceD_ice_isA(incoming: request, current: current)
        case "ice_ping":
            return try (servant as? Object ?? breakDisp.defaultObject)._iceD_ice_ping(incoming: request, current: current)
        default:
            throw Ice.OperationNotExistException(id: current.id, facet: current.facet, operation: current.operation)
        }
    }
}

public protocol `break` {
    ///
    /// - parameter catch: `Swift.Int32`
    ///
    /// - parameter current: `Ice.Current` - The Current object for the dispatch.
    ///
    /// - returns: `Swift.Int32`
    func `case`(catch: Swift.Int32, current: Ice.Current) throws -> Swift.Int32
}


/// Dispatcher for `char` servants.
public struct charDisp: Ice.Disp {
    public let servant: char
    private static let defaultObject = Ice.ObjectI<charTraits>()

    public init(_ servant: char) {
        self.servant = servant
    }

    public func dispatch(request: Ice.Request, current: Ice.Current) throws -> PromiseKit.Promise<Ice.OutputStream>? {
        request.startOver()
        switch current.operation {
        case "explicit":
            return try servant._iceD_explicit(incoming: request, current: current)
        case "ice_id":
            return try (servant as? Object ?? charDisp.defaultObject)._iceD_ice_id(incoming: request, current: current)
        case "ice_ids":
            return try (servant as? Object ?? charDisp.defaultObject)._iceD_ice_ids(incoming: request, current: current)
        case "ice_isA":
            return try (servant as? Object ?? charDisp.defaultObject)._iceD_ice_isA(incoming: request, current: current)
        case "ice_ping":
            return try (servant as? Object ?? charDisp.defaultObject)._iceD_ice_ping(incoming: request, current: current)
        default:
            throw Ice.OperationNotExistException(id: current.id, facet: current.facet, operation: current.operation)
        }
    }
}

public protocol char {
    ///
    /// - parameter current: `Ice.Current` - The Current object for the dispatch.
    func explicit(current: Ice.Current) throws
}


/// Dispatcher for ``switch`` servants.
public struct switchDisp: Ice.Disp {
    public let servant: `switch`
    private static let defaultObject = Ice.ObjectI<switchTraits>()

    public init(_ servant: `switch`) {
        self.servant = servant
    }

    public func dispatch(request: Ice.Request, current: Ice.Current) throws -> PromiseKit.Promise<Ice.OutputStream>? {
        request.startOver()
        switch current.operation {
        case "foo":
            return try servant._iceD_foo(incoming: request, current: current)
        case "foo2":
            return try servant._iceD_foo2(incoming: request, current: current)
        case "foo3":
            return try servant._iceD_foo3(incoming: request, current: current)
        case "ice_id":
            return try (servant as? Object ?? switchDisp.defaultObject)._iceD_ice_id(incoming: request, current: current)
        case "ice_ids":
            return try (servant as? Object ?? switchDisp.defaultObject)._iceD_ice_ids(incoming: request, current: current)
        case "ice_isA":
            return try (servant as? Object ?? switchDisp.defaultObject)._iceD_ice_isA(incoming: request, current: current)
        case "ice_ping":
            return try (servant as? Object ?? switchDisp.defaultObject)._iceD_ice_ping(incoming: request, current: current)
        default:
            throw Ice.OperationNotExistException(id: current.id, facet: current.facet, operation: current.operation)
        }
    }
}

public protocol `switch` {
    ///
    /// - parameter export: `charPrx?`
    ///
    /// - parameter current: `Ice.Current` - The Current object for the dispatch.
    ///
    /// - returns: `Swift.Int32`
    func foo(export: charPrx?, current: Ice.Current) throws -> Swift.Int32

    ///
    /// - parameter export: `charPrx?`
    ///
    /// - parameter YES: `Swift.Int32`
    ///
    /// - parameter current: `Ice.Current` - The Current object for the dispatch.
    ///
    /// - returns: `Swift.Int32`
    func foo2(export: charPrx?, YES: Swift.Int32, current: Ice.Current) throws -> Swift.Int32

    ///
    /// - parameter export: `charPrx?`
    ///
    /// - parameter current: `Ice.Current` - The Current object for the dispatch.
    ///
    /// - returns: `Swift.Int32`
    func foo3(export: charPrx?, current: Ice.Current) throws -> Swift.Int32
}


/// Dispatcher for `doOperations` servants.
public struct doDisp: Ice.Disp {
    public let servant: doOperations
    private static let defaultObject = Ice.ObjectI<doTraits>()

    public init(_ servant: doOperations) {
        self.servant = servant
    }

    public func dispatch(request: Ice.Request, current: Ice.Current) throws -> PromiseKit.Promise<Ice.OutputStream>? {
        request.startOver()
        switch current.operation {
        case "case":
            return try servant._iceD_case(incoming: request, current: current)
        case "explicit":
            return try servant._iceD_explicit(incoming: request, current: current)
        case "foo":
            return try servant._iceD_foo(incoming: request, current: current)
        case "foo2":
            return try servant._iceD_foo2(incoming: request, current: current)
        case "foo3":
            return try servant._iceD_foo3(incoming: request, current: current)
        case "ice_id":
            return try (servant as? Object ?? doDisp.defaultObject)._iceD_ice_id(incoming: request, current: current)
        case "ice_ids":
            return try (servant as? Object ?? doDisp.defaultObject)._iceD_ice_ids(incoming: request, current: current)
        case "ice_isA":
            return try (servant as? Object ?? doDisp.defaultObject)._iceD_ice_isA(incoming: request, current: current)
        case "ice_ping":
            return try (servant as? Object ?? doDisp.defaultObject)._iceD_ice_ping(incoming: request, current: current)
        default:
            throw Ice.OperationNotExistException(id: current.id, facet: current.facet, operation: current.operation)
        }
    }
}

public protocol doOperations: char, `break`, `switch` {}

/// break overview.
///
/// break Methods:
///
///  - `case`: 
public extension `break` {
    func _iceD_case(incoming inS: Ice.Incoming, current: Ice.Current) throws -> PromiseKit.Promise<Ice.OutputStream>? {
        let iceP_catch: Swift.Int32 = try inS.read { istr in
            let iceP_catch: Swift.Int32 = try istr.read()
            return iceP_catch
        }

        let iceP_try = try self.`case`(catch: iceP_catch, current: current)

        return inS.setResult{ ostr in
            ostr.write(iceP_try)
        }
    }
}

/// char overview.
///
/// char Methods:
///
///  - explicit: 
public extension char {
    func _iceD_explicit(incoming inS: Ice.Incoming, current: Ice.Current) throws -> PromiseKit.Promise<Ice.OutputStream>? {
        try inS.readEmptyParams()

        try self.explicit(current: current)

        return inS.setResult()
    }
}

/// switch overview.
///
/// switch Methods:
///
///  - foo: 
///
///  - foo2: 
///
///  - foo3: 
public extension `switch` {
    func _iceD_foo(incoming inS: Ice.Incoming, current: Ice.Current) throws -> PromiseKit.Promise<Ice.OutputStream>? {
        let iceP_export: charPrx? = try inS.read { istr in
            let iceP_export: charPrx? = try istr.read(charPrx.self)
            return iceP_export
        }

        let iceP_volatile = try self.foo(export: iceP_export, current: current)

        return inS.setResult{ ostr in
            ostr.write(iceP_volatile)
        }
    }

    func _iceD_foo2(incoming inS: Ice.Incoming, current: Ice.Current) throws -> PromiseKit.Promise<Ice.OutputStream>? {
        let (iceP_export, iceP_YES): (charPrx?, Swift.Int32) = try inS.read { istr in
            let iceP_export: charPrx? = try istr.read(charPrx.self)
            let iceP_YES: Swift.Int32 = try istr.read()
            return (iceP_export, iceP_YES)
        }

        let iceP_nil = try self.foo2(export: iceP_export, YES: iceP_YES, current: current)

        return inS.setResult{ ostr in
            ostr.write(iceP_nil)
        }
    }

    func _iceD_foo3(incoming inS: Ice.Incoming, current: Ice.Current) throws -> PromiseKit.Promise<Ice.OutputStream>? {
        let iceP_export: charPrx? = try inS.read { istr in
            let iceP_export: charPrx? = try istr.read(charPrx.self)
            return iceP_export
        }

        let iceP_nil = try self.foo3(export: iceP_export, current: current)

        return inS.setResult{ ostr in
            ostr.write(iceP_nil)
        }
    }
}

/// do overview.
public extension doOperations {}

public protocol friend:  Swift.AnyObject {
    ///
    /// - parameter if: ``continue``
    ///
    /// - parameter d: `auto`
    ///
    /// - parameter inline: `delete`
    ///
    /// - parameter private: `Ice.Value?`
    ///
    /// - parameter mutable: ``do`?`
    ///
    /// - parameter namespace: `breakPrx?`
    ///
    /// - parameter new: `charPrx?`
    ///
    /// - parameter not: `switchPrx?`
    ///
    /// - parameter operator: `doPrx?`
    ///
    /// - parameter or: `Swift.Int32`
    ///
    /// - parameter protected: `Swift.Int32`
    ///
    /// - parameter public: `Swift.Int32`
    ///
    /// - parameter register: `Swift.Int32`
    ///
    /// - returns: `auto`
    func goto(if: `continue`, d: auto, inline: delete, private: Ice.Value?, mutable: `do`?, namespace: breakPrx?, new: charPrx?, not: switchPrx?, operator: doPrx?, or: Swift.Int32, protected: Swift.Int32, public: Swift.Int32, register: Swift.Int32) throws -> auto

    ///
    /// - parameter bycopy: `Swift.Int32`
    ///
    /// - parameter byref: `Swift.Int32`
    ///
    /// - parameter id: `Swift.Int32`
    ///
    /// - parameter IMP: `Swift.Int32`
    ///
    /// - parameter in: `Swift.Int32`
    ///
    /// - parameter inout: `Swift.Int32`
    ///
    /// - parameter nil: `Swift.Int32`
    ///
    /// - parameter NO: `Swift.Int32`
    ///
    /// - parameter oneway: `Swift.Int32`
    ///
    /// - parameter SEL: `Swift.Int32`
    ///
    /// - parameter super: `Swift.Int32`
    ///
    /// - parameter YES: `Swift.Int32`
    func objc(bycopy: Swift.Int32, byref: Swift.Int32, id: Swift.Int32, IMP: Swift.Int32, in: Swift.Int32, inout: Swift.Int32, nil: Swift.Int32, NO: Swift.Int32, oneway: Swift.Int32, SEL: Swift.Int32, super: Swift.Int32, YES: Swift.Int32) throws
}
