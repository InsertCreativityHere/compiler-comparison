// Copyright (c) ZeroC, Inc.

// slice2swift version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Key.ice'.</auto-generated>

import Foundation
import Ice

public enum andassert: Swift.UInt8, Swift.Sendable {
    case break = 0
    public init() {
        self = .break
    }
}

/// An `Ice.InputStream` extension to read `andassert` enumerated values from the stream.
public extension Ice.InputStream {
    /// Read an enumerated value.
    ///
    /// - Returns:  The enumerated value.
    func read() throws -> andassert {
        let rawValue: Swift.UInt8 = try read(enumMaxValue: 0)
        guard let val = andassert(rawValue: rawValue) else {
            throw Ice.MarshalException("invalid enum value")
        }
        return val
    }

    /// Read an optional enumerated value from the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    ///
    /// - Returns: The enumerated value.
    func read(tag: Swift.Int32) throws -> andassert? {
        guard try readOptional(tag: tag, expectedFormat: .Size) else {
            return nil
        }
        return try read() as andassert
    }
}

/// An `Ice.OutputStream` extension to write `andassert` enumerated values to the stream.
public extension Ice.OutputStream {
    /// Writes an enumerated value to the stream.
    ///
    /// - Parameter v: The enumerator to write.
    func write(_ v: andassert) {
        write(enum: v.rawValue, maxValue: 0)
    }

    /// Writes an optional enumerated value to the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    /// - Parameter value: The enumerator to write.
    func write(tag: Swift.Int32, value: andassert?) {
        guard let v = value else {
            return
        }
        write(tag: tag, val: v.rawValue, maxValue: 0)
    }
}

public struct andcontinue: Swift.Hashable, Swift.Sendable {
    public var def: Swift.Int32 = 0

    public init() {}

    public init(def: Swift.Int32) {
        self.def = def
    }
}

/// An `Ice.InputStream` extension to read `andcontinue` structured values from the stream.
public extension Ice.InputStream {
    /// Read a `andcontinue` structured value from the stream.
    ///
    /// - Returns: The structured value read from the stream.
    func read() throws -> sending andcontinue {
        var v = andcontinue()
        v.def = try self.read()
        return v
    }

    /// Read an optional `andcontinue?` structured value from the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    ///
    /// - Returns: The structured value read from the stream.
    func read(tag: Swift.Int32) throws -> sending andcontinue? {
        guard try readOptional(tag: tag, expectedFormat: .VSize) else {
            return nil
        }
        try skipSize()
        return try read() as andcontinue
    }
}

/// An `Ice.OutputStream` extension to write `andcontinue` structured values from the stream.
public extension Ice.OutputStream {
    /// Write a `andcontinue` structured value to the stream.
    ///
    /// - Parameter v: The value to write to the stream.
    func write(_ v: andcontinue) {
        self.write(v.def)
    }

    /// Write an optional `andcontinue?` structured value to the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    /// - Parameter value: The value to write to the stream.
    func write(tag: Swift.Int32, value: andcontinue?) {
        if let v = value {
            if writeOptional(tag: tag, format: .VSize) {
                write(size: 4)
                write(v)
            }
        }
    }
}

/// Traits for Slice interface 'del'.
public struct anddelTraits: Ice.SliceTraits {
    public static let staticIds = ["::Ice::Object", "::Test::and::del"]
    public static let staticId = "::Test::and::del"
}

public protocol anddelPrx: Ice.ObjectPrx {}

private final class anddelPrxI: Ice.ObjectPrxI, anddelPrx, @unchecked Sendable {
    public override class func ice_staticId() -> Swift.String {
        return anddelTraits.staticId
    }
}

/// Makes a new proxy from a communicator and a proxy string.
///
/// - Parameters:
///    - communicator: The communicator of the new proxy.
///    - proxyString: The proxy string to parse.
///    - type: The type of the new proxy.
///
/// - Throws: `Ice.ParseException` if the proxy string is invalid.
///
/// - Returns: A new proxy with the requested type.
public func makeProxy(communicator: Ice.Communicator, proxyString: String, type: anddelPrx.Protocol) throws -> anddelPrx {
    try communicator.makeProxyImpl(proxyString) as anddelPrxI
}

/// Casts a proxy to the requested type. This call contacts the server and verifies that the object
/// implements this type.
///
/// It will throw a local exception if a communication error occurs. You can optionally supply a
/// facet name and a context map.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///   - context: The optional context dictionary for the remote invocation.
///
/// - Returns: A proxy with the requested type or nil if the objet does not support this type.
///
/// - Throws: `Ice.LocalException` if a communication error occurs.
public func checkedCast(prx: Ice.ObjectPrx, type: anddelPrx.Protocol, facet: Swift.String? = nil, context: Ice.Context? = nil) async throws -> anddelPrx? {
    return try await anddelPrxI.checkedCast(prx: prx, facet: facet, context: context) as anddelPrxI?
}

/// Downcasts the given proxy to this type without contacting the remote server.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///
/// - Returns: A proxy with the requested type.
public func uncheckedCast(prx: Ice.ObjectPrx, type: anddelPrx.Protocol, facet: Swift.String? = nil) -> anddelPrx {
    return anddelPrxI.uncheckedCast(prx: prx, facet: facet) as anddelPrxI
}

/// Returns the Slice type id of the interface associated with this proxy type.
///
/// - Parameter type:  The proxy type to retrieve the type id.
///
/// - Returns: The type id of the interface associated with this proxy type.
public func ice_staticId(_ type: anddelPrx.Protocol) -> Swift.String {
    return anddelTraits.staticId
}

/// Extension to `Ice.InputStream` class to support reading proxies of type
/// `anddelPrx`.
public extension Ice.InputStream {
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(_ type: anddelPrx.Protocol) throws -> anddelPrx? {
        return try read() as anddelPrxI?
    }
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter tag:  The numeric tag associated with the value.
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(tag: Swift.Int32, type: anddelPrx.Protocol) throws -> anddelPrx? {
        return try read(tag: tag) as anddelPrxI?
    }
}

public extension anddelPrx {
    func elif(_ iceP_else: Swift.Int32, context: Ice.Context? = nil) async throws -> Swift.Int32 {
        return try await _impl._invoke(operation: "elif",
                                       mode: .normal,
                                       write: { ostr in
                                           ostr.write(iceP_else)
                                       },
                                       read: read,
                                       context: context)

        func read(istr: Ice.InputStream) throws -> sending Swift.Int32 {
            let iceP_except: Swift.Int32 = try istr.read()
            return iceP_except
        }
    }
}

/// Traits for Slice interface 'exec'.
public struct andexecTraits: Ice.SliceTraits {
    public static let staticIds = ["::Ice::Object", "::Test::and::exec"]
    public static let staticId = "::Test::and::exec"
}

public protocol andexecPrx: Ice.ObjectPrx {}

private final class andexecPrxI: Ice.ObjectPrxI, andexecPrx, @unchecked Sendable {
    public override class func ice_staticId() -> Swift.String {
        return andexecTraits.staticId
    }
}

/// Makes a new proxy from a communicator and a proxy string.
///
/// - Parameters:
///    - communicator: The communicator of the new proxy.
///    - proxyString: The proxy string to parse.
///    - type: The type of the new proxy.
///
/// - Throws: `Ice.ParseException` if the proxy string is invalid.
///
/// - Returns: A new proxy with the requested type.
public func makeProxy(communicator: Ice.Communicator, proxyString: String, type: andexecPrx.Protocol) throws -> andexecPrx {
    try communicator.makeProxyImpl(proxyString) as andexecPrxI
}

/// Casts a proxy to the requested type. This call contacts the server and verifies that the object
/// implements this type.
///
/// It will throw a local exception if a communication error occurs. You can optionally supply a
/// facet name and a context map.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///   - context: The optional context dictionary for the remote invocation.
///
/// - Returns: A proxy with the requested type or nil if the objet does not support this type.
///
/// - Throws: `Ice.LocalException` if a communication error occurs.
public func checkedCast(prx: Ice.ObjectPrx, type: andexecPrx.Protocol, facet: Swift.String? = nil, context: Ice.Context? = nil) async throws -> andexecPrx? {
    return try await andexecPrxI.checkedCast(prx: prx, facet: facet, context: context) as andexecPrxI?
}

/// Downcasts the given proxy to this type without contacting the remote server.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///
/// - Returns: A proxy with the requested type.
public func uncheckedCast(prx: Ice.ObjectPrx, type: andexecPrx.Protocol, facet: Swift.String? = nil) -> andexecPrx {
    return andexecPrxI.uncheckedCast(prx: prx, facet: facet) as andexecPrxI
}

/// Returns the Slice type id of the interface associated with this proxy type.
///
/// - Parameter type:  The proxy type to retrieve the type id.
///
/// - Returns: The type id of the interface associated with this proxy type.
public func ice_staticId(_ type: andexecPrx.Protocol) -> Swift.String {
    return andexecTraits.staticId
}

/// Extension to `Ice.InputStream` class to support reading proxies of type
/// `andexecPrx`.
public extension Ice.InputStream {
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(_ type: andexecPrx.Protocol) throws -> andexecPrx? {
        return try read() as andexecPrxI?
    }
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter tag:  The numeric tag associated with the value.
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(tag: Swift.Int32, type: andexecPrx.Protocol) throws -> andexecPrx? {
        return try read(tag: tag) as andexecPrxI?
    }
}

public extension andexecPrx {
    func finally(context: Ice.Context? = nil) async throws {
        return try await _impl._invoke(operation: "finally",
                                       mode: .normal,
                                       context: context)
    }
}

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_Test_and_for() -> AnyObject.Type {
        andfor.self
    }
}

open class andfor: Ice.Value {
    public var foo: Swift.Int32 = 0
    public var from: andexecPrx? = nil

    public required init() {}

    public init(foo: Swift.Int32, from: andexecPrx?) {
        self.foo = foo
        self.from = from
    }

    /// - Returns: The Slice type ID of the interface supported by this object.
    open override class func ice_staticId() -> Swift.String { "::Test::and::for" }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        nonisolated(unsafe) let iceP_self = self
        iceP_self.foo = try istr.read()
        iceP_self.from = try istr.read(andexecPrx.self)
        try istr.endSlice()
    }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: andfor.ice_staticId(), compactId: -1, last: true)
        ostr.write(self.foo)
        ostr.write(self.from)
        ostr.endSlice()
    }
}

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_Test_and_is() -> AnyObject.Type {
        andis.self
    }
}

open class andis: Ice.UserException, @unchecked Sendable {
    public var bar: Swift.Int32 = 0

    public required init() {}

    public init(bar: Swift.Int32) {
        self.bar = bar
    }

    /// - Returns: The Slice type ID of this exception.
    open override class func ice_staticId() -> Swift.String { "::Test::and::is" }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: andis.ice_staticId(), compactId: -1, last: true)
        ostr.write(self.bar)
        ostr.endSlice()
    }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        self.bar = try istr.read()
        try istr.endSlice()
    }
}

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_Test_and_not() -> AnyObject.Type {
        andnot.self
    }
}

open class andnot: andis, @unchecked Sendable {
    public var pass: Swift.Int32 = 0

    public required init() {
        super.init()
    }

    public init(bar: Swift.Int32, pass: Swift.Int32) {
        self.pass = pass
        super.init(bar: bar)
    }

    /// - Returns: The Slice type ID of this exception.
    open override class func ice_staticId() -> Swift.String { "::Test::and::not" }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: andnot.ice_staticId(), compactId: -1, last: false)
        ostr.write(self.pass)
        ostr.endSlice()
        super._iceWriteImpl(to: ostr);
    }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        self.pass = try istr.read()
        try istr.endSlice()
        try super._iceReadImpl(from: istr);
    }
}

/// Traits for Slice interface 'if'.
public struct andifTraits: Ice.SliceTraits {
    public static let staticIds = ["::Ice::Object", "::Test::and::del", "::Test::and::exec", "::Test::and::if"]
    public static let staticId = "::Test::and::if"
}

public protocol andifPrx: andexecPrx, anddelPrx {}

private final class andifPrxI: Ice.ObjectPrxI, andifPrx, @unchecked Sendable {
    public override class func ice_staticId() -> Swift.String {
        return andifTraits.staticId
    }
}

/// Makes a new proxy from a communicator and a proxy string.
///
/// - Parameters:
///    - communicator: The communicator of the new proxy.
///    - proxyString: The proxy string to parse.
///    - type: The type of the new proxy.
///
/// - Throws: `Ice.ParseException` if the proxy string is invalid.
///
/// - Returns: A new proxy with the requested type.
public func makeProxy(communicator: Ice.Communicator, proxyString: String, type: andifPrx.Protocol) throws -> andifPrx {
    try communicator.makeProxyImpl(proxyString) as andifPrxI
}

/// Casts a proxy to the requested type. This call contacts the server and verifies that the object
/// implements this type.
///
/// It will throw a local exception if a communication error occurs. You can optionally supply a
/// facet name and a context map.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///   - context: The optional context dictionary for the remote invocation.
///
/// - Returns: A proxy with the requested type or nil if the objet does not support this type.
///
/// - Throws: `Ice.LocalException` if a communication error occurs.
public func checkedCast(prx: Ice.ObjectPrx, type: andifPrx.Protocol, facet: Swift.String? = nil, context: Ice.Context? = nil) async throws -> andifPrx? {
    return try await andifPrxI.checkedCast(prx: prx, facet: facet, context: context) as andifPrxI?
}

/// Downcasts the given proxy to this type without contacting the remote server.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///
/// - Returns: A proxy with the requested type.
public func uncheckedCast(prx: Ice.ObjectPrx, type: andifPrx.Protocol, facet: Swift.String? = nil) -> andifPrx {
    return andifPrxI.uncheckedCast(prx: prx, facet: facet) as andifPrxI
}

/// Returns the Slice type id of the interface associated with this proxy type.
///
/// - Parameter type:  The proxy type to retrieve the type id.
///
/// - Returns: The type id of the interface associated with this proxy type.
public func ice_staticId(_ type: andifPrx.Protocol) -> Swift.String {
    return andifTraits.staticId
}

/// Extension to `Ice.InputStream` class to support reading proxies of type
/// `andifPrx`.
public extension Ice.InputStream {
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(_ type: andifPrx.Protocol) throws -> andifPrx? {
        return try read() as andifPrxI?
    }
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter tag:  The numeric tag associated with the value.
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(tag: Swift.Int32, type: andifPrx.Protocol) throws -> andifPrx? {
        return try read(tag: tag) as andifPrxI?
    }
}

public extension andifPrx {
    func raise(else iceP_else: andcontinue, return iceP_return: andfor?, while iceP_while: anddelPrx?, yield iceP_yield: andexecPrx?, or iceP_or: andifPrx?, global iceP_global: Swift.Int32, context: Ice.Context? = nil) async throws -> andassert {
        return try await _impl._invoke(operation: "raise",
                                       mode: .normal,
                                       write: { ostr in
                                           ostr.write(iceP_else)
                                           ostr.write(iceP_return)
                                           ostr.write(iceP_while)
                                           ostr.write(iceP_yield)
                                           ostr.write(iceP_or)
                                           ostr.write(iceP_global)
                                           ostr.writePendingValues()
                                       },
                                       read: read,
                                       userException:{ ex in
                                           do  {
                                               throw ex
                                           } catch let error as andis {
                                               throw error
                                           } catch is Ice.UserException {}
                                       },
                                       context: context)

        func read(istr: Ice.InputStream) throws -> sending andassert {
            let iceP_returnValue: andassert = try istr.read()
            return iceP_returnValue
        }
    }
}

public typealias andimport = [andassert]

/// Helper class to read and write `andimport` sequence values from
/// `Ice.InputStream` and `Ice.OutputStream`.
public struct andimportHelper {
    /// Read a `andimport` sequence from the stream.
    ///
    /// - Parameter istr: The stream to read from.
    ///
    /// - Returns: The sequence read from the stream.
    public static func read(from istr: Ice.InputStream) throws -> sending andimport {
        let sz = try istr.readAndCheckSeqSize(minSize: 1)
        var v = andimport()
        v.reserveCapacity(sz)
        for _ in 0 ..< sz {
            let j: andassert = try istr.read()
            v.append(j)
        }
        return v
    }

    /// Read an optional `andimport?` sequence from the stream.
    ///
    /// - Parameter istr: The stream to read from.
    /// - Parameter tag: The numeric tag associated with the value.
    ///
    /// - Returns: The sequence read from the stream.
    public static func read(from istr: Ice.InputStream, tag: Swift.Int32) throws -> sending andimport? {
        guard try istr.readOptional(tag: tag, expectedFormat: .FSize) else {
            return nil
        }
        try istr.skip(4)
        return try read(from: istr)
    }

    /// Write a `andimport` sequence to the stream.
    ///
    /// - Parameter ostr: The stream to write to.
    /// - Parameter v: The sequence value to write to the stream.
    public static func write(to ostr: Ice.OutputStream, value v: andimport) {
        ostr.write(size: v.count)
        for item in v {
            ostr.write(item)
        }
    }

    /// Write an optional `andimport?` sequence to the stream.
    ///
    /// - Parameters:
    ///   - ostr: The stream to write to.
    ///   - tag: The numeric tag associated with the value.
    ///   - v: The sequence value to write to the stream.
    public static func write(to ostr: Ice.OutputStream,  tag: Swift.Int32, value v: andimport?) {
        guard let val = v else {
            return
        }
        if ostr.writeOptional(tag: tag, format: .FSize) {
            let pos = ostr.startSize()
            write(to: ostr, value: val)
            ostr.endSize(position: pos)
        }
    }
}

public typealias andin = [Swift.String: andassert]

/// Helper class to read and write `andin` dictionary values from
/// `Ice.InputStream` and `Ice.OutputStream`.
public struct andinHelper {
    /// Read a `andin` dictionary from the stream.
    ///
    /// - Parameter istr: The stream to read from.
    ///
    /// - Returns: The dictionary read from the stream.
    public static func read(from istr: Ice.InputStream) throws -> sending andin {
        let sz = try Swift.Int(istr.readSize())
        var v = andin()
        for _ in 0 ..< sz {
            let key: Swift.String = try istr.read()
            let value: andassert = try istr.read()
            v[key] = value
        }
        return v
    }

    /// Read an optional `andin?` dictionary from the stream.
    ///
    /// - Parameter istr: The stream to read from.
    /// - Parameter tag: The numeric tag associated with the value.
    ///
    /// - Returns: The dictionary read from the stream.
    public static func read(from istr: Ice.InputStream, tag: Swift.Int32) throws -> sending andin? {
        guard try istr.readOptional(tag: tag, expectedFormat: .FSize) else {
            return nil
        }
        try istr.skip(4)
        return try read(from: istr)
    }

    /// Write a `andin` dictionary to the stream.
    ///
    /// - Parameter ostr: The stream to write to.
    /// - Parameter v: The dictionary value to write to the stream.
    public static func write(to ostr: Ice.OutputStream, value v: andin) {
        ostr.write(size: v.count)
        for (key, value) in v {
            ostr.write(key)
            ostr.write(value)
        }
    }

    /// Write an optional `andin?` dictionary to the stream.
    ///
    /// - Parameters:
    ///   - ostr: The stream to write to.
    ///   - tag: The numeric tag associated with the value.
    ///   - v: The dictionary value to write to the stream.
    public static func write(to ostr: Ice.OutputStream, tag: Swift.Int32, value v: andin?) {
        guard let val = v else {
            return
        }
        if ostr.writeOptional(tag: tag, format: .FSize) {
            let pos = ostr.startSize()
            write(to: ostr, value: val)
            ostr.endSize(position: pos)
        }
    }
}

public let andlambda: Swift.Int32 = 0

public enum andEnumNone: Swift.UInt8, Swift.Sendable {
    case None = 0
    public init() {
        self = .None
    }
}

/// An `Ice.InputStream` extension to read `andEnumNone` enumerated values from the stream.
public extension Ice.InputStream {
    /// Read an enumerated value.
    ///
    /// - Returns:  The enumerated value.
    func read() throws -> andEnumNone {
        let rawValue: Swift.UInt8 = try read(enumMaxValue: 0)
        guard let val = andEnumNone(rawValue: rawValue) else {
            throw Ice.MarshalException("invalid enum value")
        }
        return val
    }

    /// Read an optional enumerated value from the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    ///
    /// - Returns: The enumerated value.
    func read(tag: Swift.Int32) throws -> andEnumNone? {
        guard try readOptional(tag: tag, expectedFormat: .Size) else {
            return nil
        }
        return try read() as andEnumNone
    }
}

/// An `Ice.OutputStream` extension to write `andEnumNone` enumerated values to the stream.
public extension Ice.OutputStream {
    /// Writes an enumerated value to the stream.
    ///
    /// - Parameter v: The enumerator to write.
    func write(_ v: andEnumNone) {
        write(enum: v.rawValue, maxValue: 0)
    }

    /// Writes an optional enumerated value to the stream.
    ///
    /// - Parameter tag: The numeric tag associated with the value.
    /// - Parameter value: The enumerator to write.
    func write(tag: Swift.Int32, value: andEnumNone?) {
        guard let v = value else {
            return
        }
        write(tag: tag, val: v.rawValue, maxValue: 0)
    }
}

public protocol anddel: Ice.Dispatcher {
    func elif(else: Swift.Int32, current: Ice.Current) async throws -> Swift.Int32
}

public protocol andexec: Ice.Dispatcher {
    func finally(current: Ice.Current) async throws
}

public protocol andif: andexec, anddel {
    func raise(else: andcontinue, return: sending andfor?, while: anddelPrx?, yield: andexecPrx?, or: andifPrx?, global: Swift.Int32, current: Ice.Current) async throws -> andassert
}

extension anddel {
    private static var defaultObject: Ice.Object {
        Ice.DefaultObject<anddelTraits>()
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// - Parameter request: The incoming request.
    /// - Returns: The outgoing response.
    public func dispatch(_ request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try await Self.dispatch(self, request: request)
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// Call this static method from the `dispatch` method of your servant class when you want to reuse a base servant class in a derived servant class.
    /// - Parameters:
    ///   - servant: The servant to dispatch the request to.
    ///   - request: The incoming request.
    /// - Returns: The outgoing response.
    public static func dispatch(_ servant: anddel, request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        switch request.current.operation {
        case "elif":
            try await servant._iceD_elif(request)
        case "ice_id":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_id(request)
        case "ice_ids":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ids(request)
        case "ice_isA":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_isA(request)
        case "ice_ping":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ping(request)
        default:
            throw Ice.OperationNotExistException()
        }
    }

    public func _iceD_elif(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try request.current.checkNonIdempotent()
        let istr = request.inputStream
        _ = try istr.startEncapsulation()
        let iceP_else: Swift.Int32 = try istr.read()
        let result = try await self.elif(else: iceP_else, current: request.current)
        return request.current.makeOutgoingResponse(result, formatType: nil) { ostr, value in 
            let iceP_except = value
            ostr.write(iceP_except)
        }
    }
}

extension andexec {
    private static var defaultObject: Ice.Object {
        Ice.DefaultObject<andexecTraits>()
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// - Parameter request: The incoming request.
    /// - Returns: The outgoing response.
    public func dispatch(_ request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try await Self.dispatch(self, request: request)
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// Call this static method from the `dispatch` method of your servant class when you want to reuse a base servant class in a derived servant class.
    /// - Parameters:
    ///   - servant: The servant to dispatch the request to.
    ///   - request: The incoming request.
    /// - Returns: The outgoing response.
    public static func dispatch(_ servant: andexec, request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        switch request.current.operation {
        case "finally":
            try await servant._iceD_finally(request)
        case "ice_id":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_id(request)
        case "ice_ids":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ids(request)
        case "ice_isA":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_isA(request)
        case "ice_ping":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ping(request)
        default:
            throw Ice.OperationNotExistException()
        }
    }

    public func _iceD_finally(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try request.current.checkNonIdempotent()
        _ = try request.inputStream.skipEmptyEncapsulation()
        try await self.finally(current: request.current)
        return request.current.makeEmptyOutgoingResponse()
    }
}

extension andif {
    private static var defaultObject: Ice.Object {
        Ice.DefaultObject<andifTraits>()
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// - Parameter request: The incoming request.
    /// - Returns: The outgoing response.
    public func dispatch(_ request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try await Self.dispatch(self, request: request)
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// Call this static method from the `dispatch` method of your servant class when you want to reuse a base servant class in a derived servant class.
    /// - Parameters:
    ///   - servant: The servant to dispatch the request to.
    ///   - request: The incoming request.
    /// - Returns: The outgoing response.
    public static func dispatch(_ servant: andif, request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        switch request.current.operation {
        case "finally":
            try await servant._iceD_finally(request)
        case "elif":
            try await servant._iceD_elif(request)
        case "raise":
            try await servant._iceD_raise(request)
        case "ice_id":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_id(request)
        case "ice_ids":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ids(request)
        case "ice_isA":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_isA(request)
        case "ice_ping":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ping(request)
        default:
            throw Ice.OperationNotExistException()
        }
    }

    public func _iceD_raise(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try request.current.checkNonIdempotent()
        let istr = request.inputStream
        _ = try istr.startEncapsulation()
        let iceP_else: andcontinue = try istr.read()
        nonisolated(unsafe) var iceP_return: andfor?
        try istr.read(andfor.self) { iceP_return = $0 }
        let iceP_while: anddelPrx? = try istr.read(anddelPrx.self)
        let iceP_yield: andexecPrx? = try istr.read(andexecPrx.self)
        let iceP_or: andifPrx? = try istr.read(andifPrx.self)
        let iceP_global: Swift.Int32 = try istr.read()
        try istr.readPendingValues()
        let result = try await self.raise(else: iceP_else, return: iceP_return, while: iceP_while, yield: iceP_yield, or: iceP_or, global: iceP_global, current: request.current)
        return request.current.makeOutgoingResponse(result, formatType: nil) { ostr, value in 
            let iceP_returnValue = value
            ostr.write(iceP_returnValue)
        }
    }
}
