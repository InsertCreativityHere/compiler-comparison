// Copyright (c) ZeroC, Inc.

// slice2swift version 3.8.0-alpha.0
// <auto-generated>Generated from Slice file 'Router.ice'.</auto-generated>

import Foundation
import Ice

public extension Ice.DefaultSliceLoader {
    @objc static func resolveTypeId_Glacier2_SessionNotExistException() -> AnyObject.Type {
        SessionNotExistException.self
    }
}

/// The exception that is thrown when a client tries to destroy a session with a router, but no session exists for
/// this client.
open class SessionNotExistException: Ice.UserException, @unchecked Sendable {
    /// - Returns: The Slice type ID of this exception.
    open override class func ice_staticId() -> Swift.String { "::Glacier2::SessionNotExistException" }

    open override func _iceWriteImpl(to ostr: Ice.OutputStream) {
        ostr.startSlice(typeId: SessionNotExistException.ice_staticId(), compactId: -1, last: true)
        ostr.endSlice()
    }

    open override func _iceReadImpl(from istr: Ice.InputStream) throws {
        _ = try istr.startSlice()
        try istr.endSlice()
    }
}

/// Traits for Slice interface 'Router'.
public struct RouterTraits: Ice.SliceTraits {
    public static let staticIds = ["::Glacier2::Router", "::Ice::Object", "::Ice::Router"]
    public static let staticId = "::Glacier2::Router"
}

/// The Glacier2 specialization of the `Ice/RouterPrx` interface.
///
/// RouterPrx Methods:
///  - getCategoryForClient: Gets a unique category that identifies the client (caller) in the router.
///  - getCategoryForClientAsync: Gets a unique category that identifies the client (caller) in the router.
///  - createSession: Creates a session for the client (caller) with the router.
///  - createSessionAsync: Creates a session for the client (caller) with the router.
///  - createSessionFromSecureConnection: Creates a per-client session with the router.
///  - createSessionFromSecureConnectionAsync: Creates a per-client session with the router.
///  - refreshSession: Keeps the session with this router alive.
///  - refreshSessionAsync: Keeps the session with this router alive.
///  - destroySession: Destroys the session of the caller with this router.
///  - destroySessionAsync: Destroys the session of the caller with this router.
///  - getSessionTimeout: Gets the idle timeout used by the server-side of the connection.
///  - getSessionTimeoutAsync: Gets the idle timeout used by the server-side of the connection.
///  - getACMTimeout: Gets the idle timeout used by the server-side of the connection.
///  - getACMTimeoutAsync: Gets the idle timeout used by the server-side of the connection.
public protocol RouterPrx: Ice.RouterPrx {}

private final class RouterPrxI: Ice.ObjectPrxI, RouterPrx, @unchecked Sendable {
    public override class func ice_staticId() -> Swift.String {
        return RouterTraits.staticId
    }
}

/// Makes a new proxy from a communicator and a proxy string.
///
/// - Parameters:
///    - communicator: The communicator of the new proxy.
///    - proxyString: The proxy string to parse.
///    - type: The type of the new proxy.
///
/// - Throws: `Ice.ParseException` if the proxy string is invalid.
///
/// - Returns: A new proxy with the requested type.
public func makeProxy(communicator: Ice.Communicator, proxyString: String, type: RouterPrx.Protocol) throws -> RouterPrx {
    try communicator.makeProxyImpl(proxyString) as RouterPrxI
}

/// Casts a proxy to the requested type. This call contacts the server and verifies that the object
/// implements this type.
///
/// It will throw a local exception if a communication error occurs. You can optionally supply a
/// facet name and a context map.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///   - context: The optional context dictionary for the remote invocation.
///
/// - Returns: A proxy with the requested type or nil if the objet does not support this type.
///
/// - Throws: `Ice.LocalException` if a communication error occurs.
public func checkedCast(prx: Ice.ObjectPrx, type: RouterPrx.Protocol, facet: Swift.String? = nil, context: Ice.Context? = nil) async throws -> RouterPrx? {
    return try await RouterPrxI.checkedCast(prx: prx, facet: facet, context: context) as RouterPrxI?
}

/// Downcasts the given proxy to this type without contacting the remote server.
///
/// - Parameters:
///   - prx: The proxy to be cast.
///   - type: The proxy type to cast to.
///   - facet: The optional name of the desired facet.
///
/// - Returns: A proxy with the requested type.
public func uncheckedCast(prx: Ice.ObjectPrx, type: RouterPrx.Protocol, facet: Swift.String? = nil) -> RouterPrx {
    return RouterPrxI.uncheckedCast(prx: prx, facet: facet) as RouterPrxI
}

/// Returns the Slice type id of the interface associated with this proxy type.
///
/// - Parameter type:  The proxy type to retrieve the type id.
///
/// - Returns: The type id of the interface associated with this proxy type.
public func ice_staticId(_ type: RouterPrx.Protocol) -> Swift.String {
    return RouterTraits.staticId
}

/// Extension to `Ice.InputStream` class to support reading proxies of type
/// `RouterPrx`.
public extension Ice.InputStream {
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(_ type: RouterPrx.Protocol) throws -> RouterPrx? {
        return try read() as RouterPrxI?
    }
    /// Extracts a proxy from the stream. The stream must have been initialized with a communicator.
    ///
    /// - Parameter tag:  The numeric tag associated with the value.
    /// - Parameter type: The type of the proxy to be extracted.
    ///
    /// - Returns: The extracted proxy.
    func read(tag: Swift.Int32, type: RouterPrx.Protocol) throws -> RouterPrx? {
        return try read(tag: tag) as RouterPrxI?
    }
}

/// The Glacier2 specialization of the `Ice/RouterPrx` interface.
///
/// RouterPrx Methods:
///  - getCategoryForClient: Gets a unique category that identifies the client (caller) in the router.
///  - getCategoryForClientAsync: Gets a unique category that identifies the client (caller) in the router.
///  - createSession: Creates a session for the client (caller) with the router.
///  - createSessionAsync: Creates a session for the client (caller) with the router.
///  - createSessionFromSecureConnection: Creates a per-client session with the router.
///  - createSessionFromSecureConnectionAsync: Creates a per-client session with the router.
///  - refreshSession: Keeps the session with this router alive.
///  - refreshSessionAsync: Keeps the session with this router alive.
///  - destroySession: Destroys the session of the caller with this router.
///  - destroySessionAsync: Destroys the session of the caller with this router.
///  - getSessionTimeout: Gets the idle timeout used by the server-side of the connection.
///  - getSessionTimeoutAsync: Gets the idle timeout used by the server-side of the connection.
///  - getACMTimeout: Gets the idle timeout used by the server-side of the connection.
///  - getACMTimeoutAsync: Gets the idle timeout used by the server-side of the connection.
public extension RouterPrx {
    /// Gets a unique category that identifies the client (caller) in the router. This category must be used in the
    /// identities of all the client's callback objects.
    ///
    /// - Parameter context: Optional request context.
    ///
    /// - Returns: The category. It's an empty string when `Glacier2.Server.Endpoints` is not configured.
    func getCategoryForClient(context: Ice.Context? = nil) async throws -> Swift.String {
        return try await _impl._invoke(operation: "getCategoryForClient",
                                       mode: .idempotent,
                                       read: read,
                                       context: context)

        func read(istr: Ice.InputStream) throws -> sending Swift.String {
            let iceP_returnValue: Swift.String = try istr.read()
            return iceP_returnValue
        }
    }

    /// Creates a session for the client (caller) with the router. If a ``SessionManagerPrx`` is configured,
    /// a proxy to a ``SessionPrx`` object is returned to the client. Otherwise, null is returned and only an
    /// internal session (i.e., not visible to the client) is created.
    /// If a non-null session proxy is returned, it must be configured to route through the router that created it.
    /// This occurs automatically when the router is configured as the client's default router at the time the
    /// session proxy is created in the client application; otherwise, the client must configure the session proxy
    /// explicitly.
    ///
    /// - Parameters:
    ///   - iceP_userId: The user ID.
    ///   - iceP_password: The password.
    ///   - context: Optional request context.
    ///
    /// - Returns: A proxy for the newly created session, or null if no ``SessionManagerPrx`` is configured.
    ///
    /// - Throws: CannotCreateSessionException Thrown when the session cannot be created. PermissionDeniedException Thrown when an authentication or authorization failure occurs.
    func createSession(userId iceP_userId: Swift.String, password iceP_password: Swift.String, context: Ice.Context? = nil) async throws -> SessionPrx? {
        return try await _impl._invoke(operation: "createSession",
                                       mode: .normal,
                                       write: { ostr in
                                           ostr.write(iceP_userId)
                                           ostr.write(iceP_password)
                                       },
                                       read: read,
                                       userException:{ ex in
                                           do  {
                                               throw ex
                                           } catch let error as PermissionDeniedException {
                                               throw error
                                           } catch let error as CannotCreateSessionException {
                                               throw error
                                           } catch is Ice.UserException {}
                                       },
                                       context: context)

        func read(istr: Ice.InputStream) throws -> sending SessionPrx? {
            let iceP_returnValue: SessionPrx? = try istr.read(SessionPrx.self)
            return iceP_returnValue
        }
    }

    /// Creates a per-client session with the router. The user is authenticated through the SSL certificate(s)
    /// associated with the connection. If an ``SSLSessionManagerPrx`` is configured, a proxy to a ``SessionPrx``
    /// object is returned to the client. Otherwise, null is returned and only an internal session (i.e., not
    /// visible to the client) is created.
    /// If a non-null session proxy is returned, it must be configured to route through the router that created it.
    /// This occurs automatically when the router is configured as the client's default router at the time the
    /// session proxy is created in the client application; otherwise, the client must configure the session proxy
    /// explicitly.
    ///
    /// - Parameter context: Optional request context.
    ///
    /// - Returns: A proxy for the newly created session, or null if no ``SSLSessionManagerPrx`` is configured.
    ///
    /// - Throws: CannotCreateSessionException Thrown when the session cannot be created. PermissionDeniedException Thrown when an authentication or authorization failure occurs.
    func createSessionFromSecureConnection(context: Ice.Context? = nil) async throws -> SessionPrx? {
        return try await _impl._invoke(operation: "createSessionFromSecureConnection",
                                       mode: .normal,
                                       read: read,
                                       userException:{ ex in
                                           do  {
                                               throw ex
                                           } catch let error as PermissionDeniedException {
                                               throw error
                                           } catch let error as CannotCreateSessionException {
                                               throw error
                                           } catch is Ice.UserException {}
                                       },
                                       context: context)

        func read(istr: Ice.InputStream) throws -> sending SessionPrx? {
            let iceP_returnValue: SessionPrx? = try istr.read(SessionPrx.self)
            return iceP_returnValue
        }
    }

    /// Keeps the session with this router alive.
    ///
    /// - Parameter context: Optional request context.
    ///
    /// - Throws:
    ///   - SessionNotExistException Thrown when no session exists for the caller (client).
    func refreshSession(context: Ice.Context? = nil) async throws {
        return try await _impl._invoke(operation: "refreshSession",
                                       mode: .normal,
                                       userException:{ ex in
                                           do  {
                                               throw ex
                                           } catch let error as SessionNotExistException {
                                               throw error
                                           } catch is Ice.UserException {}
                                       },
                                       context: context)
    }

    /// Destroys the session of the caller with this router.
    ///
    /// - Parameter context: Optional request context.
    ///
    /// - Throws:
    ///   - SessionNotExistException Thrown when no session exists for the caller (client).
    func destroySession(context: Ice.Context? = nil) async throws {
        return try await _impl._invoke(operation: "destroySession",
                                       mode: .normal,
                                       userException:{ ex in
                                           do  {
                                               throw ex
                                           } catch let error as SessionNotExistException {
                                               throw error
                                           } catch is Ice.UserException {}
                                       },
                                       context: context)
    }

    /// Gets the idle timeout used by the server-side of the connection.
    ///
    /// - Parameter context: Optional request context.
    ///
    /// - Returns: The idle timeout (in seconds).
    func getSessionTimeout(context: Ice.Context? = nil) async throws -> Swift.Int64 {
        return try await _impl._invoke(operation: "getSessionTimeout",
                                       mode: .idempotent,
                                       read: read,
                                       context: context)

        func read(istr: Ice.InputStream) throws -> sending Swift.Int64 {
            let iceP_returnValue: Swift.Int64 = try istr.read()
            return iceP_returnValue
        }
    }

    /// Gets the idle timeout used by the server-side of the connection.
    ///
    /// - Parameter context: Optional request context.
    ///
    /// - Returns: The idle timeout (in seconds).
    func getACMTimeout(context: Ice.Context? = nil) async throws -> Swift.Int32 {
        return try await _impl._invoke(operation: "getACMTimeout",
                                       mode: .idempotent,
                                       read: read,
                                       context: context)

        func read(istr: Ice.InputStream) throws -> sending Swift.Int32 {
            let iceP_returnValue: Swift.Int32 = try istr.read()
            return iceP_returnValue
        }
    }
}

/// The Glacier2 specialization of the `Ice/RouterPrx` interface.
public protocol Router: Ice.Router {
    /// Gets a unique category that identifies the client (caller) in the router. This category must be used in the
    /// identities of all the client's callback objects.
    ///
    /// - Parameter current: The Current object for the dispatch.
    ///
    /// - Returns: The category. It's an empty string when `Glacier2.Server.Endpoints` is not configured.
    func getCategoryForClient(current: Ice.Current) async throws -> Swift.String

    /// Creates a session for the client (caller) with the router. If a ``SessionManagerPrx`` is configured,
    /// a proxy to a ``SessionPrx`` object is returned to the client. Otherwise, null is returned and only an
    /// internal session (i.e., not visible to the client) is created.
    /// If a non-null session proxy is returned, it must be configured to route through the router that created it.
    /// This occurs automatically when the router is configured as the client's default router at the time the
    /// session proxy is created in the client application; otherwise, the client must configure the session proxy
    /// explicitly.
    ///
    /// - Parameters:
    ///   - userId: The user ID.
    ///   - password: The password.
    ///   - current: The Current object for the dispatch.
    ///
    /// - Returns: A proxy for the newly created session, or null if no ``SessionManagerPrx`` is configured.
    ///
    /// - Throws: CannotCreateSessionException Thrown when the session cannot be created. PermissionDeniedException Thrown when an authentication or authorization failure occurs.
    func createSession(userId: Swift.String, password: Swift.String, current: Ice.Current) async throws -> SessionPrx?

    /// Creates a per-client session with the router. The user is authenticated through the SSL certificate(s)
    /// associated with the connection. If an ``SSLSessionManagerPrx`` is configured, a proxy to a ``SessionPrx``
    /// object is returned to the client. Otherwise, null is returned and only an internal session (i.e., not
    /// visible to the client) is created.
    /// If a non-null session proxy is returned, it must be configured to route through the router that created it.
    /// This occurs automatically when the router is configured as the client's default router at the time the
    /// session proxy is created in the client application; otherwise, the client must configure the session proxy
    /// explicitly.
    ///
    /// - Parameter current: The Current object for the dispatch.
    ///
    /// - Returns: A proxy for the newly created session, or null if no ``SSLSessionManagerPrx`` is configured.
    ///
    /// - Throws: CannotCreateSessionException Thrown when the session cannot be created. PermissionDeniedException Thrown when an authentication or authorization failure occurs.
    func createSessionFromSecureConnection(current: Ice.Current) async throws -> SessionPrx?

    /// Keeps the session with this router alive.
    ///
    /// - Parameter current: The Current object for the dispatch.
    ///
    /// - Throws:
    ///   - SessionNotExistException Thrown when no session exists for the caller (client).
    func refreshSession(current: Ice.Current) async throws

    /// Destroys the session of the caller with this router.
    ///
    /// - Parameter current: The Current object for the dispatch.
    ///
    /// - Throws:
    ///   - SessionNotExistException Thrown when no session exists for the caller (client).
    func destroySession(current: Ice.Current) async throws

    /// Gets the idle timeout used by the server-side of the connection.
    ///
    /// - Parameter current: The Current object for the dispatch.
    ///
    /// - Returns: The idle timeout (in seconds).
    func getSessionTimeout(current: Ice.Current) async throws -> Swift.Int64

    /// Gets the idle timeout used by the server-side of the connection.
    ///
    /// - Parameter current: The Current object for the dispatch.
    ///
    /// - Returns: The idle timeout (in seconds).
    func getACMTimeout(current: Ice.Current) async throws -> Swift.Int32
}

/// The Glacier2 specialization of the `Ice/RouterPrx` interface.
///
/// Router Methods:
///  - getCategoryForClient: Gets a unique category that identifies the client (caller) in the router.
///  - createSession: Creates a session for the client (caller) with the router.
///  - createSessionFromSecureConnection: Creates a per-client session with the router.
///  - refreshSession: Keeps the session with this router alive.
///  - destroySession: Destroys the session of the caller with this router.
///  - getSessionTimeout: Gets the idle timeout used by the server-side of the connection.
///  - getACMTimeout: Gets the idle timeout used by the server-side of the connection.
extension Router {
    private static var defaultObject: Ice.Object {
        Ice.DefaultObject<RouterTraits>()
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// - Parameter request: The incoming request.
    /// - Returns: The outgoing response.
    public func dispatch(_ request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try await Self.dispatch(self, request: request)
    }

    /// Dispatches an incoming request to one of the instance methods of the generated protocol, based on the operation name carried by the request.
    /// Call this static method from the `dispatch` method of your servant class when you want to reuse a base servant class in a derived servant class.
    /// - Parameters:
    ///   - servant: The servant to dispatch the request to.
    ///   - request: The incoming request.
    /// - Returns: The outgoing response.
    public static func dispatch(_ servant: Router, request: sending Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        switch request.current.operation {
        case "getClientProxy":
            try await servant._iceD_getClientProxy(request)
        case "getServerProxy":
            try await servant._iceD_getServerProxy(request)
        case "addProxies":
            try await servant._iceD_addProxies(request)
        case "getCategoryForClient":
            try await servant._iceD_getCategoryForClient(request)
        case "createSession":
            try await servant._iceD_createSession(request)
        case "createSessionFromSecureConnection":
            try await servant._iceD_createSessionFromSecureConnection(request)
        case "refreshSession":
            try await servant._iceD_refreshSession(request)
        case "destroySession":
            try await servant._iceD_destroySession(request)
        case "getSessionTimeout":
            try await servant._iceD_getSessionTimeout(request)
        case "getACMTimeout":
            try await servant._iceD_getACMTimeout(request)
        case "ice_id":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_id(request)
        case "ice_ids":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ids(request)
        case "ice_isA":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_isA(request)
        case "ice_ping":
            try await (servant as? Ice.Object ?? Self.defaultObject)._iceD_ice_ping(request)
        default:
            throw Ice.OperationNotExistException()
        }
    }

    public func _iceD_getCategoryForClient(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        _ = try request.inputStream.skipEmptyEncapsulation()
        let result = try await self.getCategoryForClient(current: request.current)
        return request.current.makeOutgoingResponse(result, formatType: nil) { ostr, value in 
            let iceP_returnValue = value
            ostr.write(iceP_returnValue)
        }
    }

    public func _iceD_createSession(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try request.current.checkNonIdempotent()
        let istr = request.inputStream
        _ = try istr.startEncapsulation()
        let iceP_userId: Swift.String = try istr.read()
        let iceP_password: Swift.String = try istr.read()
        let result = try await self.createSession(userId: iceP_userId, password: iceP_password, current: request.current)
        return request.current.makeOutgoingResponse(result, formatType: nil) { ostr, value in 
            let iceP_returnValue = value
            ostr.write(iceP_returnValue)
        }
    }

    public func _iceD_createSessionFromSecureConnection(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try request.current.checkNonIdempotent()
        _ = try request.inputStream.skipEmptyEncapsulation()
        let result = try await self.createSessionFromSecureConnection(current: request.current)
        return request.current.makeOutgoingResponse(result, formatType: nil) { ostr, value in 
            let iceP_returnValue = value
            ostr.write(iceP_returnValue)
        }
    }

    public func _iceD_refreshSession(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try request.current.checkNonIdempotent()
        _ = try request.inputStream.skipEmptyEncapsulation()
        try await self.refreshSession(current: request.current)
        return request.current.makeEmptyOutgoingResponse()
    }

    public func _iceD_destroySession(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        try request.current.checkNonIdempotent()
        _ = try request.inputStream.skipEmptyEncapsulation()
        try await self.destroySession(current: request.current)
        return request.current.makeEmptyOutgoingResponse()
    }

    public func _iceD_getSessionTimeout(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        _ = try request.inputStream.skipEmptyEncapsulation()
        let result = try await self.getSessionTimeout(current: request.current)
        return request.current.makeOutgoingResponse(result, formatType: nil) { ostr, value in 
            let iceP_returnValue = value
            ostr.write(iceP_returnValue)
        }
    }

    public func _iceD_getACMTimeout(_ request: Ice.IncomingRequest) async throws -> Ice.OutgoingResponse {
        _ = try request.inputStream.skipEmptyEncapsulation()
        let result = try await self.getACMTimeout(current: request.current)
        return request.current.makeOutgoingResponse(result, formatType: nil) { ostr, value in 
            let iceP_returnValue = value
            ostr.write(iceP_returnValue)
        }
    }
}
